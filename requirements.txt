# SENTIENTIA Content Pipeline — Python dependencies.
# Pinned to compatible minor ranges; bump after CI green on the bump PR.

# Agent 1 — PDF SOP parser.
pdfplumber>=0.11,<0.12

# Test runner (pytest fixtures + assertions).
pytest>=8.0,<10.0

# Agent 1 unit-test fixture generator (renders the sample PDF).
reportlab>=4.0,<5.0
