# UAT asks - ready to send (2026-09-03)

Two draft messages, grounded in `UAT-VALIDATION-PLAN-2026-09-03.md` (Asks
outstanding, Phases 2-4), `UAT-SENTIENTIA-DEPLOY-CHECKLIST.md` (sections 0, 1,
6), `ENTERPRISE-IDENTITY-PACK.md` (sections 1-3) and
`OAUTH2-SMTP-M365-RUNBOOK.md` (sections 1-2). No credentials included.

---

## Message 1 - Amul / Cloud.in (ticket HS-20260819-79876)

**To:** Amul (Ganesh's reporting manager)
**Cc:** Cloud.in support - ticket HS-20260819-79876
**Subject:** UAT Sentientia - Stage A passed; 5 items before the live-data rehearsal

Hi Amul, team,

Stage A (fresh install) passed on 3 September; the site is live at
https://academy2.airpay.ninja. Thanks for the fast provisioning. Before we
restore the live backup for the rehearsal (Stage B, real employee data), five
items need to close. Proposing items 1, 2 and 4 within 3 working days; item 3
once we send our capacity baseline - please confirm these work for you.

1. Office/VPN egress IP allowlist on the LB security group. WHY: the LB is
   currently open to the internet, and Stage B restores real employee PII
   into UAT. Please tell us which egress IP(s) you need us to confirm so you
   can lock the security group down. BY: 3 working days.

2. Current size of live moodledata (including filedir) and of the DB dump,
   plus RDS storage headroom (autoscaling is off, 10-20 GB provisioned). WHY:
   we need these to plan disk before the restore. BY: 3 working days.

3. Resize decision: EC2 to t3a.medium or larger, RDS to db.t3.medium. WHY:
   the ticket lists "4 GB RAM" for db.t3.small, which is actually a 2 GB
   class; the current t3a.small/db.t3.small pair is fine for the install
   smoke but will be tight under real load. BY: after we send our capacity
   baseline.

4. At the LB/Apache layer: HSTS response header, `ServerTokens Prod` +
   `ServerSignature Off`, and CloudWatch alarms for disk and CPU on both EC2
   and RDS. WHY: baseline security and observability before real data lands.
   BY: 3 working days.

5. Housekeeping: remove the unused, world-writable /var/www/html/moodledata
   directory left from provisioning, and confirm the LMS server account is
   key-based SSH only (it was issued a password at handover; we had asked
   for key-based). BY: 3 working days.

From our side: DB password rotation and an app-scoped DB user (no SUPER) for
config.php are already in progress - no action needed from you there.

Thanks,
Nitin

---

## Message 2 - Suyash (IT), cc Matt (CTO)

**To:** Suyash (IT)
**Cc:** Matt (CTO, approver)
**Subject:** UAT Sentientia - Microsoft 365 items needed for SSO and outbound mail testing

Hi Suyash, Matt,

We are ready to test Entra SSO and OAuth2 SMTP on UAT
(https://academy2.airpay.ninja) as part of the Sentientia cutover. Per the IT
mandate (no fixed SMTP credentials or app passwords, MFA/SSO via Entra), we
need the following from the M365 tenant:

a) Entra ID app registration for SSO: redirect URI
`https://academy2.airpay.ninja/admin/oauth2callback.php`, delegated
Microsoft Graph permissions `openid`, `profile`, `email`, `User.Read`. Please
send the client ID and secret over a secure channel, plus the tenant ID.

b) A second app registration for OAuth2 SMTP (or the same app, if you
prefer) with delegated `Office 365 Exchange Online -> SMTP.Send` plus
`offline_access`, and admin consent granted. Note: SMTP.Send is a separate
Exchange Online API permission, not the similarly named Graph one.

c) A licensed service mailbox (for example academy-noreply@airpay.co.in)
with Authenticated SMTP (SMTP AUTH) enabled on that mailbox. Please also
confirm whether Security Defaults or a tenant-wide SMTP AUTH disablement is
in place, since either would block this even with a valid token, and whether
a carve-out is needed for this mailbox.

d) reCAPTCHA v2 site and secret keys for academy2.airpay.ninja (and later
airpay.academy) from the company Google account, for the public signup form.

e) Confirm the VPN allowlist previously requested for our access remains in
place.

During UAT, outbound mail goes only to one QA mailbox - no learner or staff
inboxes are touched. The same app registrations and mailbox get reused for
production after sign-off, so this is a one-time setup, not a recurring ask.

Could you give us a target date for (a)-(d)? Happy to jump on a call if that
is faster.

Thanks,
Nitin
