# QA Walkthrough — Bug Log (2026-05-29)

Persona-driven exhaustive UX/QA walk of local Airpay Academy (XAMPP, latest code synced).
Severity: **P0** blocker · **P1** broken/wrong · **P2** minor/UX · **P3** cosmetic.
Fix status: `OPEN` / `FIXED` / `WONTFIX` / `ENV-GAP` / `OPEN-QUESTION`.

| ID | Sev | Persona | Surface | Issue | Status |
|----|-----|---------|---------|-------|--------|
| G-1 | P1 | Guest | `frontpage.php:372` + `otploginform.mustache:134` | Navbar "Register" CTA + OTP "register with us" pointed to legacy `/local/users/signup.php` → **404**. Hero/footer/onboarding already use `/local/airpay_users/signup.php`. Repointed both. Verified → 200. | **FIXED** ✅ |
| G-2 | — | Guest | course "Details"/"Enroll" → `/local/search/coursedetails.php` (6 files) | 404 **locally only**. `local_search` is a BizLMS production plugin not mirrored to workspace/XAMPP (confirmed via `config_plugins` + missing `local_custom_category` + `table_exists()` guards). Correct for prod; rewriting would break prod. | ENV-GAP (no change) |
| SA-02 | P2 | Site Admin | `theme/airpayux/settings.php:27` | Theme settings page registered under leftover parent name `themesettingepsilon`; Moodle's selector auto-links `themesettingairpayux` → `sectionerror`. Renamed to convention. Verified → 200. | **FIXED** ✅ |
| SA-03 | — | Site Admin | `/local/sentientia_recommendations/index.php` | 404 — plugin is backend+CLI scaffold with no `index.php`. **Nothing links to it** (grep clean), so no user impact. | NOT-A-BUG |
| SA-04 | P2 | Site Admin | `core_renderer::custom_secured_redirection()` (`core_renderer.php:1243` + `:1250`) | **Unconditional** redirect of `/course/management.php` AND `/course/index.php` → catalog (no capability guard; query string stripped before match, so `?categoryid=` is also caught). Native course/category management hub, bulk ops, restore-to-category unreachable for admins; only deep-link `/course/editcategory.php` & `/course/edit.php` survive. The sibling `/my/dashboard.php` trainer redirect (`:1254`) IS capability-gated — so gating this one is consistent. **RECOMMEND** (not yet applied, awaiting go-ahead): gate both redirects so `is_siteadmin()` / `moodle/category:manage` / `moodle/course:create` holders reach native management while learners/guests still get the catalog. | INVESTIGATED → recommend gate |
| SA-05 | P3 | Site Admin | theme settings `configtitle` lang string | Theme settings page title still reads "Epsilon" (un-rebranded fork string). Cosmetic branding leak (matters for white-label product). | OPEN (cosmetic) |
| OA-GRAN | P1 | L&D Admin (latent) → **CONFIRMED P1** on Compliance Officer | `role_detector::detect()` vs page caps | role_detector grants the **L&D Admin shell** (sidebar: Manage Users/Courses/Reports/Classrooms) to anyone with `administrator`@**category**, but those pages `require_capability('local/airpay_*:view', context_system::instance())` (**system** context). **CONFIRMED on qa_compliance**: 5/8 sidebar links (Manage Users, Manage Courses, Online Exams, Classrooms, Reports) are dead — all 404 `nopermission`. The links render but cannot be accessed. **FIXED**: `sidebar_navigation.php` isldadmin block now gates each admin link by the same `has_capability('local/airpay_*:view', system)` its target page enforces. Verified — qa_compliance sidebar now shows 6 links, **0 dead** (was 5/8 dead); full admins keep all links (cap bypass / they hold the caps). See BUG-C-001 in `compliance.md`. | **FIXED** ✅ |
| C-002 | P1 | Compliance | `airpay_compliance_report/export.php:14` | Export Excel gated on `is_siteadmin() OR local/courses:manage` only — but `local/courses:manage` is **never registered** locally (BizLMS prod-only cap), and index.php (which compliance officers CAN reach) also accepts `administrator`@category + `moodle/site:viewreports`. So anyone who could view the report got a nopermission on Export. **FIXED (v2, 2026-05-29) — superseded the inline mirror-fix with a dedicated capability** `local/airpay_compliance_report:export` (`db/access.php` + `classes/permission.php` + install/upgrade grant to course-managers & role 9). export.php and the dashboard Export button now both gate on `permission::can_export()`, which resolves the cap even when granted via a **category-context** role assignment (the Compliance Officer / OrgAdmin shell) — the case a system-context `has_capability()` misses. **Scope tightened by owner decision:** admins + Compliance Officer export; **line managers VIEW but cannot bulk-export PII** (the inline v1 let managers export). Phantom `local/courses:manage` reference dropped. Verified: qa_compliance xlsx export → **200 + attachment** (no nopermission); `can_export()` correct for all 7 qa personas (managers/employees/trainer/public = no). See `docs/visual-evidence/2026-05-29/`. | **FIXED** ✅ |
| C-003 | — | Compliance | "ZEEA users leak into BU=1 filter" | **FALSE POSITIVE**. The "ATZ"/`airpay.tz` users are Airpay **Tanzania** (sub-org `/1/116/...`, root=1) — legitimately in the Airpay tenant tree, NOT ZEEA `/177`. The matrix tenant filter (`compliance_engine.php:394-398`) is provably correct (`= '/1' OR LIKE '/1/%'`, explicit anti-`/177`-leak comment). Verified via DB: all `airpay.tz` users root=1. | NOT-A-BUG |
| C-004 | P3 | Compliance | compliance report KPI bar | KPI summary cards stay at global aggregate when a BU filter is applied (table updates, cards don't). Minor; may be by-design (KPIs = global overview). Logged. | OPEN (minor) |
| C-005 | P2 | Compliance | `airpay_compliance_report/export.php` CSV branch | `export.php?format=csv` renders a Moodle themed **error page (HTTP 404)** instead of a CSV. Root cause: the CSV branch reads the matrix with the wrong shape — `$matrix['courses']` as arrays (`$course['shortname']`) when the engine returns **objects** (`->coursename`), and row keys `empid`/`firstname`/`lastname`/`statuses` that don't exist (real keys: `employee_id`/`fullname`/`courses`, per the working xlsx branch). **Pre-existing** (predates C-002), unrelated to the permission gate, and **not reachable from the UI** (the Export Excel button → xlsx, which works). Surfaced during C-002 verification now that non-siteadmins can reach export.php. **FIXED (2026-05-29):** the CSV branch now consumes the same shape as the xlsx branch (objects `->coursename`; row keys `employee_id`/`email`/`fullname`/`designation`/`courses` with `status_label`) and mirrors the xlsx Summary block. Verified: qa_compliance `?format=csv` → **200 + text/csv** (33 KB; header + per-course status columns + summary). | **FIXED** ✅ |
| OA-01..07 | — | L&D Admin | admin pages `nopermissions` + missing "New Path" | **NOT BUGS** — provisioning artifact (qa_orgadmin had `administrator`@category; pages check caps@system). Resolved by `administrator`@system → all 200 + "New Path" appears. Verified empirically. | RESOLVED (test-setup) |
| OA-08 / T-02 | P1 | Trainer | `/local/sentientia_live/index.php` is a Phase-E.0 placeholder; real trainer UI at `trainer/index.php` is unlinked | `index.php` is an intentional E.0 placeholder; the shipped Phase-E.1 trainer dashboard lives at `local/sentientia_live/trainer/index.php`. **FIXED (2026-05-29):** `theme_airpayux\sidebar_navigation` now adds a "Live Sessions" → `trainer/index.php` link in the **Manager AND Learner** shells, gated by `can_create_live_session()` (= `has_capability('local/sentientia_live:create')` + the `live.enabled` flag) so it only shows to users who can actually enter. Verified: rendered qa_trainer (Manager shell) sidebar now contains the link (Dashboard · My Team · Compliance · **Live Sessions** · …). The static E.0 `index.php` placeholder is left as-is by design. | **FIXED** ✅ |
| T-01 | P1 | Trainer | `sentientia_live/db/access.php` caps | `local/sentientia_live:create` + `:run` grant only to `editingteacher`+`manager` archetypes. The BizLMS `trainer` role is archetype `teacher` → trainers **cannot create/run** the live sessions the feature exists for. **FIXED (2026-05-29):** added `'teacher'=>CAP_ALLOW` to both caps in `db/access.php` (correct for fresh installs / future Sentientia customers) AND a `db/upgrade.php` step (savepoint `2026052900`) that back-fills the new default onto existing `archetype=teacher` roles via `assign_capability(overwrite=false)` — Moodle only auto-applies archetype defaults on a cap's *first* install, never on a later upgrade that merely adds an archetype (confirmed in `lib/accesslib.php::update_capabilities`). Plugin `2026052504`→`2026052900`; upgrade ran clean + purged caches. Verified: `has_capability('local/sentientia_live:create', system, qa_trainer)` → **YES** (was NO); `:run` → **YES**. | **FIXED** ✅ |
| T-03 | P2 | Trainer/Manager | `airpay_manager/index.php:34` | Manager-shell user with `viewreports` but 0 direct reports hit a raw `nopermission` **exception (500)** instead of the template's graceful empty state (admins already fell through). **FIXED**: dropped the throw → everyone gets the `has_team=false` empty state. Verified: qa_trainer → 200, "My Team" empty dashboard, no exception. | **FIXED** ✅ |
| T-04 | P2 | Trainer | `/blocks/airpay_trainer/dashboard.php` → "Class block_airpay_trainer not found" | Block IS installed (`mdl_block` id=57, v2026041600); the fatal was a code bug — `dashboard.php` did `new block_airpay_trainer()` without loading the class. Legacy `block_<name>` classes are NOT handled by core_component's autoloader (confirmed: `class_exists('block_airpay_trainer', autoload=true)` → FALSE); they load only via the block framework. **FIXED (2026-05-29):** `dashboard.php` now `require_once`s `blocks/moodleblock.class.php` (block_base) + `block_airpay_trainer.php` before instantiating. Verified: class loads, `get_content()` runs (qa_trainer → graceful "No training sessions assigned." empty state), no fatal. No version bump (pure page-logic fix, no DB/cap change). | **FIXED** ✅ |
| T-05 | P3 | Trainer | `/my/dashboard.php` trainer redirect never fires | Root cause confirmed: the redirect (`core_renderer.php:1254-1265`) gates on `block/airpay_trainer:viewtrainerslist`, archetypes `editingteacher`+`manager` — NOT `teacher`, so the BizLMS `trainer` role lacks it (same shape as T-01); the BizLMS fallback cap `block/trainerdashboard:viewtrainerslist` isn't registered locally. **DECISION PENDING (product call):** with T-01/T-02 done, trainers now get the rich `/my/` Manager/Learner shell + a "Live Sessions" link — *better* than the minimal `block_airpay_trainer` classroom-sessions page. Recommend **leave trainers on /my/ (by-design)** rather than grant the cap (which would force-redirect them to the minimal page = UX regression). T-04 already ensures that page no longer fatals if reached (the redirect still fires for editingteacher/manager, or via direct URL). | INVESTIGATED → owner decision |
| E-01 | P1 | Employee | `airpay_catalog/course.php` free-course CTA → `/enrol/index.php` | Airpay employee cannot self-enrol in a free course: the "Enroll Now — Free" CTA → `/enrol/index.php?id=71`, which finds **no self-enrol method** on the course and silently redirects to dashboard; `mycourses.php` stays empty. **Localised by the Public walk** — cart-enrol works for /77 Public, so this is Airpay-tenant/free-course-specific, NOT a cart-layer bug. Likely intended flow on prod = **auto-enrol** (cohort/HRMS), so the self-service "Free" CTA is misleading locally. **RECOMMEND** (product-flow call, not a safe inline fix): either enable a self-enrol method on free Airpay courses, or replace the self-service CTA with the correct enrol path / "auto-enrolled" state. Verify on staging where cohort sync runs. | OPEN → recommend |
| E-02 | P2 | Employee | `/local/airpay_skills/index.php?userid=3421` | Learner gets 404/`nopermission` viewing their own skills page; no sidebar link surfaces it either. Needs the intended learner-skills UX confirmed (the self-rate workflow exists) before fixing. | OPEN → verify |
| E-03 | P3 | Employee | catalog LIST "Enroll" button | Uses `?action=addtocart` even for free courses (UX inconsistent with the course-DETAIL page which shows "Enroll Now — Free"). Cosmetic/flow nit; tied to E-01. | OPEN (minor) |
| P-01 | P2 | Public | sidebar "My Cart" link | Sidebar "My Cart" → `/local/airpay_cart/index.php` (renders empty), but the working cart is `/local/airpay_catalog/cart.php`. User adding items via catalog then clicking the sidebar cart sees an empty cart. (Cart-URL area was recently in flux — PR #3 legacy add-to-cart fix — so flagged for the owner, not inline-fixed.) | OPEN → recommend |
| P-02 | P3 | Public | navbar cart badge | Badge doesn't increment after add-to-cart on the `catalog/course.php` confirmation layout (AMD `cart_badge` not wired on that layout). | OPEN (minor) |
| M-01 | P3 | Manager | `airpay_manager` team dashboard | Populated team dashboard renders + drill-down works, but the "Team Members" KPI tile showed 0 while the member (qa_employee) is listed — likely tied to the report having 0 enrolments (E-01) or a count-vs-list edge. Verify after E-01. | OPEN (minor) |

> SA-01 (`/admin/course/index.php` 404) was a **bad probe URL** on my part — that path isn't a Moodle 5.x route. Dismissed, not a finding.

## Personas
- ✅ Guest (unauthenticated) — done, report `guest.md`
- ✅ Site Admin (qa_siteadmin) — done, report `siteadmin.md`
- ✅ **CALIBRATION CHECKPOINT** — passed; user approved deeper-interaction walks for remaining 6 + "investigate+recommend" on SA-04
- ✅ Org/L&D Admin (qa_orgadmin) — done (deep walk + orchestrator cap-context verification), report `orgadmin.md`. **0 product bugs** (7 artifacts resolved) + 1 P3 latent.
- ✅ Compliance Officer (qa_compliance) — done, report `compliance.md`. **4 bugs** (BUG-C-001 P1 dead sidebar, BUG-C-002 P1 export blocked, BUG-C-003 P2 ZEEA tenant leak, BUG-C-004 P3 KPI bar not re-aggregated on BU filter).
- ✅ Trainer (qa_trainer) — done, report `trainer.md`. **T-01 + T-02 + OA-08 FIXED** (2026-05-29): Sentientia Live is now reachable by the BizLMS trainer role — `teacher` archetype granted `:create`/`:run` (+ upgrade back-fill onto existing roles), and a capability-gated "Live Sessions" sidebar link added (Manager + Learner shells). T-03 P2 **fixed** (manager 500→empty state). Remaining cluster: T-04/T-05 (block_airpay_trainer class + /my/dashboard.php redirect) **logged → recommend**.
- ✅ Employee / Learner (qa_employee) — done, report `employee.md`. Learner shell correct (no admin-link leak, cart correctly absent for Airpay), profile/certificates render. **E-01 P1** (Airpay self-enrol broken) + E-02/E-03 logged → recommend/verify.
- ✅ Public Learner (qa_public, /77) — done, report `public.md`. **Tenant isolation HOLDS** (sees only Public content, can't reach Airpay /1 courses); cart present + add→view works. P-01 P2 (sidebar cart URL) + P-02 P3 logged.
- ✅ Manager (qa_manager) — done. Populated team dashboard + drill-down + team compliance all 200 (T-03 fix confirmed on the populated path); correct Manager sidebar. M-01 P3 (team-size KPI nit) logged.
- **✅ ALL 8 PERSONAS WALKED** (Guest, Site Admin, Org Admin, Compliance, Trainer, Employee, Public, Manager).

## Provisioning note (important for reading walk reports)
qa_* personas are provisioned to satisfy `role_detector` tiers. **L&D-admin personas
(qa_orgadmin, qa_compliance) were re-granted `administrator`@SYSTEM** after the orgadmin
walk revealed that `administrator`@category alone triggers the admin *shell* but not the
system-context page caps. Employee-based personas (trainer/manager/employee/public) have
`employee`@system from the start. Any `nopermissions` in a walk report must be checked
against this before being called a product bug.

## Env note (blocks some local QA — expected, not a regression)
BizLMS plugin suite (`local_search`, `local_users`, `local_courses`, `local_request`, `local_programs`,
`local_classroom`, + `local_custom_category` table) is **not deployed to local XAMPP** — only the fork's
`local_airpay_*` / `local_sentientia_*` plugins are. Flows that land on a BizLMS page (course detail,
legacy enrol) can't be exercised locally; verify on staging/production.

## Notes
- Latest code synced to XAMPP (theme/local/blocks), upgrade + purge done, login 200.
- qa_siteadmin provisioned (siteadmin; `admin/search.php` → 200 confirms full access).
- Other 6 personas provisioned post-checkpoint.
- Subagent delegation (Sonnet, shared authenticated browser) validated end-to-end.
