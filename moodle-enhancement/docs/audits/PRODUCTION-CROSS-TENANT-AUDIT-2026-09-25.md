# Production cross-tenant audit - airpay.academy (BizLMS 4.1.2) - 2026-09-25

**Status: READ-ONLY audit. Nothing was changed on production or in the production codebase.**
Codebase: `D:/Claude Local/Moodle Backup/01-production-codebase/html` (snapshot of the eAbyas BizLMS
fork that runs https://www.airpay.academy), cross-checked against the later patches in
`02-enhancement-code` and the 2026-04-17 production DB import (role/capability rows only).

Method: the same defect classes ADR-031 fixed in Sentientia (unscoped capabilities default-granted to
an archetype; plugin capabilities that skip the tenant filter; fail-open when a tenant does not resolve;
writes that never check the target's tenant; privilege escalation). Sweeper agents per partition, then
one adversarial skeptic per finding that re-read the code and tried to refute it.

**Result:** 98 findings reported; 90 CONFIRMED, 0 UNCERTAIN, 8 REFUTED. Of the confirmed/uncertain: 51 P0, 33 P1, 6 P2 (severity is a first-pass heuristic from the defect class and who can reach it - review before triage).

## What this means

- Several web services and pages in production check only `require_login()` before deleting or
  editing organisation, course, classroom, program, group and user records by id, so **any logged-in
  account** - not just a tenant admin - can reach them. These are the P0 rows below.
- Some list/download paths concatenate request parameters into SQL (`PARAM_RAW`), which is both a
  tenant leak and an injection class.
- The BizLMS scoping helper fails open for users with no `local_userdata` row (1,560 rows vs ~2,871
  users in the April import), so many list pages return every tenant for those users.
- The successor Sentientia product (UAT, Moodle 5.2) replaces these plugins; ADR-031 closes the same
  classes there. Production stays exposed until the cutover or until interim patches ship.

## Recommended interim actions (need Nitin's go - production deploys are gated)

1. **Disable the unauthenticated-capability write services** in production (Site admin > Server >
   Web services > External services: remove the affected functions from the AJAX-enabled service, or
   add a `require_capability` + target-tenant check in each `external.php` method). Highest value, smallest
   change: the P0 delete/edit endpoints.
2. **Parameterise the SQL** in the `PARAM_RAW` concatenations (users download / filters, courses ajax,
   program users) or remove the parameters.
3. **Revoke `local/costcenter:manage_multiorganizations` from every role except the site admin** (it is
   held by role 1 `manager` at system context in the April import; role 9 does not hold it) and audit
   who holds role 1 at system context.
4. **Backfill `local_userdata`** for users without a row, or make the scoping helper fail closed.
5. Treat the Sentientia cutover (ADR-028 track) as the durable fix; do not port these plugins forward.

## Confirmed / uncertain findings

| # | Sev | Verdict | Plugin | Reachable by | Where |
|---|-----|---------|--------|--------------|-------|
| 1 | P0 | CONFIRMED | block_learnerscript | Any authenticated user | blocks/learnerscript/ajax.php:1136-1156 ('departmentusers': any orgid), :773-802 ('enrolledusers': any courseid), :751-772 ('usercourses': any userid, interpolated into SQL), ... |
| 2 | P0 | CONFIRMED | block_learnerscript / block_reportdashboard | Any authenticated user | blocks/learnerscript/externallib.php:1594-1640 (resetlsconfig: deletes all block_learnerscript and block_ls_schedule rows and dashboard block instances), :999-1052 ... |
| 3 | P0 | CONFIRMED | block_reportdashboard / block_learnerscript / block_trainerdashboard | Any authenticated user | blocks/reportdashboard/ajax.php:69-75 ('userlist' = get_users(true,$search), no tenant filter); blocks/reportdashboard/externallib.php:47-51 (userlist: $term concatenated into ... |
| 4 | P0 | CONFIRMED | evaluation | Any authenticated user. | local/evaluation/classes/external.php:73-143 (evaluation_update_instance :119, add :129) |
| 5 | P0 | CONFIRMED | evaluation | Any authenticated user. | local/evaluation/classes/external.php:397-450 addnew_question (insert :446, update :448); displayquestion :280-316 (pagebreak write, no capability) |
| 6 | P0 | CONFIRMED | evaluation | Any authenticated user. | local/evaluation/classes/external.php:465-478 evaluation_update_status |
| 7 | P0 | CONFIRMED | evaluation | Any authenticated user, through core_get_fragment. | local/evaluation/lib.php:2953-3075 local_evaluation_output_fragment_enrolledusers |
| 8 | P0 | CONFIRMED | evaluation | Any authenticated user. | local/evaluation/lib.php:2638-2645 get_listof_evalautions (filterdata evaluation and eval_type interpolated into IN (...)) |
| 9 | P0 | CONFIRMED | local (top-level includes.php) | Any caller of these enrolment pages (org admins). | local/includes.php:235-264 access_courses_permission (the tenant check is commented out and it always returns true for any non-empty courseid; used by courses/courseenrol.php:86, ... |
| 10 | P0 | CONFIRMED | local (top-level), local_program, local_learningplan | Unauthenticated. | local/course_completions.php:36-40 (runs the core completion daily and regular tasks), local/program/test.php (program completion task), ... |
| 11 | P0 | CONFIRMED | local_assignroles | Any manager-archetype holder (role 1 or 9 default-grants manageassignroles per mdl_role_capabilities row 1488/1805) reachable via ... | submit_assignrole_form (classes/external.php:29-61) and submit_assigncostcenterrole_form (:398-430): validate_context uses accesslib::get_module_context() (caller's own context, ... |
| 12 | P0 | CONFIRMED | local_classroom | Any logged-in user. | local/classroom/externallib.php:1087 delete_classroom_instance, :1321 delete_session_instance, :1430 delete_classroomevaluation_instance, :1722 delete_classroomcourse_instance. No ... |
| 13 | P0 | CONFIRMED | local_costcenter | Any authenticated user who can call the AJAX WS. | local/costcenter/classes/external.php costcenter_delete_costcenter (:191-228): no validate_parameters context, no require_capability, no is_siteadmin — takes id and deletes ... |
| 14 | P0 | CONFIRMED | local_costcenter | Any authenticated user reachable via the AJAX WS. | local/costcenter/classes/external.php submit_costcenterform_form (:53-101) and department_create (:926-971): validate_context uses caller's own get_module_context(), no ... |
| 15 | P0 | CONFIRMED | local_courses | Any logged-in user. | local/courses/classes/external.php:655 delete_course (service local_courses_deletecourse, ajax). No capability check and no validate_context. |
| 16 | P0 | CONFIRMED | local_courses | Any logged-in user. validate_context on a category context only requires login. | local/courses/classes/external.php:1176 course_update_status, :66 submit_create_course_form (update_course at :215 and :294, id taken from form data), :369 ... |
| 17 | P0 | CONFIRMED | local_courses | Any logged-in user, and also reachable by CSRF. | local/courses/index.php:53-64 (repeated at :160-170) |
| 18 | P0 | CONFIRMED | local_courses | Any logged-in user, and also reachable by CSRF. | local/courses/enrolledusers.php:32-60 (only require_login, no sesskey) |
| 19 | P0 | CONFIRMED | local_courses | local/courses:enrol holders (org admins). | local/courses/classes/form/mass_enroll_form.php:65 (roleassign is a hidden PARAM_INT) and :71-73 (firstcolumn is a hidden PARAM_RAW), used in local/courses/lib.php:67 and :186 ... |
| 20 | P0 | CONFIRMED | local_courses | Any logged-in user. | local/courses/ajax.php:34-47 (action=enrolledusers, only require_login) -> local/courses/renderer.php:290-328 get_course_enrolledusers. search[value] is concatenated into SQL at ... |
| 21 | P0 | CONFIRMED | local_courses (entry) -> local_users filters | Any org admin whose role has the manager archetype at their own org category passes has_capability at their own context and gets all ... | local/courses/classes/external.php:745 global_filters_form_option_selector (service local_courses_form_option_selector, db/services.php:78, ajax, no capability) dispatches to ... |
| 22 | P0 | CONFIRMED | local_custom_category | Any authenticated user (ajax.php has no cap gate at all). | local/custom_category/ajax.php:36-42 case 'deletecustomcategory' deletes {local_custom_category} by required_param skillid with only require_login (sets context but never checks ... |
| 23 | P0 | CONFIRMED | local_forum | Any authenticated user, student included | local/forum/classes/external.php:460-487 (delete_forum, delete_course($id) at :467); exposed as ajax WS local_forum_deleteforum in db/services.php:14-21 |
| 24 | P0 | CONFIRMED | local_forum | Any authenticated user. validate_context on the course's category context only needs a visible category. | local/forum/classes/external.php:549-573 (course_update_status: flips course.visible via update_record at :562); WS local_forum_course_update_status, services.php:38-45 (wrongly ... |
| 25 | P0 | CONFIRMED | local_forum | Any authenticated user | local/forum/classes/external.php:795-839 (forum_subscribe: manual enrol_user($instance, $USER->id, 5) at :820); WS local_forum_subscribe (services.php:88-95, typed 'read') |
| 26 | P0 | CONFIRMED | local_forum | Any authenticated user | local/forum/classes/external.php:68-284 (submit_create_forum_form): create_course at :162, update_course at :201, and update_course($data) with raw parse_str() input at :217 and ... |
| 27 | P0 | CONFIRMED | local_forum | Any authenticated user who knows any manual enrolid | local/forum/forumenrol.php:57-66 (view=ajax runs before the require_capability calls at :75-77). It passes raw $_GET['options'] to course_enrolled_users() ... |
| 28 | P0 | CONFIRMED | local_groups | Any authenticated user via the AJAX WS. | local/groups/classes/external.php groups_delete_groups (:170-181) deletes {local_groups}+{local_groups_permissions} by client id with no cap check; groups_status_confirm ... |
| 29 | P0 | CONFIRMED | local_learningplan | Any logged-in user, and also reachable by CSRF. | local/learningplan/classes/external.php:108 delete_learningplan, :149 toggle_learningplan, :221 lpcourse_unassign_course, :243 lpcourse_unassign_user (no capability). ... |
| 30 | P0 | CONFIRMED | local_location | Any authenticated user via AJAX WS. | local/location/classes/external.php submit_instituteform_form (:28-70) and submit_roomform_form (:91-131): validate_context on caller's get_module_context(), no ... |
| 31 | P0 | CONFIRMED | local_myteam | Any user who is a supervisor of at least one person (record_exists open_supervisorid). The get_team_* read helpers ARE scoped by ... | local/myteam/classes/external.php courseallocationdependencies (:278-345 case 'courseallocate' -> courseallocation_lib.php:153 courseallocation() enrols $user), ... |
| 32 | P0 | CONFIRMED | local_program | Any logged-in user. | local/program/externallib.php:128 delete_program_instance, :463 delete_programcourse_instance, :695 delete_level_instance, :776 inactive_program_instance, :820 ... |
| 33 | P0 | CONFIRMED | local_ratings | Unauthenticated visitors and any user | local/ratings/delete.php:6-11 (raw $_REQUEST, delete_records('local_comment', id)); comment.php:6-31, update.php:9-61 and index.php:28-73 also write with no require_login or ... |
| 34 | P0 | CONFIRMED | local_ratings | Any authenticated user | local/ratings/classes/external.php:93-122 (save_comment: the client supplies 'userid' and the comment is inserted or overwritten as that user); :461-494 (get_reviews: ... |
| 35 | P0 | CONFIRMED | local_request | Approvers in the Public and ZEEA tenants. Any logged-in user for the injectable path. | local/request/classes/output/requestview.php:117-118. The org costcenter id (1/77/177) is passed as the $userid argument of local_users ... |
| 36 | P0 | CONFIRMED | local_tags | Any logged-in user. | local/tags/manage.php:30,98-160 (require_login + sesskey only) -> classes/tag.php:1541 delete_tags, classes/collection.php:267 delete |
| 37 | P0 | CONFIRMED | local_users | Any authenticated user who can reach the AJAX web service (all fields incl. id and open_costcenterid arrive in client jsonformdata; the org ... | local/users/classes/external.php submit_create_user_form (:53-112) calls functions/users.php update_existinguser (:90) / insert_newuser (:31); delete_user (:139-166) and ... |
| 38 | P0 | CONFIRMED | local_users | Any authenticated user (services ajax=>true). | local/users/classes/external.php profiledata (:686-731) and profilemoduledata (:801-846) take decodedata->userid and call \local_<module>\local\user::user_profile_content($userid) ... |
| 39 | P0 | CONFIRMED | local_users | Any holder of local/users:manage. If held at system context (manager archetype, role 9), the export spans all tenants; the raw extrasql ... | local/users/download.php:42 checks has_capability('local/users:manage', $categorycontext) then passes optional_param('extrasql', '', PARAM_RAW) into functions/users.php ... |
| 40 | P0 | CONFIRMED | notifications | Anyone, including an unauthenticated request. Any logged-in user whose local_userdata row is missing or empty also gets every tenant. | local/notifications/email_status_filters.php:25-120 (filter at :54-56, query at :101) |
| 41 | P0 | CONFIRMED | notifications | Any authenticated user. | local/notifications/externallib.php:65-160 (update :107/:112, insert :130); form validation at classes/forms/notification_form.php:277 interpolates costcenterid/notificationid |
| 42 | P0 | CONFIRMED | onlineexams | Any authenticated user with a session (learners of Airpay, Public or ZEEA), through the AJAX web-service endpoint. | local/onlineexams/classes/external.php:496-530 (delete_course at :503); service local_onlineexams_deleteonlineexams, ajax=true, db/services.php:39-46 |
| 43 | P0 | CONFIRMED | onlineexams | Any authenticated user. | local/onlineexams/classes/external.php:585-611 (update_record at :598); service declared type 'read' at db/services.php:63-70 |
| 44 | P0 | CONFIRMED | onlineexams | Any authenticated user. | local/onlineexams/classes/external.php:68-310 (create_course :165, update_course :223 and :248, update_moduleinfo :240/:254) |
| 45 | P0 | CONFIRMED | onlineexams | Any authenticated user. | local/onlineexams/onlineexamsenrol.php:57-66, calling local/courses/lib.php:1071-1160 course_enrolled_users(), where options email, uname and idnumber are interpolated into IN ... |
| 46 | P0 | CONFIRMED | onlineexams | Any authenticated user. | local/onlineexams/custom_ajax.php:38-60 (page 1) and :108-160 (page 2) |
| 47 | P0 | CONFIRMED | onlineexams | Any authenticated user, through core_get_fragment or the details page. | local/onlineexams/lib.php:46-60 local_onlineexams_output_fragment_custom_onlineexams_form; also onlinexamdetails.php:18-35 |
| 48 | P0 | CONFIRMED | skillrepository | Any authenticated user. | local/skillrepository/ajax.php:37-125: deletecategory :86-89, deleteskill :116-120 (also resets course.open_skill site-wide), deletelevel :109-114 (resets course.open_level ... |
| 49 | P0 | CONFIRMED | skillrepository | Any authenticated user. | local/skillrepository/classes/external.php:259-263 delete_skill (ajax=true, db/services.php:53-60) |
| 50 | P0 | CONFIRMED | skillrepository | Any authenticated user. | local/skillrepository/classes/external.php:160-181 repository_selector (organisation at :176, query at :178) |
| 51 | P0 | CONFIRMED | skillrepository | Any authenticated user. | local/skillrepository/classes/external.php:591-625 submit_skills_interested_form (update :618) |
| 52 | P1 | CONFIRMED | block_learnerscript | Any user who can run a custom SQL report. That is any authenticated user, given the ignored permission check above. | blocks/learnerscript/reports/sql/report.class.php:166, :185, :204 (FILTER_ORGANIZATION/DEPARTMENTS are replaced only when $USER->open_costcenterid > 0) and :292 (preg_replace ... |
| 53 | P1 | CONFIRMED | block_learnerscript / block_reportdashboard | Any authenticated user, student included | blocks/learnerscript/classes/local/reportbase.php:532 (create_report calls $this->check_permissions() and discards the result). Entry points: ... |
| 54 | P1 | CONFIRMED | costcenter (shared helper inherited by all four plugins) | Any non-admin without a local_userdata row. Managers assigned at system context. | local/costcenter/classes/lib/accesslib.php:245-285 and 315-363 (no local_userdata row gives an empty filter; an empty or NULL costcenterpath gives LIKE '/%', which matches every ... |
| 55 | P1 | CONFIRMED | evaluation | Org admins and trainers holding local/evaluation:edititems without manage_multiorganizations. | local/evaluation/lib.php:289-296 dep_sql(). Reached from renderer.php:352-356 (tagged_evaluations, for edititems holders) and lib.php:862-868 (evaluation_get_template_list('all')) |
| 56 | P1 | CONFIRMED | evaluation | Org admins or trainers holding edititems or deletetemplate in their own tenant. | local/evaluation/index.php:61, 148-160 (soft-delete :151, no sesskey); use_templ.php:40-55; import.php:49-154 (delete_all_items :154); ajax.php:43-53 (reorder); export.php:44-48; ... |
| 57 | P1 | CONFIRMED | evaluation | Org admins holding enroll_users. | local/evaluation/users_assign.php:31-51, 124-175 (insert :151); the ajax branch :67-76 calls lib.php:2313-2437 evaluation_enrolled_users (raw IN(...) at :2332-2338) |
| 58 | P1 | CONFIRMED | evaluation | Report viewers with viewreports and no resolved path, or with a system-context role. | local/evaluation/classes/external.php:1448-1500 get_non_respondents, :1556-1580 get_responses_analysis; mitigating filters at lib.php:560 and classes/responses_table.php:346 |
| 59 | P1 | CONFIRMED | local_biz_cart | Any authenticated user | local/biz_cart/classes/external/search_users.php:61-73 (only validate_context(system)); it calls biz_cart::load_users() at classes/biz_cart.php:1651-1700, which searches all ... |
| 60 | P1 | CONFIRMED | local_biz_cart | Any authenticated user | local/biz_cart/classes/external.php:148-193 (transactions_view_for_admin), :244-292 (view_course_transaction_log), :342-387 (view_course_standard_log): only require_login. Scoping ... |
| 61 | P1 | CONFIRMED | local_biz_cart | Any authenticated user | local/biz_cart/classes/external/get_history_items.php:84-88 (capability logic inverted: non-cashiers may pass any userid, cashiers get only their own); ... |
| 62 | P1 | CONFIRMED | local_biz_cart | Any holder of a manager-archetype role at system context (e.g. a tenant admin given a system-level manager role) | local/biz_cart/cashier.php:36-45 (buy_for_user(any userid)); classes/biz_cart.php:348-366 (set_user), :851-900 (confirm_payment for other users), :1126-1200 (cancel_purchase with ... |
| 63 | P1 | CONFIRMED | local_biz_cart (with local_courses cart provider and paygw_airpay) | Any authenticated user | local/biz_cart/classes/biz_cart.php:84-203 (allow_add_item_to_cart / add_item_to_cart) calls local/courses/classes/biz_cart/service_provider.php:54-80 and :311-335, which load any ... |
| 64 | P1 | CONFIRMED | local_classroom | Any logged-in user without the manage capability, for example a learner. | local/classroom/externallib.php:530 classroomviewusers (no capability) -> local/classroom/classes/classroom.php:2161-2200 classroomusers. The tenant filter at :2188 is applied ... |
| 65 | P1 | CONFIRMED | local_classroom | Org admins of any tenant. | local/classroom/enrollusers.php:49,54-55 (capability at the caller's own context), :35 (any cid), :139-143 and :162-165 (raw add[]/remove[] ids) -> classes/classroom.php:1242 ... |
| 66 | P1 | CONFIRMED | local_costcenter accesslib (helper used by every plugin in this partition) | Any non-admin without a usable local_userdata path, including system-context managers. | local/costcenter/classes/lib/accesslib.php:245-285 get_user_roleswitch_costcenterpath_concatsql and :315-363 userpath_match_sql. local_userdata is only written by ... |
| 67 | P1 | CONFIRMED | local_costcenter accesslib (root cause behind path scoping in biz_cart, forum, learnerscript and trainerdashboard) | Any non-admin user without a resolvable local_userdata path, e.g. a newly created user or an imported account missing that row | local/costcenter/classes/lib/accesslib.php:238-280 (get_user_roleswitch_costcenterpath_concatsql: an empty costcenterpath falls back to userpath_match_sql, and an empty result ... |
| 68 | P1 | CONFIRMED | local_courses | An org admin with a manager-archetype role in any tenant. | local/courses/courses.php:53-54 (capability checked at the caller's own role-switch context), :73-75 delete_course($deleteid) |
| 69 | P1 | CONFIRMED | local_courses | Org admins with a manager archetype for writes. Any logged-in user for the ajax branch. | local/courses/courseenrol.php:55,77-79 (capabilities at the caller's own context), :86 uses local/includes.php access_courses_permission (always true, see separate hit), :43-47 vs ... |
| 70 | P1 | CONFIRMED | local_forum | Tenant/org admins holding a manager-archetype role at their category context | local/forum/forumenrol.php:41-45, 75-87, 175-231, 232-293. The tenant gate has_user_permission::access_courses_permission() (local/includes.php:235-264) always returns true, and ... |
| 71 | P1 | CONFIRMED | local_forum | Org admins, or any role holding local/forum:view at their role-switch context | local/forum/index.php:54-57 (gate) and :73-88 (delete_course on any ?delete=<id> with sesskey) |
| 72 | P1 | CONFIRMED | local_groups | Any holder of moodle/cohort:assign; role 9 (org admin) has moodle/cohort:assign at system context (mdl_role_capabilities row ~2192-equiv) ... | local/groups/assign.php:43 require_capability('moodle/cohort:assign', caller context) then acts on $id (any cohort) and add[]/remove[] (optional_param PARAM_RAW) at :141-190 via ... |
| 73 | P1 | CONFIRMED | local_location | Any holder of local/location:viewinstitute/manageinstitute (org admins). Cross-tenant because id is unvalidated against costcenter. | local/location/index.php:84-87 if($delete){ $institute->delete_institutes($id); } and room.php:60-62 delete_rooms($id). delete_institutes/delete_rooms ... |
| 74 | P1 | CONFIRMED | local_program | Any logged-in user. | local/program/ajax.php:88-99 (action=viewprogramusers, only require_login) -> classes/output/renderer.php:859-930 -> classes/program.php:984-1017 programusers (no tenant filter; ... |
| 75 | P1 | CONFIRMED | local_recompletion | Course editing teachers and managers. | local/recompletion/editcompletion.php:30-37 and the form write path. users[] are not checked for enrolment or tenant. resetcompletion.php:47-50 is similar for the 'user' param. |
| 76 | P1 | CONFIRMED | local_request | Any logged-in user. | local/request/ajax.php:21-39 and local/request/tableresponse.php:42-57 (only require_login) -> classes/api/requestapi.php:161 approve, :245 deny, :139 delete. requestapi::create ... |
| 77 | P1 | CONFIRMED | local_search | Any authenticated learner. | local/search/courseinfo.php:14-34 (?enrol=1&id=X self-enrols $USER to any course with a self enrol instance) and classes/external.php enrol_user_to_module (:152-175) dispatches to ... |
| 78 | P1 | CONFIRMED | notifications | Org admins (notifications:view is default-granted to manager, db/access.php:37-44). Ids are enumerable from the unauthenticated listing ... | local/notifications/email_status_details.php:30-40, classes/output/renderer.php:264-287, classes/local/email_status_func.php:89-95 |
| 79 | P1 | CONFIRMED | notifications | Org admins (manager archetype). | local/notifications/index.php:31-50 (delete_records :47) |
| 80 | P1 | CONFIRMED | notifications | Any authenticated user. | local/notifications/custom_ajax.php:33-120 (pattern built at :40); the same pattern is at classes/forms/notification_form.php:84 and :190 |
| 81 | P1 | CONFIRMED | onlineexams | Org admins holding the onlineexams enrol caps (default-granted to the manager archetype, db/access.php:30-93). | local/onlineexams/onlineexamsenrol.php:41-45, 83-86, 168-221, 224-284. roleid at :43 is passed to enrol_user at :198. Runtime assign_capability/unassign_capability at :178-186, ... |
| 82 | P1 | CONFIRMED | onlineexams | Org admins (local/courses:view is default-granted to the manager archetype, local/courses/db/access.php:54-61). | local/onlineexams/index.php:28, 54, 74-88 (delete_course at :76) |
| 83 | P1 | CONFIRMED | skillrepository | editingteacher-archetype (trainer) and manager-archetype roles. | local/skillrepository/index.php:55-83 (update by submitted id :77-78); skill_category.php:46-80 |
| 84 | P1 | CONFIRMED | skillrepository | Anyone viewing the levels table (level.php and ajax getlevelstable are require_login only). | local/skillrepository/classes/lib/accesslib.php:33-44 (reads local_evaluations.open_path by skill id); classes/local/querylib.php:36 (filter uses alias sk. on a query aliased lcl) |
| 85 | P2 | CONFIRMED | evaluation | Org admins or report viewers with viewreports in their own tenant, using a mobile web-service token. | local/evaluation/classes/external.php:1191-1240 get_analysis (mobile service); data from lib.php:1892-1950 evaluation_get_group_values, which has no tenant filter; ... |
| 86 | P2 | CONFIRMED | evaluation | Holders of viewanalysepage in the evaluation's tenant, or at system context for all tenants. | local/evaluation/show_entries.php:80-86 (superuser SQL built and never executed); classes/completion.php:80 (anonymity filter commented out) |
| 87 | P2 | CONFIRMED | local_costcenter | Any authenticated non-admin whose role-switch context is unset and who has no mdl_local_userdata row. In the prod dump mdl_local_userdata ... | local/costcenter/classes/lib/accesslib.php:245-285 get_user_roleswitch_costcenterpath_concatsql() and :315-363 userpath_match_sql(). If ... |
| 88 | P2 | CONFIRMED | local_program | Org admins. | local/program/enrollusers.php:50,55-56 (capability at the caller's own context), :35 bcid, :134/:155 program_add/remove_assignusers |
| 89 | P2 | CONFIRMED | notifications | Any authenticated user. | local/notifications/externallib.php:196-230 managenotificationsview; lib.php:762-790 notification_details |
| 90 | P2 | CONFIRMED | skillrepository | Any authenticated user. | local/skillrepository/classes/external.php:42-70 (skill update :63), :99-130 (category, classes/event/insertcategory.php:45-51), :212-235 (level, classes/local/querylib.php:14-25) |

### 1. [P0] block_learnerscript - 2/4 - lookups by org/course/user id from the request with no scope check

- **Capability:** none
- **Where:** blocks/learnerscript/ajax.php:1136-1156 ('departmentusers': any orgid), :773-802 ('enrolledusers': any courseid), :751-772 ('usercourses': any userid, interpolated into SQL), :1079-1135 ('departmentcourses'), :811-827 ('orgdepts'), :803-810 ('categorycourses', interpolated). Only require_login at :298.
- **What crosses tenants:** Full names of every active user in any tenant (orgid=77 or 177 from an Airpay login), student names of any course, the enrolled courses of any user, and course/department names of other tenants. JSON body values reach the queries unvalidated because they are only used as optional_param defaults, so usercourses/categorycourses are also injectable.
- **Who can reach it:** Any authenticated user
- **Fix:** 1. After `require_login()` at ajax.php:298, add a check for these six actions: require `block/learnerscript:viewreports` or `:managereports` (or is_siteadmin) in `$context`, and return an error when it fails. 2. Clean every value taken from `$requests[]`. Run `clean_param($requests['x'], PARAM_INT)` (and the matching PARAM_* type) before using it as an optional_param default, or replace the defaults with `clean_param` outright. The simplest fix is to wrap each default assignment at :43-243 in clean_param. 3. Rewrite the SQL at :761 and :805 with named placeholders: `u.id = :userid` and `open_categoryid = :catid`. Use `$DB->get_in_or_equal()` in place of the `implode()` IN lists at :779-781 and :1122-1123. 4. Scope checks for non-siteadmins: - enrolledusers / usercourses: check that the course's `open_path`, or the target user's `open_path`, lies inside the caller's costcenter path. Use ...
- **Already patched?** Not in 02-enhancement-code
- **Verifier:** I re-read the code and the finding holds. One point needs a correction: a clamp exists for the two department actions, and it fails open. File: blocks/learnerscript/ajax.php in the 01 snapshot. It is identical to 02-enhancement-code/new-blocks/learnerscript/ajax.php (diff shows no differences), and nothing under bug-fixes/ or modified-files/ touches learnerscript, so it has not been patched there. 1. Only a login check guards these actions. Line 298 is `require_login()`. The switch at :304 runs six actions without any has_capability call: usercourses :751, enrolledusers :773, categorycourses :803, orgdepts :811, departmentcourses :1079, departmentusers :1136. The block/learnerscript:* ...

### 2. [P0] block_learnerscript / block_reportdashboard - 5 - unauthorised edits to report definitions and dashboards shared by all tenants

- **Capability:** none (block/learnerscript:managereports never checked)
- **Where:** blocks/learnerscript/externallib.php:1594-1640 (resetlsconfig: deletes all block_learnerscript and block_ls_schedule rows and dashboard block instances), :999-1052 (updatereport_conditions: field and operator end up in SQL at components/conditions/ccoursefield/plugin.class.php:50), :1391-1428 (deletecomponenet: can remove a report's filter or permission elements); blocks/learnerscript/ajax.php:545-615 ('updatereport') and :1479-1494 ('tabsposition'); blocks/reportdashboard/externallib.php:159-172 (raw SQL UPDATE of block_instances at :166); blocks/reportdashboard/ajax.php:112-128 ('updatedashboard')
- **What crosses tenants:** Any user can wipe or rewrite the report catalogue and dashboards used by every tenant, remove report permission elements, and store condition SQL that later runs for every viewer of that report. That is a stored SQL injection able to widen tenant scope for everyone.
- **Who can reach it:** Any authenticated user
- **Fix:** Fix these in blocks/learnerscript and blocks/reportdashboard, in both trees (the files are identical in 02-enhancement-code/new-blocks): 1. **Capability checks.** In each write function in learnerscript/externallib.php (updatereport_conditions, deletecomponenet, designdata, plotforms, importreports, lsreportconfigimport), call self::validate_context(context_system::instance()) followed by require_capability('block/learnerscript:managereports', $ctx). - As a narrower alternative, allow block/learnerscript:manageownreports only when $report->ownerid == $USER->id. - resetlsconfig: allow site admins only. Use is_siteadmin() || throw required_capability_exception, or require_capability('moodle/site:config', context_system::instance()). 2. **learnerscript/ajax.php.** Call require_sesskey() after require_login(). Put the same managereports/ownership check on 'updatereport', ...
- **Already patched?** Not patched (02-enhancement-code copies differ only in UI files)
- **Verifier:** I checked every cited site in the snapshot. The files are byte-identical in 02-enhancement-code/new-blocks, so this is not patched there. 1. No capability gate on the cited writes. - blocks/learnerscript/db/access.php declares managereports, manageownreports, viewreports and designreport, all with empty archetypes. So class 1 does not apply, but this finding does not depend on class 1. - In blocks/learnerscript/externallib.php, has_capability calls stop at line 960 and resume at 1815. Nothing in between has a capability check. That span holds updatereport_conditions (999), deletecomponenet (1391), importreports (1481), lsreportconfigimport and resetlsconfig (1594). - resetlsconfig step 1 ...

### 3. [P0] block_reportdashboard / block_learnerscript / block_trainerdashboard - 3 - site-wide user search and SQL injection reachable by any user

- **Capability:** none
- **Where:** blocks/reportdashboard/ajax.php:69-75 ('userlist' = get_users(true,$search), no tenant filter); blocks/reportdashboard/externallib.php:47-51 (userlist: $term concatenated into LIKE) and :94-97; blocks/reportdashboard/ajax.php:77-80 ('reportlist' $search); blocks/learnerscript/classes/local/querylib.php:456 (filter_get_orgwiseusers, via WS block_learnerscript_filterusers, externallib.php:1716); blocks/trainerdashboard/classes/manager.php:51-56, :113-118, :175-180 (search_query from dataoptions JSON). The trainerdashboard WS in classes/external.php:58+ have no capability check and are exposed to MOODLE_OFFICIAL_MOBILE_SERVICE.
- **What crosses tenants:** Names of every user in all three tenants from a single search, and through the concatenated search terms, arbitrary reads of the whole database (grades, emails, completions of every tenant). The trainerdashboard WS also return trainer names, emails and employee IDs to learners.
- **Who can reach it:** Any authenticated user
- **Fix:** 1. Parameterise every search term. Use $DB->sql_like('col', ':s', false) with the value '%' . $DB->sql_like_escape($term) . '%' passed as a bound parameter. Apply this in: - reportdashboard ajax.php:79 - reportdashboard externallib.php:50 - learnerscript querylib.php:456 - trainerdashboard manager.php, all LIKE builders (:48-55, :87-94, :112-118, :175-181, :210-216, :235-241, :280-285) 2. In reportdashboard/ajax.php, stop taking JSON-body values as optional_param defaults. Decode the JSON, then run clean_param() on each field with its declared PARAM type. Replace the concatenated integer SQL (departmentlist, the course/org queries near :385) with bound :params. 3. Add capability checks: - ajax.php 'userlist' and 'reportlist': require_capability, e.g. block/learnerscript:managereports or a dedicated reportdashboard view capability. - block_reportdashboard_userlist and ...
- **Already patched?** Not in 02-enhancement-code (reportdashboard diff is UI-only in dashboard.php; trainerdashboard not changed)
- **Verifier:** I read the code. The core claims hold. Two sub-claims are wrong or overstated. CONFIRMED 1. blocks/reportdashboard/ajax.php:44 only calls require_login(). The 'userlist' case at :69-75 calls get_users(true, $usersearch) with no capability check and no tenant filter. Core get_users (lib/datalib.php:415-467) binds its parameters, so there is no injection here. But it matches first+last name, email or username across every non-deleted user, and an empty term returns every user. The response holds user id and full name for all three tenants. It also works as an oracle: a search by email tells you whether that email exists and whose name it belongs to. Any logged-in user, including a learner, ...

### 4. [P0] evaluation - 4 (create/update any evaluation) plus missing capability

- **Capability:** none (validate_context only)
- **Where:** local/evaluation/classes/external.php:73-143 (evaluation_update_instance :119, add :129)
- **What crosses tenants:** Updates any evaluation by the form's id. open_path is recomputed from submitted hierarchy fields (:114), so another tenant's evaluation can be re-homed or edited. Can also create evaluations in any org.
- **Who can reach it:** Any authenticated user.
- **Fix:** In local_evaluation_external::submit_create_evaluation_form (classes/external.php:73): (a) Update path (id > 0): load the record first with $old = $DB->get_record('local_evaluations', ['id' => (int)$data['id']], '*', MUST_EXIST). Take $ctx = \local_evaluation\lib\accesslib::get_module_context($old->id) and call require_capability('local/evaluation:edititems', $ctx), or addinstance if that is the intended edit capability. Then assert that $old->open_path lies inside one of the caller's $USER->useraccess['currentroleinfo']['contextinfo'][*]['costcenterpath'] entries. Skip that assertion only for site admins or holders of local/costcenter:manage_multiorganizations, and that capability must not be default-granted. Use a boundary-safe comparison: $p === $scope || strpos($p.'/', rtrim($scope,'/').'/') === 0. (b) Create path: require_capability('local/evaluation:addinstance', ...
- **Already patched?** No.
- **Verifier:** I read the code myself and the finding holds. Nothing in the chain checks a capability or the target's tenant. 1. The endpoint is open to any logged-in user. db/services.php:26-33 registers local_evaluation_submit_create_evaluation_form as type 'write' with 'ajax' => true. It declares no capabilities, so any logged-in user can call it through lib/ajax/service.php with their own sesskey. In local/evaluation/classes/external.php:73-143, the only gate is self::validate_context() at :83, on a context id the caller supplies. For the system context that amounts to require_login(). There is no has_capability or require_capability anywhere in the function. 2. The id being updated is whatever the ...

### 5. [P0] evaluation - 4 (write question items of any evaluation)

- **Capability:** none (validate_context of the caller's own context)
- **Where:** local/evaluation/classes/external.php:397-450 addnew_question (insert :446, update :448); displayquestion :280-316 (pagebreak write, no capability)
- **What crosses tenants:** Inserts or overwrites local_evaluation_item rows by caller-supplied id and evaluation. When no item form class matches, raw parsed request fields go straight to update_record.
- **Who can reach it:** Any authenticated user.
- **Fix:** In local/evaluation/classes/external.php: 1. addnew_question: - Decode and parse the form data first. - If an id is submitted, load it with $DB->get_record('local_evaluation_item', ['id' => $id], '*', MUST_EXIST). Take the evaluation id from that stored row and ignore the submitted 'evaluation' (or reject the call when the two differ). For a new item, require a submitted evaluation id and load that local_evaluations row with MUST_EXIST. - Resolve the context from the evaluation: $context = (new \local_evaluation\lib\accesslib())::get_module_context($evaluation->id). Then call self::validate_context($context) and require_capability('local/evaluation:edititems', $context). - Check tenant scope: unless the caller is a site admin, require that the evaluation's open_path sits under the caller's own open_path using a bounded path-prefix match (for example '/1' must not match '/177'), such as ...
- **Already patched?** No.
- **Verifier:** I re-read the code and could not refute the finding. 1. Nothing checks a capability. local_evaluation_external::addnew_question (local/evaluation/classes/external.php:397) calls validate_parameters and then get_module_context() with no evaluation id. That returns the system context for a site admin, or the caller's own role-switch context for anyone else (local/costcenter/classes/lib/accesslib.php:74-95). It then calls self::validate_context($context) at :405. validate_context only runs require_login on that context and sets PAGE. There is no require_capability or has_capability anywhere in the function. db/services.php (local_evaluation_addnew_question, type write, ajax true) declares no ...

### 6. [P0] evaluation - 4 (write)

- **Capability:** none
- **Where:** local/evaluation/classes/external.php:465-478 evaluation_update_status
- **What crosses tenants:** Toggles visibility of any evaluation in any tenant.
- **Who can reach it:** Any authenticated user.
- **Fix:** In local/evaluation/classes/external.php, evaluation_update_status(): - Load the record with MUST_EXIST first: $evaluation = $DB->get_record('local_evaluations', ['id' => $params['id']], '*', MUST_EXIST); and use $params['id'], not the raw $id. - Resolve the context of the target evaluation: $context = \local_evaluation\lib\accesslib::get_module_context($evaluation->id); then run self::validate_context($context). If the resolved context is empty or false, throw an error; do not fall back to the system context. - Require the same capabilities the UI uses: require_capability('local/evaluation:edititems', $context); require_capability('local/evaluation:addinstance', $context). - Add an explicit tenant check for callers who are not site admins. Unless is_siteadmin(), the evaluation's open_path must equal $USER->open_path or be a descendant of it, using a bounded prefix comparison (the path ...
- **Already patched?** No.
- **Verifier:** I read the code myself and could not refute the finding. The web service changes data, has no capability check and no tenant check, and any logged-in user can call it. 1. Registration. In local/evaluation/db/services.php:67-75, local_evaluation_evaluation_update_status is declared as type 'write' with 'ajax' => true. It has no 'capabilities' key and does not set 'loginrequired' => false. So lib/externallib.php:226-233 only requires that the caller is logged in and sends a valid sesskey. That applies to every authenticated session, including an ordinary learner with only the 'user' archetype. No plugin in local/ or blocks/ uses override_webservice_execution to add a check. 2. Context check. ...

### 7. [P0] evaluation - 3 (fail-open) plus missing capability

- **Capability:** none
- **Where:** local/evaluation/lib.php:2953-3075 local_evaluation_output_fragment_enrolledusers
- **What crosses tenants:** Full names, emails, employee ids, enrolment dates, submission dates and status of every assignee of any evaluation id. The only filter is the viewer's own u.open_path: learners see colleagues in their subtree, and every tenant is returned when the viewer's path does not resolve.
- **Who can reach it:** Any authenticated user, through core_get_fragment.
- **Fix:** In `local/evaluation/lib.php`, `local_evaluation_output_fragment_enrolledusers()`: (a) Validate the id and load the evaluation: `$id = clean_param($args['id'], PARAM_INT); $evaluation = $DB->get_record('local_evaluations', ['id' => $id, 'deleted' => 0], '*', MUST_EXIST);` (b) Require the reporting capability in the evaluation's own tenant context: `$context = (new \local_evaluation\lib\accesslib())::get_module_context(); require_capability('local/evaluation:viewreports', $context);`. Unless `is_siteadmin()`, also check that `$evaluation->open_path` equals or is a descendant of the viewer's tenant path, using a bounded prefix match (`path = :p OR path LIKE :p || '/%'`), and throw `required_capability_exception` otherwise. (c) Stop relying on `get_costcenter_path_field_concatsql` to fail safely. Filter the assignee query by the evaluation's tenant, so `u.open_path` must be under the ...
- **Already patched?** No.
- **Verifier:** I checked the claim against the code in the production snapshot and it holds. There is one small correction to the sweeper's description of how the scope is worked out. 1. There is no capability check. `local_evaluation_output_fragment_enrolledusers` (`D:/Claude Local/Moodle Backup/01-production-codebase/html/local/evaluation/lib.php:2953-3075`) never calls `require_capability`, `has_capability` or `require_login`. It never loads the evaluation named by `$args['id']` and never checks which tenant owns it. The sibling fragment `addquestions_or_enrol` at :2924 does call `require_capability('local/evaluation:edititems')`, so the omission is specific to this callback. 2. Any logged-in user can ...

### 8. [P0] evaluation - SQL injection, which crosses all tenants

- **Capability:** none (require_login in local_evaluation_evaluationview, external.php:176-216)
- **Where:** local/evaluation/lib.php:2638-2645 get_listof_evalautions (filterdata evaluation and eval_type interpolated into IN (...))
- **What crosses tenants:** Arbitrary database read across all tenants.
- **Who can reach it:** Any authenticated user.
- **Fix:** In local/evaluation/lib.php get_listof_evalautions(), lines 2638-2645, stop inserting user input into the SQL string. Clean each filter to an array of integers, then bind it: $evalids = array_filter(array_map('intval', is_array($filtervalues->evaluation) ? $filtervalues->evaluation : explode(',', (string)$filtervalues->evaluation))); if ($evalids) { [$insql, $inparams] = $DB->get_in_or_equal($evalids, SQL_PARAMS_NAMED, 'evalid'); $fromsql .= " AND a.id $insql"; $filterparams += $inparams; } Do the same for eval_type. Its values may not all be integers: whitelist them against the known type values if they are strings, otherwise clean with PARAM_INT. Bind them with the prefix 'evaltype'. Then set $filterparams = [] once, before the evaluation/eval_type blocks (it is currently initialised at line 2656, after them), and merge it into $params as now. For the ...
- **Already patched?** No.
- **Verifier:** I read the code and could not refute the finding. The injection is real and any logged-in user can reach it. 1. Entry point. local/evaluation/db/services.php:34-41 registers local_evaluation_evaluationview with type 'read' and ajax => true. It needs a login and a sesskey. It checks no capability. 2. No capability or scope check before the query. local_evaluation_external::evaluationview (classes/external.php:176-212) calls only require_login() at line 178. It declares filterdata as PARAM_RAW (line 161). At line 196 it runs json_decode($filterdata) and sets no field types. At line 202 it passes the decoded object straight to get_listof_evalautions(). 3. The injection itself. In ...

### 9. [P0] local (top-level includes.php) - 3-fail-open / 1-unscoped-cap

- **Capability:** none / local/costcenter:assign_multiple_departments_manage (manager archetype)
- **Where:** local/includes.php:235-264 access_courses_permission (the tenant check is commented out and it always returns true for any non-empty courseid; used by courses/courseenrol.php:86, courses/mass_enroll.php:54, forum/forumenrol.php:84, onlineexams/onlineexamsenrol.php:83). local/includes.php:314 and local/courses/includes.php:111 access_user_permission return true for any target user when the caller holds assign_multiple_departments_manage.
- **What crosses tenants:** The 'department level restriction' guard on enrolment pages is a no-op, so any tenant's course passes.
- **Who can reach it:** Any caller of these enrolment pages (org admins).
- **Fix:** In courseenrol.php, forumenrol.php and onlineexamsenrol.php: (1) Work out the course from $instance->courseid only. Ignore or reject the `id` param when it does not match. (2) Replace access_courses_permission() in local/includes.php with a real, bounded check. The course's open_path must equal the caller's tenant path or sit under it (path_descendant-style match on '/<cc>' or '/<cc>/%', never an unbounded LIKE '/1%'). Deny when the caller's path does not resolve (fail closed). Only is_siteadmin() bypasses it. (3) Check capabilities in the course's own context: local_courses accesslib::get_module_context($instance->courseid), not the caller's default context. (4) Before enrol_user() or unenrol_user(), intersect `add[]` and `remove[]` with the IDs returned by course_enrolled_users('add'|'remove', $courseid, $options) (the tenant-scoped list), or check every target user's open_path ...
- **Already patched?** No
- **Verifier:** The main claim holds up in the code and can be reached. Two of the sub-claims are overstated. 1) The guard is a no-op (confirmed). In local/includes.php:235-268, has_user_permission::access_courses_permission() returns false only when courseid is empty. The tenant comparison (lines 245-261) is commented out, and line 264 always does `return true`. Nothing in 02-enhancement-code patches this function: all-modifications.patch only changes how local/search/coursedetails.php includes this file. 2) Cross-tenant enrolment writes are reachable (confirmed) in local/courses/courseenrol.php: - Lines 43-47: the course comes only from the `enrolid` request param, via `$DB->get_record('enrol', ...

### 10. [P0] local (top-level), local_program, local_learningplan - 3-unauthenticated-trigger

- **Capability:** none (no require_login)
- **Where:** local/course_completions.php:36-40 (runs the core completion daily and regular tasks), local/program/test.php (program completion task), local/learningplan/lep_course_completion.php (complete_the_lep for all plan users), local/program/checkenrol.php:10-20 (enrols an arbitrary userid who is a program member)
- **What crosses tenants:** Site-wide completion writes and enrolments across all tenants, with load or DoS potential.
- **Who can reach it:** Unauthenticated.
- **Fix:** - local/course_completions.php and local/program/test.php: remove them, or move them under admin/cli or tests. Core already has scheduled tasks for the completion cron (completion_daily_task, completion_regular_task), and local/program has local_program\task\programcompletion. If they must stay, add require_login(); require_capability('moodle/site:config', context_system::instance()); require_sesskey(); - local/learningplan/lep_course_completion.php: first check whether a production cron job curls this URL. Then replace it with a scheduled task, e.g. local_learningplan\task\lep_completion calling complete_the_lep, running under the task lock, and delete the web script. Until then, add the same site-admin, capability and sesskey guards. - local/program/checkenrol.php: - add require_login() and require_sesskey() (and put sesskey on the renderer.php:500 link); - drop the userid parameter ...
- **Already patched?** No
- **Verifier:** I re-read all four scripts in the 01-production-codebase snapshot. None of them calls require_login, require_capability, require_sesskey or is_siteadmin. I also looked for anything that would force a login globally and found nothing: config.php has no require_login, forcelogin or auto_prepend (I grepped only for those words), lib/setup.php has no eAbyas login hook, there is no .htaccess or .user.ini, and the epsilon theme layouts do not redirect guests. So all four URLs run for an anonymous visitor. The one thing I cannot check is the production web-server (nginx/Apache) config, which is not in the snapshot. None of these files appears in 02-enhancement-code (all-modifications.patch or the ...

### 11. [P0] local_assignroles - (1)+(5) privilege escalation — assign/unassign any role to any user in any tenant

- **Capability:** local/costcenter:manage_multiorganizations OR local/assignroles:manageassignroles (both manager-archetype defaults)
- **Where:** submit_assignrole_form (classes/external.php:29-61) and submit_assigncostcenterrole_form (:398-430): validate_context uses accesslib::get_module_context() (caller's own context, ignores the target category), then rolesassign() -> classes/local/assignrole.php:39-45 calls core role_assign($roleid,$user,$contextid) with client-chosen roleid/users/category. local_unassign_role (:95-110) calls role_unassign with client contextid/roleid/userid and no check. role_ids/role_costcenterusers AJAX (external.php:150-323) source the pickable roles/users but the submit path never re-validates them.
- **What crosses tenants:** Grants any role (incl. Administrator, roleid via 'administrator' allowed at external.php:212) to any user at any category context across all 3 tenants; can unassign others' roles.
- **Who can reach it:** Any manager-archetype holder (role 1 or 9 default-grants manageassignroles per mdl_role_capabilities row 1488/1805) reachable via index.php:50 which admits manage_multiorganizations OR manageassignroles.
- **Fix:** In local/assignroles/classes/external.php: (a) submit_assignrole_form and submit_assigncostcenterrole_form: - Resolve the target costcenter from the validated server-side param (costcenterid, with PARAM_INT). Never use raw $data['open_costcenterid']. - Load local_costcenter with MUST_EXIST and build $targetctx = context_coursecat::instance($cc->category). - Call self::validate_context($targetctx), then require_capability('local/assignroles:manageassignroles', $targetctx) and require_capability('moodle/role:assign', $targetctx). - Unless is_siteadmin(), require the costcenter's path to be the caller's tenant root or a descendant of it, using a bounded path check (tenant::path_descendant_filter-style '/1' vs '/1/%', not an unbounded LIKE). - Cast roleid to int and require it to be in array_keys(get_assignable_roles($targetctx)). Use core get_assignable_roles, not the plugin's ...
- **Already patched?** Not addressed in 02-enhancement-code.
- **Verifier:** I re-read the code in the 01-production-codebase snapshot. The finding holds, and the exposure is wider than the sweeper said: nothing on the write path checks a capability, so ANY logged-in user can reach it, not just manager-archetype holders. 1. Default grant is real. local/assignroles/db/access.php gives local/assignroles:manageassignroles to 'manager' => CAP_ALLOW at CONTEXT_COURSECAT. local/costcenter/db/access.php:151-158 gives local/costcenter:manage_multiorganizations to 'manager' => CAP_ALLOW. But these only gate the UI page (index.php:50) and the left-menu entry (lib.php:302,318). 2. No capability check on the write services. db/services.php:28-59 declares all four functions ...

### 12. [P0] local_classroom - 4-write-no-target-check

- **Capability:** none (ajax web services)
- **Where:** local/classroom/externallib.php:1087 delete_classroom_instance, :1321 delete_session_instance, :1430 delete_classroomevaluation_instance, :1722 delete_classroomcourse_instance. No capability check and no validate_context.
- **What crosses tenants:** Deletes any tenant's classroom together with its sessions, attendance, completions, evaluations and calendar events.
- **Who can reach it:** Any logged-in user.
- **Fix:** In each of the four functions (and in manageclassroomStatus_instance), start with $params = self::validate_parameters(...). Then load the target record with MUST_EXIST: local_classroom for the classroom delete, local_classroom_sessions then its classroomid for the session delete, local_evaluations for the evaluation delete, and local_classroom_courses for the course link, making sure plugin='classroom' and instance/classroomid match the classroomid sent in the request. Next, $ctx = (new \local_classroom\lib\accesslib())::get_module_context($classroomid); then self::validate_context($ctx); then require_capability at $ctx with the matching capability: local/classroom:deleteclassroom, :deletesession, :deletefeedback or :deletecourse, together with local/classroom:manageclassroom as the renderer already requires. Add a tenant check: unless the user is a site admin or truly holds ...
- **Already patched?** No
- **Verifier:** I checked every step in the production snapshot (D:/Claude Local/Moodle Backup/01-production-codebase/html) and the finding holds. 1. How the functions are exposed. local/classroom/db/services.php registers four functions with 'ajax' => true and 'type' => 'write', and none of them sets loginrequired: local_classroom_deleteclassroom (lines 42-49), local_classroom_deletesession (58-65), local_classroom_deleteclassroomevaluation (66-73) and local_classroom_deleteclassroomcourse (98-105). In core, lib/externallib.php call_external_function (from line 191, reached through lib/ajax/service.php:81) only checks allowed_from_ajax, isloggedin() and require_sesskey(). Every logged-in user has their ...

### 13. [P0] local_costcenter - (4) unauthenticated-of-capability write — delete any costcenter/org node

- **Capability:** none (require_login only) on the delete web service
- **Where:** local/costcenter/classes/external.php costcenter_delete_costcenter (:191-228): no validate_parameters context, no require_capability, no is_siteadmin — takes id and deletes {local_costcenter}, {local_costcenter_permissions} and the {course_categories} row. costcenter_status_confirm (:129-170) likewise has no cap check and, on hide, bulk-suspends every user under that costcenter path. Both ajax=>true in db/services.php.
- **What crosses tenants:** Destruction of any tenant's org node/category; mass-suspension of all users under any org id (writes mdl_user.suspended across tenants).
- **Who can reach it:** Any authenticated user who can call the AJAX WS.
- **Fix:** In both costcenter_delete_costcenter and costcenter_status_confirm: (a) Load the costcenter with MUST_EXIST. (b) Resolve the target's context with context_coursecat::instance($costcenter->category) and call self::validate_context() on it. (c) Call require_capability('local/costcenter:delete', ctx) for delete and require_capability('local/costcenter:visible', ctx) for the status toggle. (d) Unless the caller is site admin or holds manage_multiorganizations (which should itself not be default-granted to the manager archetype), check that the target's path is inside the caller's own open_path, using a bounded match: target path = caller path OR LIKE caller path . '/%'. (e) Replace LIKE '%id%' in status_confirm with a bounded path match. Match rows where concat('/',open_path,'/') LIKE '%/<id>/%', or better, build the node's full path and use open_path = :p OR open_path LIKE :p/%. Suspend ...
- **Already patched?** Not addressed in 02-enhancement-code (renderer/settings patched only).
- **Verifier:** I read the code myself and the finding holds. File: D:/Claude Local/Moodle Backup/01-production-codebase/html/local/costcenter/classes/external.php. 1) costcenter_delete_costcenter (external.php:191-222). There is no validate_context, no require_capability/has_capability, no is_siteadmin and no check on the caller's costcenter or open_path. With the given id it: - renames the linked category's idnumber (:206); - deletes the {local_costcenter} row (:208); - deletes the matching {local_costcenter_permissions} rows (:209); - hard-deletes the {course_categories} row with a raw delete_records (:210). This is not the category API, so the category's courses and its context are left orphaned. 2) ...

### 14. [P0] local_costcenter - (4) unchecked create/edit of org nodes

- **Capability:** none (require_login only)
- **Where:** local/costcenter/classes/external.php submit_costcenterform_form (:53-101) and department_create (:926-971): validate_context uses caller's own get_module_context(), no require_capability; costcenter_edit_instance/insert_instance (lib.php) write regardless of target. department_create resolves org by shortname from client input.
- **What crosses tenants:** Create/rename org units and departments in any tenant.
- **Who can reach it:** Any authenticated user reachable via the AJAX WS.
- **Fix:** 1. In submit_costcenterform_form, after validate_context: - Edit (id > 0): load the target record with MUST_EXIST. Require that the target path lies inside the caller's costcenter path; reuse accesslib::get_costcenter_path_field_concatsql('path', null, 'lowerandsamepath') in an EXISTS check. Then call require_capability('local/costcenter:update', get_module_context($target->path)). Only let fields defined in the form be written. - Create: reject formtype=organization unless the user is a site admin. For department types, load the resolved parent (open_costcenterid, open_department, open_subdepartment or open_level4department), check that its path is inside the caller's scope, and call require_capability('local/costcenter:create', get_module_context($parent->path)). - Make the function return the id it declares as its return value. 2. In local_costcenter_organization_hierarchy_fields ...
- **Already patched?** Not addressed in 02-enhancement-code.
- **Verifier:** I checked this in the snapshot and it holds. Both web service functions skip every capability and tenant check, so any logged-in user can write org nodes in any tenant. There is one condition: department_create only works if production runs PHP older than 8.0 (explained below). How the call is reached - local/costcenter/db/services.php:30-37 and :86-93 register local_costcenter_submit_costcenterform_form and local_costcenter_department_create as type 'write' with 'ajax' => true. Neither lists a capability (in Moodle that key would only be documentation anyway). - lib/externallib.php:221-234 (call_external_function) only checks that the call is allowed from AJAX, that the user is logged in ...

### 15. [P0] local_courses - 4-write-no-target-check

- **Capability:** none (ajax web service)
- **Where:** local/courses/classes/external.php:655 delete_course (service local_courses_deletecourse, ajax). No capability check and no validate_context.
- **What crosses tenants:** Deletes any course in any tenant, including enrolments, grades and completions.
- **Who can reach it:** Any logged-in user.
- **Fix:** In local/courses/classes/external.php, change delete_course() as follows: 1. Validate the parameters and load the course before doing anything: `$params = self::validate_parameters(self::delete_course_parameters(), compact('action','id','confirm','name'));` `$course = $DB->get_record('course', ['id' => $params['id']], '*', MUST_EXIST);` Refuse the call if `$course->id == SITEID`. 2. Check the course context and the same capabilities the UI checks: `$context = context_course::instance($course->id);` `self::validate_context($context);` `require_capability('moodle/course:delete', $context);` `require_capability('local/courses:delete', $context);` `require_capability('local/courses:manage', $context);` 3. Check that the course belongs to the caller's tenant. Unless the caller `is_siteadmin()`, compare the course's costcenter path (`$course->open_path`, or its category's costcenter mapping) ...
- **Already patched?** No
- **Verifier:** I checked this against the production snapshot (Moodle 4.1.2+, Build 20230401) and every part of the claim holds. 1. The service is callable over AJAX. In local/courses/db/services.php:70-77, `local_courses_deletecourse` maps to `local_courses_external::delete_course`. It has `ajax => true` and `type => write`. It sets no `capabilities` key and no `loginrequired` key, so `loginrequired` defaults to true (lib/externallib.php:165-168). 2. The only gate is login plus sesskey. The request enters through lib/ajax/service.php:81, which calls `external_api::call_external_function($methodname, $args, true)`. That function (lib/externallib.php:191-260) checks three things: the function is allowed ...

### 16. [P0] local_courses - 4-write-no-target-check

- **Capability:** none
- **Where:** local/courses/classes/external.php:1176 course_update_status, :66 submit_create_course_form (update_course at :215 and :294, id taken from form data), :369 submit_create_category_form (:409 update, :411 create), :1665 delete_coursetype, :1725 coursetype_update_status, :1795 adddashboardcourses
- **What crosses tenants:** Changes any course's visibility and fields, including open_path and category, so a course can be moved to another tenant. Creates or renames any category, including tenant root categories. Deletes or edits course types.
- **Who can reach it:** Any logged-in user. validate_context on a category context only requires login.
- **Fix:** In each function, check the TARGET object after validate_context. Never rely on the context the caller sends. (a) course_update_status: load the course with MUST_EXIST, then require_capability('local/courses:visibility', context_course::instance($id)) (or moodle/course:visibility). Also require that the course's open_path lies within the caller's tenant: is_siteadmin(), or the path starts with one of $USER->useraccess['currentroleinfo']['contextinfo'][*]['costcenterpath'] on a '/'-bounded match. Deny when contextinfo is empty (fail closed). (b) submit_create_course_form: - When id > 0: require_capability('moodle/course:update', context_course::instance($id)) plus the same open_path-in-tenant check. - When creating: require_capability('moodle/course:create') on the target category context. - Stop passing the raw $data to update_course at line 294. Build the update object only from ...
- **Already patched?** No
- **Verifier:** I re-read the snapshot code and the claim holds. None of the six functions calls require_capability or has_capability on the course, category or course type being changed. All of them are declared ajax => true in local/courses/db/services.php:29-186, so any logged-in session can call them through lib/ajax/service.php. Core's call_external_function (lib/externallib.php:226-235) only checks isloggedin() and the sesskey. isloggedin() is also true for the guest user, so a guest can call them too if guest login is enabled. validate_context (lib/externallib.php:489-517) only runs require_login(null) for a system or category context. The services.php entries declare no capabilities, and Moodle ...

### 17. [P0] local_courses - 4-write-no-target-check

- **Capability:** none (page)
- **Where:** local/courses/index.php:53-64 (repeated at :160-170)
- **What crosses tenants:** Hides or shows any course category and every course in it through a GET request, with no sesskey.
- **Who can reach it:** Any logged-in user, and also reachable by CSRF.
- **Fix:** In local/courses/index.php: 1. Delete the duplicate block at lines 160-170. 2. Move the toggle at lines 53-65 so it runs only after the capability gate, and harden it: ```php if ($categoryid > 0 && $visible != -1) { require_sesskey(); $cat = core_course_category::get($categoryid, MUST_EXIST, true); require_capability('moodle/category:manage', context_coursecat::instance($cat->id)); // Tenant scope: the category must be inside the actor's costcenter unless siteadmin. // Use the BizLMS category-to-open_path mapping via local_costcenter\lib\accesslib. if ($visible) { $cat->show(); } else { $cat->hide(); } // core API: cascades to courses, keeps visibleold, fires events redirect(new moodle_url('/local/custom_category/index.php')); } ``` 3. Drop the raw `$DB->execute` string concatenation and the separate `$hide` parameter; core's show()/hide() handles the course cascade. The template ...
- **Already patched?** No
- **Verifier:** I checked this in the production snapshot, D:/Claude Local/Moodle Backup/01-production-codebase/html/local/courses/index.php, and the finding holds. 1. Line 32 is the only check that runs before the write, and it is a plain `require_login()`. Line 50 calls `local_courses\lib\accesslib::get_module_context()`, which is `local_costcenter\lib\accesslib::get_module_context` (lines 74-97). That function only returns a context. It never throws and never checks a capability. 2. The write is at lines 53-65. It runs when `categoryid > 0` and `visible != -1`, both taken from GET/POST as PARAM_INT. It calls `$DB->update_record('course_categories', {id: categoryid, visible})` and then `UPDATE {course} ...

### 18. [P0] local_courses - 4-write-no-target-check

- **Capability:** none
- **Where:** local/courses/enrolledusers.php:32-60 (only require_login, no sesskey)
- **What crosses tenants:** Unenrols any user_enrolments row from any course, and renders the course's enrolled users.
- **Who can reach it:** Any logged-in user, and also reachable by CSRF.
- **Fix:** In local/courses/enrolledusers.php, change only the ue branch, before $OUTPUT->header(): 1. Protect the request: if ($ue) { require_sesskey(); require a confirm param; }. 2. Load the enrolment from the target course only, and fail if it belongs anywhere else: $ue = $DB->get_record_sql('SELECT ue.*, e.enrol, e.courseid FROM {user_enrolments} ue JOIN {enrol} e ON e.id = ue.enrolid WHERE ue.id = :ueid AND e.courseid = :courseid', ['ueid' => $ueid, 'courseid' => $courseid], MUST_EXIST);. 3. Unenrol against the enrolment's own instance: $instance = $DB->get_record('enrol', ['id' => $ue->enrolid], '*', MUST_EXIST);. Do not look the instance up by plugin type. 4. Check permissions: require_capability('enrol/' . $ue->enrol . ':unenrol', context_course::instance($courseid)), plus the plugin gate local/courses:managecourses on the course's category context. Also require ...
- **Already patched?** No
- **Verifier:** I re-read the code and could not refute the finding. Snapshot: D:/Claude Local/Moodle Backup/01-production-codebase/html. 1. There is no authorization on the write. local/courses/enrolledusers.php:32 calls only require_login(). Line 34 calls get_module_context($courseid), which only looks up a context. The chain is local/courses/classes/lib/accesslib.php:47 -> local/costcenter/classes/lib/accesslib.php:74 and :364, and neither does a capability check. There is no require_capability, has_capability or require_sesskey anywhere before the unenrol. The page reads the ue id with optional_param('ue', 0, PARAM_INT) at line 31. Lines 55-66 then: - load that user_enrolments row by id from any course ...

### 19. [P0] local_courses - 5-priv-escalation

- **Capability:** local/courses:enrol (manager archetype)
- **Where:** local/courses/classes/form/mass_enroll_form.php:65 (roleassign is a hidden PARAM_INT) and :71-73 (firstcolumn is a hidden PARAM_RAW), used in local/courses/lib.php:67 and :186 enrol_user(..., $roleid) and :123 (column name and CSV value interpolated into SQL). mass_enroll.php:45-46 checks capability at the caller's own context.
- **What crosses tenants:** Enrols users with an arbitrary role id (for example manager or editingteacher) at course context, bypassing get_assignable_roles. The CSV and hidden-field values reach SQL unparameterised.
- **Who can reach it:** local/courses:enrol holders (org admins).
- **Fix:** mass_enroll.php: - Replace the check at lines 45-46 with `$coursecontext = context_course::instance($course->id); require_capability('local/courses:enrol', $coursecontext);` or check at `get_module_context($course->id)`. - Replace `access_courses_permission()`, which always returns true in local/includes.php:235-265, with a real path-bounded check: the course `open_path` must equal, or sit under `'/'.`, one of the caller's `local_userdata` costcenter paths, unless the caller is a site admin. Form and lib.php: - Remove the `roleassign` and `firstcolumn` hidden fields. Set them on the server instead: `$roleid = $DB->get_field('role','id',['shortname'=>'employee'], MUST_EXIST)` and `$useridfield = 'idnumber'`. - If the role must stay selectable, reject any value not in `array_keys(get_assignable_roles($coursecontext))`. If the id column must stay selectable, whitelist it with ...
- **Already patched?** No
- **Verifier:** I re-read the code in the 01-production-codebase snapshot and the finding holds. Two of its line numbers are a little off, and two things it did not mention make it worse. 1. The capability is granted by default. `local/courses/db/access.php:30-37` gives `local/courses:enrol` (CONTEXT_COURSECAT) to the manager archetype with CAP_ALLOW. BizLMS org admins hold their roles at costcenter category context, and `get_user_roles_in_catgeorycontexts` reads exactly those assignments. 2. The capability is checked at the wrong context, and the course has no tenant gate. `mass_enroll.php:45-46` calls `get_module_context()` with no course id. `local/costcenter/classes/lib/accesslib.php:74-97` then ...

### 20. [P0] local_courses - 3-fail-open/read

- **Capability:** none
- **Where:** local/courses/ajax.php:34-47 (action=enrolledusers, only require_login) -> local/courses/renderer.php:290-328 get_course_enrolledusers. search[value] is concatenated into SQL at :321.
- **What crosses tenants:** Enrolled-user list (name, email, employee id, completion date) for any course, scoped only by the caller's path. The concatenated search makes the query injectable, which allows reads across tenants.
- **Who can reach it:** Any logged-in user.
- **Fix:** 1. local/courses/ajax.php: - Call require_login() before anything else. - Make courseid required_param(PARAM_INT). - Load the course with MUST_EXIST and use its context_course. - Call require_capability('local/courses:managecourses' or 'moodle/course:enrolreview', that course context) before the switch. - Check that the course's open_path is inside the caller's tenant with a proper path-descendant check. Refuse if the course belongs to another costcenter. - Read search as optional_param_array(..., PARAM_TEXT). 2. local/courses/renderer.php:320-322: replace the string building with bound parameters. Use $DB->sql_like($DB->sql_fullname('u.firstname','u.lastname'), ':s1', false) OR $DB->sql_like('u.open_employeeid', ':s2', false), and set $params['s1'] = $params['s2'] = '%'.$DB->sql_like_escape($dataobj->search).'%'. Also bind courseid and userid at :367-369 instead of interpolating them. ...
- **Already patched?** No
- **Verifier:** I checked this against the code in the 01 snapshot. The 02-enhancement-code tree does not touch local/courses/ajax.php, local/courses/renderer.php or the costcenter/users accesslib, so the finding is not patched there. 1. There is no capability check. local/courses/ajax.php:29-50 only calls require_login() at :35, with no course and no capability, and no require_capability / has_capability. action=enrolledusers (:40-47) passes the caller's courseid, start, length and search[value] straight to renderer->get_course_enrolledusers(). Any logged-in account (learner/employee, and a guest session if guest login is on) can call it for any course id. The data is a read, so no sesskey is needed, and ...

### 21. [P0] local_courses (entry) -> local_users filters - 1-unscoped-cap-default-granted

- **Capability:** local/costcenter:manage_multiorganizations (default-granted to manager archetype in local/costcenter/db/access.php)
- **Where:** local/courses/classes/external.php:745 global_filters_form_option_selector (service local_courses_form_option_selector, db/services.php:78, ajax, no capability) dispatches to local/users/lib.php users_filter:134, email_filter:197, employeeid_filter:256, username_filter:557
- **What crosses tenants:** Names, emails, employee IDs and usernames of every user in every tenant. The query argument is PARAM_RAW and is concatenated into the LIKE clause, so it is also an injection point.
- **Who can reach it:** Any org admin whose role has the manager archetype at their own org category passes has_capability at their own context and gets all tenants. Any logged-in user reaches the injectable path.
- **Fix:** 1. **Web service** (local/courses/classes/external.php:745): - Call `self::validate_context()` and require a real capability, e.g. `local/users:view` or `local/users:manage` at the caller's resolved org context. - Replace the PARAM_RAW `action` with an explicit allowlist (`users`, `email`, `employeeid`, ...) instead of building a plugin path and function name from it. - Cap `perpage` to 1..100 and treat `page` as a page number (offset = page*perpage). 2. **Parameterise the LIKE** in users_filter, email_filter and employeeid_filter (local/users/lib.php:148/151, 206/209, 265/268): ```php $DB->sql_like($field, ':q', false); $params['q'] = ($searchanywhere ? '%' : '') . $DB->sql_like_escape($query) . '%'; ``` 3. **Fail closed** in local_costcenter\lib\accesslib (get_user_roleswitch_costcenterpath_concatsql and userpath_match_sql): - When no path resolves for a non-admin, return `AND 1=0`, ...
- **Already patched?** No: 02-enhancement-code contains no local/courses or local/users/lib.php changes. Live server may differ.
- **Verifier:** I re-read every link in the chain in the 01-production-codebase snapshot. The finding holds, with two corrections and one addition. **Entry point, verified** - local/courses/db/services.php:78-85 registers `local_courses_form_option_selector` with ajax=true and type=read. It sets no capabilities and no loginrequired override. - lib/externallib.php:165-168 and 226-233 then only require `isloggedin()` plus sesskey. Any logged-in user can call it (a guest session too, if guest access is on). - local/courses/classes/external.php:745-783 calls no validate_context and no require_capability. `query` and `action` are both PARAM_RAW. Line 780 calls `<action>_filter('', $query, $searchanywhere, ...

### 22. [P0] local_custom_category - (4) unchecked delete + unchecked create/edit

- **Capability:** none (require_login only)
- **Where:** local/custom_category/ajax.php:36-42 case 'deletecustomcategory' deletes {local_custom_category} by required_param skillid with only require_login (sets context but never checks the cap). classes/external.php submit_custom_category_form (:41-66) validate_context on caller context, no require_capability; classes/lib.php:7-42 custom_category_operations writes costcenterid from client open_costcenterid (falls back to caller's) with no tenant check.
- **What crosses tenants:** Delete/create/re-parent any tenant's course categories.
- **Who can reach it:** Any authenticated user (ajax.php has no cap gate at all).
- **Fix:** ajax.php: - Add `require_sesskey()`. - Load the target row with MUST_EXIST, resolve the context for its costcenterid via `accesslib::get_module_context('/'.$rec->costcenterid)` (or check that the costcenterid is inside one of the caller's `$USER->useraccess['currentroleinfo']['contextinfo']` costcenterpaths, which is the same idea as tenant::path_descendant_filter), and call `require_capability('local/custom_category:create_custom_category', $ctx)` unless is_siteadmin(). - Refuse the delete if `course.open_categoryid = id` or any child `parentid = id` exists. - Accept POST only. external.php submit_custom_category_form: - After validate_context, call `require_capability('local/custom_category:create_custom_category', $context)`. - For non-admins, check that the effective costcenterid (client open_costcenterid, or the fallback) is within the caller's role costcenterpaths, and reject ...
- **Already patched?** Not addressed in 02-enhancement-code.
- **Verifier:** I re-read the code in D:/Claude Local/Moodle Backup/01-production-codebase/html/local/custom_category and every part of the claim holds. 1) Unchecked delete (local/custom_category/ajax.php:24-43). The only guard is `require_login()` at line 30. Line 32 sets the page context and checks nothing. At lines 37-39, `deletecustomcategory` runs `$DB->delete_records('local_custom_category', ['id' => required_param('skillid', PARAM_INT)])`. There is no has_capability, no confirm_sesskey, no costcenterid check, and no check that courses (course.open_categoryid) or child categories still point at the row. The UI hides the delete button in those cases; the server does not care. `required_param` reads ...

### 23. [P0] local_forum - 4 - destructive write with no capability check and no target costcenter check

- **Capability:** none (only a logged-in AJAX session)
- **Where:** local/forum/classes/external.php:460-487 (delete_forum, delete_course($id) at :467); exposed as ajax WS local_forum_deleteforum in db/services.php:14-21
- **What crosses tenants:** Permanently deletes ANY course by id in any tenant. There is no open_module='forum' check, so this is not limited to forum courses and can delete elearning/exam courses too. It also rewrites course_categories.coursecount.
- **Who can reach it:** Any authenticated user, student included
- **Fix:** In local_forum_external::delete_forum (local/forum/classes/external.php:460), make these changes: 1. Call self::validate_parameters(self::delete_forum_parameters(), compact('action','id','confirm','name')) and load the course with $DB->get_record('course', ['id' => $id], 'id, category, open_path, open_module', MUST_EXIST). 2. Refuse the front page (SITEID) and any course where open_module !== 'forum'. 3. Set $context = context_course::instance($course->id), then call self::validate_context($context). 4. Call require_capability('local/forum:delete', $context) and require_capability('moodle/course:delete', $context). Also require the course's category context, because the plugin capabilities are defined at CONTEXT_COURSECAT. 5. For callers who are not site admins, refuse unless the course's open_path is equal to, or a descendant of, the caller's resolved costcenter path. Use one ...
- **Already patched?** No copy of local/forum in 02-enhancement-code, so the snapshot matches known code. Verify on live.
- **Verifier:** I checked this against the snapshot code and it holds. 1. The function is exposed. local/forum/db/services.php:39-46 registers local_forum_deleteforum with 'ajax' => true and 'type' => 'write'. It sets no 'capabilities' key and does not set loginrequired=false, so it gets the core defaults. 2. Core only checks login and sesskey. In lib/externallib.php:220-240, call_external_function (reached through lib/ajax/service.php:81) checks allowed_from_ajax, isloggedin() and require_sesskey(), then validates parameters. It checks no capability. Any logged-in session can reach delete_forum, including a student in any tenant. 3. delete_forum in local/forum/classes/external.php:460-487 checks nothing. ...

### 24. [P0] local_forum - 4 - write on any course with no capability or tenant check

- **Capability:** none
- **Where:** local/forum/classes/external.php:549-573 (course_update_status: flips course.visible via update_record at :562); WS local_forum_course_update_status, services.php:38-45 (wrongly typed as 'read')
- **What crosses tenants:** Hides or unhides any course in any tenant. It also pushes the change into block_trending_modules.
- **Who can reach it:** Any authenticated user. validate_context on the course's category context only needs a visible category.
- **Fix:** In `local_forum_external::course_update_status` (`local/forum/classes/external.php:549`): 1. Load the course with MUST_EXIST. 2. Require `$course->open_module === 'forum'` (or whatever marker the plugin uses to identify forum courses); otherwise throw. 3. Require that the course's `open_path` is equal to or inside the caller's scope. Compare path segments (for example with a `path_descendant_filter` or an equivalent boundary-safe prefix check); do not use a bare string prefix. Site admins bypass this check. 4. Replace the bare `validate_context($context)` with: - `validate_context($context)` (the costcenter category context) - `require_capability('local/forum:visibility', $context)` - `require_capability('moodle/course:visibility', context_course::instance($course->id))` 5. Call `rebuild_course_cache($course->id)` (or use `course_change_visibility()`) so caches stay consistent. Update ...
- **Already patched?** Not in 02-enhancement-code
- **Verifier:** I checked the finding against the code and it holds. One detail is overstated, and that detail makes the defect worse, not better. 1. Registration. `local/forum/db/services.php:63-70` registers `local_forum_course_update_status` with `'ajax' => true` and `'type' => 'read'`. There is no `capabilities` key, and that key would not be enforced anyway. 2. Handler. `local/forum/classes/external.php:549-573` (`course_update_status`) does these steps in order: - `validate_parameters` - resolves the context with `local_courses\lib\accesslib::get_module_context($id)`. This reads `course.open_path` and passes it to `local_costcenter\lib\accesslib::costcenterpath_contextdata` ...

### 25. [P0] local_forum - 4 - enrolment write with no target costcenter check

- **Capability:** none
- **Where:** local/forum/classes/external.php:795-839 (forum_subscribe: manual enrol_user($instance, $USER->id, 5) at :820); WS local_forum_subscribe (services.php:88-95, typed 'read')
- **What crosses tenants:** The caller enrols themselves as a student (roleid 5 hard-coded) into ANY course id that has a manual enrol instance, in any tenant. That gives cross-tenant access to course content, the participant list (names/emails), forum posts and grades.
- **Who can reach it:** Any authenticated user
- **Fix:** Changes in local/forum/classes/external.php forum_subscribe(): 1. Put the parameters through self::validate_parameters(self::forum_subscribe_parameters(), ...). 2. Load the course with MUST_EXIST and reject it unless id > 1, open_module = 'forum', open_coursetype = 1 and visible = 1. 3. Reject the course unless its open_path is inside the caller's own tenant path. Use the same accesslib helper the listing uses (\local_costcenter\lib\accesslib get_costcenter_path_field_concatsql / a path-descendant check bounded on '/'). Fail closed when the caller's open_path does not resolve. 4. Reject guest users and require a capability, e.g. a new local/forum:selfsubscribe granted to the employee archetype, checked with self::validate_context(context_course::instance($course->id)). 5. Stop using the manual instance with hard-coded roleid 5. Either use the course's enrol_self instance and its ...
- **Already patched?** Not in 02-enhancement-code
- **Verifier:** I checked this against the code and could not refute it. 1. Registration. local/forum/db/services.php:89-95 registers local_forum_subscribe with ajax=true and type 'read'. It has no 'capabilities' entry and no 'loginrequired' override, so loginrequired defaults to true (lib/externallib.php:165-168). The AJAX gate at lib/externallib.php:226-234 only checks isloggedin() plus require_sesskey(). Any logged-in session can call it. 2. Handler. local_forum_external::forum_subscribe (local/forum/classes/external.php:795-835) has no validate_context(), no require_capability() and no costcenter/open_path check. It does not check that $id is a forum-type course (open_module='forum', open_coursetype=1) ...

### 26. [P0] local_forum - 4 - course create/edit with no capability or target-tenant check

- **Capability:** none (the form's has_capability calls only change which fields are shown)
- **Where:** local/forum/classes/external.php:68-284 (submit_create_forum_form): create_course at :162, update_course at :201, and update_course($data) with raw parse_str() input at :217 and :242
- **What crosses tenants:** Creates courses under any submitted open_path/category, and edits ANY existing course by id in any tenant. The raw-data branches let the caller set arbitrary course columns, including open_path and category, which moves a course into another tenant.
- **Who can reach it:** Any authenticated user
- **Fix:** 1. Before building the form in submit_create_forum_form, find the real target: - Read $targetid from the submitted form data, since the JS never sends the ws id. Cast it with (int). - Existing course (id > 0): load it with get_course. Call require_capability('local/forum:update', context_coursecat::instance($course->category)) and require_capability('moodle/course:update', context_course::instance($id)). Also require the course to be a forum course (open_module='forum', format='singleactivity'). - New course: call require_capability('local/forum:create', ...) on the target category context. Do this after working out the server-side path. 2. Check tenant scope with a boundary-safe helper, not strpos() === 0. For each scope path S in the caller's role contextinfo, allow a path P only if P === S or strpos(P, S . '/') === 0. - Check the existing course's open_path before editing. - Check ...
- **Already patched?** Not in 02-enhancement-code
- **Verifier:** I re-read the code in the production snapshot and could not refute the finding. Reachability. db/services.php:30-37 registers local_forum_submit_create_forum_form as an ajax write service. It declares no capabilities and does not set loginrequired=false, so any logged-in user with a session key can call it. submit_create_forum_form (classes/external.php:68-278) checks nothing beyond validate_context() at :83. The caller supplies contextid, so passing the system context reduces that check to "is logged in". I grepped the file: the only is_siteadmin() near this code is at :135, and it only changes how the department list is joined. The function has no has_capability or require_capability ...

### 27. [P0] local_forum - 3 - list returned before any capability check, plus SQL injection

- **Capability:** none: the view=ajax branch returns before require_capability
- **Where:** local/forum/forumenrol.php:57-66 (view=ajax runs before the require_capability calls at :75-77). It passes raw $_GET['options'] to course_enrolled_users() (local/courses/lib.php:1071-1225), which interpolates email/uname/idnumber/groups (:1093-1098, :1164, :1197-1208) and the organization/department values used as placeholder names (:1099-1152) straight into SQL.
- **What crosses tenants:** Returns full names and idnumbers of users in the caller's tenant path, or of ALL tenants when the caller's path does not resolve (see the accesslib fail-open finding). The unparameterised options allow reading the whole database, across all tenants.
- **Who can reach it:** Any authenticated user who knows any manual enrolid
- **Fix:** 1. In local/forum/forumenrol.php, move the three require_capability() calls (:75-77) and the access_courses_permission() check (:83-87) above the `if ($view == 'ajax')` branch. Also add require_sesskey() to that branch. 2. Reject the request unless $course_id == $instance->courseid, or just derive the course from $instance->courseid. 3. Stop reading $_GET/$_REQUEST directly. Read each option key through optional_param() with a whitelist of keys. Clean the list-type values (email, uname, idnumber, groups, organization, department, the department levels, states, district, subdistrict, village, location, hrmsrole) as PARAM_SEQUENCE or clean_param_array(PARAM_INT), then explode them. 4. Clean `remove[]` and `add[]` as PARAM_INT arrays. Rewrite :261-263 as a parameterised query. 5. In local/courses/lib.php course_enrolled_users(): - Replace every `IN ({$params[...]})` with ...
- **Already patched?** Not in 02-enhancement-code (neither local/forum nor local/courses is there)
- **Verifier:** I checked this against the snapshot code and could not refute it. Two sweeper line numbers are off: idnumber is at 1155-1156, not :1164, and groups is at 1198-1199. 1. The ajax branch has no capability check (class 3). In local/forum/forumenrol.php, only require_login() (:55) runs before the `if ($view == 'ajax')` branch at :57-67. That branch prints JSON and exits. The checks for local/forum:enrol, :unenrol and :manage (:75-77) come after it, and so does the has_user_permission::access_courses_permission() department check (:83-87). The only other gate is that `enrolid` must be an existing manual enrol instance (:44, MUST_EXIST). The course id `id` (:42) is never checked against ...

### 28. [P0] local_groups - (4) unchecked write — delete/toggle any group

- **Capability:** none (require_login only) on the delete/status web services
- **Where:** local/groups/classes/external.php groups_delete_groups (:170-181) deletes {local_groups}+{local_groups_permissions} by client id with no cap check; groups_status_confirm (:126-151) toggles visibility by id via raw UPDATE (SQL built with $visible/$id interpolated, though ints). Both ajax=>true.
- **What crosses tenants:** Delete or hide any tenant's group/cohort wrapper.
- **Who can reach it:** Any authenticated user via the AJAX WS.
- **Fix:** For groups_delete_groups (classes/external.php:170-180): 1. Either remove 'local_groups_delete_groups' from db/services.php (no JavaScript uses it), or harden it as below. 2. Call self::validate_parameters(). 3. Load $g = $DB->get_record('local_groups', ['id'=>$id], '*', MUST_EXIST) and $cohort = $DB->get_record('cohort', ['id'=>$g->cohortid], '*', MUST_EXIST). 4. Get $ctx = context::instance_by_id($cohort->contextid), then call self::validate_context($ctx) and require_capability('moodle/cohort:manage', $ctx), or 'local/groups:delete'. 5. Check that $g->open_path falls under the caller's costcenter path using the local_groups accesslib / costcenter path helper, with a bounded prefix match. Only site admins and holders of local/costcenter:manage_multiorganizations may skip this check. 6. Instead of the raw deletes, call the lib.php local_groups_delete_groups($cohort). It removes ...
- **Already patched?** Not addressed in 02-enhancement-code.
- **Verifier:** The delete part of the finding holds up. The status-toggle part is wrong: that endpoint cannot write anything. 1. Anyone logged in can reach both endpoints. In D:/Claude Local/Moodle Backup/01-production-codebase/html/local/groups/db/services.php, local_groups_status_confirm (lines 38-45) and local_groups_delete_groups (lines 54-61) are 'type'=>'write' and 'ajax'=>true, and neither sets 'loginrequired'. In lib/externallib.php:165-168 that setting defaults to true. At lines 226-233, call_external_function then only checks isloggedin() and require_sesskey(). There is no capability check and no transaction. So any logged-in user can call both through /lib/ajax/service.php using their own ...

### 29. [P0] local_learningplan - 4-write-no-target-check

- **Capability:** none
- **Where:** local/learningplan/classes/external.php:108 delete_learningplan, :149 toggle_learningplan, :221 lpcourse_unassign_course, :243 lpcourse_unassign_user (no capability). local/learningplan/assign_courses_users.php:67,103,144,157 (only require_login, no sesskey). local/learningplan/ajax.php:58-89 (userselfenrol with arbitrary userid, publishlearningplan bulk enrol, plus the users tab of any plan at :39-54)
- **What crosses tenants:** Deletes, toggles or edits any tenant's learning plan. Enrols or unenrols arbitrary users. Reads a plan's users.
- **Who can reach it:** Any logged-in user, and also reachable by CSRF.
- **Fix:** 1. external.php: in each write function, call self::validate_parameters, then: $ctx = \local_learningplan\lib\accesslib::get_module_context($id or $planid); self::validate_context($ctx); then require_capability for the matching action: 'local/learningplan:delete' for delete, ':visible' for toggle, ':assigncourses' for unassign_course, ':assignhisusers' for unassign_user. Also check that the plan's open_path is inside the caller's scope. Reuse the costcenter path filter, or use a bounded-prefix helper. Otherwise a manager in one tenant can still reach another tenant's plan by id. For unassign_user, also check that the target user's open_path is under the plan's path. 2. assign_courses_users.php: - Add require_sesskey() and the same context + capability + plan-scope checks, starting with ':assignhisusers' / ':assigncourses'. - Read every array with optional_param_array or ...
- **Already patched?** No
- **Verifier:** I re-read every cited path in the 01-production-codebase snapshot. The core claim holds: learning plan writes have no capability check, no check of the target's tenant, and on the page scripts no sesskey. Three sub-claims are overstated and need correcting. External functions (local/learningplan/classes/external.php, registered in db/services.php:36-75 with ajax=true): - delete_learningplan (:108), toggle_learningplan (:149), lpcourse_unassign_course (:221) and lpcourse_unassign_user (:243) never call validate_context, require_capability or any tenant check. They go straight into lib functions that have no guards either: lib.php:263 togglelearningplan, :284 delete_learning_plan (deletes ...

### 30. [P0] local_location - (4) create/edit institute/room without capability or tenant check

- **Capability:** none beyond require_login on submit
- **Where:** local/location/classes/external.php submit_instituteform_form (:28-70) and submit_roomform_form (:91-131): validate_context on caller's get_module_context(), no require_capability; institute_update_instance/room_update_instance write by client id. instituteform.php:37 defaults costcenter from caller path but a hidden field / AJAX value can be overridden.
- **What crosses tenants:** Create/edit institutes and rooms in other tenants.
- **Who can reach it:** Any authenticated user via AJAX WS.
- **Fix:** In external.php submit_instituteform_form: - Call require_capability('local/location:manageinstitute', $context). Use createinstitute when id == 0 and editinstitute when id > 0. - For non-siteadmins, compute $cc = (int)explode('/', $USER->open_path)[1] and refuse if it doesn't resolve (fail closed). Overwrite $valdata->costcenter = $cc so the posted value is ignored. - When id > 0, load the existing row with get_record('local_location_institutes', ['id' => $valdata->id], '*', MUST_EXIST) and throw required_capability_exception or moodle_exception if $existing->costcenter != $cc. In submit_roomform_form: - Call require_capability('local/location:manageroom', $context), or createroom/editroom to match. - Check that the target instituteid's costcenter equals $cc. - When id > 0, look up the existing room, join to its institute, and check that institute's costcenter equals $cc before ...
- **Already patched?** Not addressed in 02-enhancement-code.
- **Verifier:** I read the code and it backs up the finding. One correction on how it happens: nothing is default-granted to an archetype. The two write endpoints check no capability at all, so every logged-in user can reach them. 1. Any logged-in user can reach the endpoints. db/services.php:28-43 registers local_location_submit_instituteform_form and local_location_submit_roomform_form as 'write' with 'ajax' => true. db/access.php declares all 11 local/location:* caps with no 'archetypes' key, so no role gets them by default. That doesn't matter here, because external.php never calls require_capability or has_capability. - external.php:35/98 builds the context from accesslib::get_module_context() and ...

### 31. [P0] local_myteam - (4) supervisor scope not enforced on the target — allocate courses to / approve requests for / read any user

- **Capability:** none (require_login only); page gate is 'are you anyone's supervisor'
- **Where:** local/myteam/classes/external.php courseallocationdependencies (:278-345 case 'courseallocate' -> courseallocation_lib.php:153 courseallocation() enrols $user), modulecourseallocation (:487-520), myteamdisplaymodulewise (:722-780 uses decodedata->userid) and teamapprovalsactions (:643-670 -> team_approvals.php:136 team_requests_approved -> local_request approve()). None verify the target 'user'/request belongs to $USER's team (open_supervisorid). team.php:35 only checks that the caller is SOME supervisor.
- **What crosses tenants:** Enrol/allocate courses, classrooms, programs to any user; approve/deny any pending learning request (approve() at local/request/.../requestapi.php:161 has no requester-ownership check); read any user's module progress.
- **Who can reach it:** Any user who is a supervisor of at least one person (record_exists open_supervisorid). The get_team_* read helpers ARE scoped by open_supervisorid, but the write/approve entry points take the target id directly and skip that scope.
- **Fix:** 1. **Shared guard.** Add one helper, e.g. `local_myteam\access::require_team_member(int $targetuserid)`: - require_login(); - `$DB->record_exists('user', ['id' => $targetuserid, 'open_supervisorid' => $USER->id, 'deleted' => 0])`, or throw required_capability_exception / moodle_exception('nopermissions'). - Also require the target's open_path to be inside the caller's tenant, using `tenant::path_descendant_filter` or a bounded `'/'.$org.'/'` prefix. Allow site admins through explicitly. 2. **Call it in every external.** Put it at the top of `courseallocationdependencies`, `modulecourseallocation`, `teamallocationview` and `myteamdisplaymodulewise`, in the users_display_modulewise fragment, and in `manageteamview`. First check that the caller supervises at least one user, the same check as team.php:35. Declare `'capabilities'` in db/services.php and change `teamapprovalsactions` and ...
- **Already patched?** Not addressed in 02-enhancement-code.
- **Verifier:** I checked this against the 01-production-codebase snapshot. The finding holds, and the problem is wider than the sweeper said: the external functions never check that the caller supervises anyone. **Who can reach it** - local/myteam/db/access.php only defines local/myteam:approve_myteam_request_record, and no external function checks it. - Every function in db/services.php is 'ajax' => true and is also in MOODLE_OFFICIAL_MOBILE_SERVICE. - The only supervisor check is on the page (team.php:35-37, record_exists open_supervisorid = $USER->id). No external function repeats it. - Each external does only require_login() and validate_parameters(). So any logged-in user, including a plain learner ...

### 32. [P0] local_program - 4-write-no-target-check

- **Capability:** none (ajax web services)
- **Where:** local/program/externallib.php:128 delete_program_instance, :463 delete_programcourse_instance, :695 delete_level_instance, :776 inactive_program_instance, :820 active_program_instance, :1012 unenrol_user (-> program_remove_assignusers)
- **What crosses tenants:** Deletes or deactivates any program, and removes any user from any program and its courses.
- **Who can reach it:** Any logged-in user.
- **Fix:** In each write function (delete_program_instance, delete_programcourse_instance, delete_level_instance, inactive_program_instance, active_program_instance, unenrol_user), add these checks in this order: 1. Call self::validate_parameters(...). 2. Load the program with $DB->get_record('local_program', ['id' => $programid], '*', MUST_EXIST). For level and level-course operations, also load the level or level-course row and assert its programid matches. 3. Resolve $ctx = \local_program\lib\accesslib::get_module_context($programid) and call self::validate_context($ctx). 4. Call require_capability on the matching capability: 'local/program:deleteprogram' for deletes, 'local/program:manageprogram' for activate/deactivate and level/course edits, and 'local/program:manageusers' (or the plugin's unenrol capability) for unenrol_user. Use the capability names that exist in ...
- **Already patched?** No
- **Verifier:** I checked this in the production snapshot at D:/Claude Local/Moodle Backup/01-production-codebase/html/local/program. None of the six named functions checks a capability or the target's tenant, and any logged-in user can call them. 1. **How the calls are reached.** db/services.php declares local_program_deleteprogram, local_program_deleteprogramcourse, local_program_deletelevel, local_program_inactiveprogram, local_program_activeprogram and local_program_unenrol_user with 'ajax' => true. None sets 'loginrequired' => false, and the 'capabilities' fields are empty, which only describes the function anyway. lib/externallib.php:221-234 (call_external_function), the path /lib/ajax/service.php ...

### 33. [P0] local_ratings - 4 - unauthenticated delete and write of other users' reviews

- **Capability:** none (no require_login)
- **Where:** local/ratings/delete.php:6-11 (raw $_REQUEST, delete_records('local_comment', id)); comment.php:6-31, update.php:9-61 and index.php:28-73 also write with no require_login or sesskey
- **What crosses tenants:** Anyone, even without logging in, can delete any course, classroom or program review or comment in any tenant by id. They can also add ratings, likes and comments as userid 0, which changes the aggregate ratings shown to every tenant.
- **Who can reach it:** Unauthenticated visitors and any user
- **Fix:** 1) Delete local/ratings/delete.php, comment.php and allcomment.php. Nothing in the JS calls them, and the web service local_ratings_save_comment replaces comment.php. 2) Move ratings.js off update.php (line 71) and index.php (line 95) onto the existing web services (local_ratings_set_module_rating / submit_rating and local_ratings_like_dislike, both login-required). Then delete update.php and index.php. 3) If they must stay for now, harden them: - Start each one with require_login(null, false) and require_sesskey(). - Read inputs with required_param(): 'itemid'/'item' as PARAM_INT, 'rating' as PARAM_INT, and check the area against a fixed list (local_courses, local_classroom, local_program, local_certification, local_learningplan). - Allow only ratings from 1 to 5. - Require local/ratings:canrate or can_participate($itemid, $area), so the user must be enrolled. - Before writing, check ...
- **Already patched?** 02-enhancement-code changes local/ratings/lib.php (context API fix) and settings.php only. delete.php, comment.php, update.php and index.php are not patched.
- **Verifier:** I re-read all four scripts in the snapshot (D:/Claude Local/Moodle Backup/01-production-codebase/html/local/ratings/) and checked the code paths that could have stopped them. None do. Could anything force a login? No. config.php only includes lib/setup.php (line 31), and setup.php never calls require_login. No local, theme, block, auth or admin/tool lib.php defines an after_config hook, and there is no .htaccess at the web root or in local/ratings. So a visitor who is not logged in runs these scripts with $USER->id = 0. None of the four calls require_login(), require_sesskey(), optional_param or required_param, or any has_capability. The one capability the plugin defines, ...

### 34. [P0] local_ratings - 4 - impersonated write, plus cross-tenant read of reviewer PII

- **Capability:** none
- **Where:** local/ratings/classes/external.php:93-122 (save_comment: the client supplies 'userid' and the comment is inserted or overwritten as that user); :461-494 (get_reviews: componentname concatenated into SQL at :470-471, returns reviewer names and emails); :143-190 with classes/lib/ratinglib.php:54-92 (display_ratings_content); reviews.php:26-64. WS are exposed to MOODLE_OFFICIAL_MOBILE_SERVICE.
- **What crosses tenants:** Any user can post or overwrite reviews in any other user's name, in any tenant. Reviewer full names and emails are returned for any item id, and get_reviews also allows arbitrary database reads.
- **Who can reach it:** Any authenticated user
- **Fix:** 1. save_comment (external.php:82-122) - Remove 'userid' from save_comment_parameters and use $USER->id throughout. - Call self::validate_context() with the item's context. Also call require_capability() on a new capability, e.g. local/ratings:comment. - Check that the caller can take part: can_participate($itemid, $commentarea), or the enrolment/membership checks in lib.php:216-245. - Check that the item belongs to the caller's costcenter: compare the item's open_path/costcenter against $USER->open_path with a bounded prefix, meaning equal to the caller's path or starting with that path plus '/'. Site admins are exempt. - Whitelist commentarea to the five known areas (local_courses, local_classroom, local_certification, local_program, local_learningplan). - Store the comment as PARAM_TEXT/PARAM_NOTAGS, not PARAM_RAW. - Set timemodified on insert. - Apply the same fixes to ...
- **Already patched?** Not patched (02-enhancement-code touches only lib.php and settings.php)
- **Verifier:** I re-read the snapshot code and every part of the sweeper's claim holds. 1. Class 4, impersonated write (save_comment). File: local/ratings/classes/external.php:82-122. - 'userid' is a required client parameter (PARAM_INT, line 88). - Line 108 looks up an existing comment for (itemid, commentarea, userid). If one exists, lines 111-114 overwrite its text. If not, lines 116-120 insert a new row with userid = the client's value. - Nothing compares that userid with $USER->id. There is no capability check and no validate_context call. can_participate() in lib.php (enrolment check) is never called, and neither is any costcenter/open_path check. - The only core gate is ...

### 35. [P0] local_request - 3-wrong-scope-key

- **Capability:** local/request:approverecord (default coursecreator/teacher/editingteacher/manager)
- **Where:** local/request/classes/output/requestview.php:117-118. The org costcenter id (1/77/177) is passed as the $userid argument of local_users accesslib::get_costcenter_path_field_concatsql, so scope comes from the open_path of user id 1/77/177. Also classes/external.php:40 view_availiable_request has no capability check, and filter values are concatenated into SQL at requestview.php:156-198.
- **What crosses tenants:** The request list (requester names, components) is scoped to an unrelated user's path, which can be another tenant. The query is also injectable.
- **Who can reach it:** Approvers in the Public and ZEEA tenants. Any logged-in user for the injectable path.
- **Fix:** 1) requestview.php:117-118: drop the explode, and do not pass $org as a user id. Use the caller's own scope: `$costcenterpathconcatsql = \local_users\lib\accesslib::get_costcenter_path_field_concatsql('u.open_path');` (a null user id falls back to the role-switch scope). Alternatively, pass a real path such as '/'.(int)$org to the local_costcenter base helper. Also fail closed: if a non-admin resolves to an empty filter, return no rows. 2) external.php view_availiable_request: after validate_parameters, add `$context = (new \local_request\lib\accesslib())::get_module_context(); self::validate_context($context); require_capability('local/request:viewrecord', $context);`. Drop the client-supplied contextid. 3) Parameterise every filter. Clean the values with `clean_param_array((array)$v, PARAM_INT)` and build each IN clause with `$DB->get_in_or_equal(..., SQL_PARAMS_NAMED, 'prefix')`, ...
- **Already patched?** No
- **Verifier:** I checked every part of the claim against the snapshot at D:/Claude Local/Moodle Backup/01-production-codebase/html/local/request. Each part holds. 1) The approve capability is granted by default. db/access.php:27-40 gives local/request:approverecord to coursecreator, teacher, editingteacher and manager (CONTEXT_COURSECAT). viewrecord (db/access.php:92-106) also goes to student and user. 2) The scope is keyed on a user id, not a tenant path. classes/output/requestview.php:117 runs `list($zero,$org,...) = explode("/",$USER->open_path)`, so $org becomes the costcenter id (1, 77 or 177). Line 118 then calls `\local_users\lib\accesslib::get_costcenter_path_field_concatsql('u.open_path', $org)`. ...

### 36. [P0] local_tags - 4-write-no-target-check

- **Capability:** none (local/tags:manage exists but is not checked)
- **Where:** local/tags/manage.php:30,98-160 (require_login + sesskey only) -> classes/tag.php:1541 delete_tags, classes/collection.php:267 delete
- **What crosses tenants:** Deletes, combines or renames site-wide tags and tag collections shared by all tenants.
- **Who can reach it:** Any logged-in user.
- **Fix:** In local/tags/manage.php, right after require_login() and the $context lookup (line 46): 1. Check the capability for the whole page: `require_capability('local/tags:manage', $context);` Better still, make it is_siteadmin() || has_capability('moodle/tag:manage', context_system::instance()) with a ::require_ equivalent. 2. Limit the collection actions (colladd, colldelete, collmoveup, collmovedown) to site admins, or to users with moodle/tag:manage at system context. {tag_coll} has no tenant column, so an org admin must not be able to change collections. 3. For the tag write actions (delete, bulk delete, bulkcombine, renamecombine, addstandardtag), check the target's tenant for every non-site-admin. For each tag ID in tagid, tagschecked[] and maintag, require: `$DB->record_exists_select('local_tags', 'tagid = :t AND open_costcenterid = :c', ['t' => $id, 'c' => $USER->open_costcenterid])` ...
- **Already patched?** No
- **Verifier:** I re-read the snapshot at D:/Claude Local/Moodle Backup/01-production-codebase/html/local/tags/ and the finding holds. 1. No capability check on the page. manage.php:30 calls only require_login(). Lines 46-86 set up the context, check that $CFG->usetags is on (a Moodle default), and load the tag and collection from request params. There is no require_capability or has_capability anywhere in the file. The actions in the switch at 88-191 check only require_sesskey(), which any user can pass with the sesskey from any page they are allowed to open. The one related check is at index.php:95, where `has_capability('local/tags:manage')` hides the "Manage tags" button. It does not protect ...

### 37. [P0] local_users - (4)+(5) write with no capability check and no target-tenant check

- **Capability:** none (require_login only)
- **Where:** local/users/classes/external.php submit_create_user_form (:53-112) calls functions/users.php update_existinguser (:90) / insert_newuser (:31); delete_user (:139-166) and suspend_local_user (:177-207) do only $DB->get_record + act. None call require_capability or verify the target user's open_path against the caller. Registered ajax=>true, type=write in db/services.php:26-48.
- **What crosses tenants:** Create/edit/suspend/soft-delete of ANY user in any tenant, including editing email/username and setting a password hash (users.php:97 hash_internal_user_password then user_update_user at :148).
- **Who can reach it:** Any authenticated user who can reach the AJAX web service (all fields incl. id and open_costcenterid arrive in client jsonformdata; the org autocomplete accepts arbitrary values per lib/form/autocomplete.php:195). No manager role even required for delete_user/suspend_local_user.
- **Fix:** 1. In delete_user, suspend_local_user, submit_create_user_form and local_create_update_user, call self::validate_parameters() and then self::validate_context(), starting with the system context. 2. Work out the context from the target user's stored record in the database, never from ids or costcenters the client sends. Use (new \local_users\lib\accesslib())::get_module_context($targetid), then call require_capability with local/users:delete, local/users:bulkstatuschange or local/users:manage, local/users:edit, or local/users:create (for a new user) as appropriate. 3. Check that the target's open_path is inside the caller's costcenterpath, using a bounded prefix: '/'.path.'/' must start with the caller's path plus '/'. Fail closed when the caller's path does not resolve. 4. Refuse any target where is_siteadmin($target) is true unless the caller is a site admin. Refuse self-suspend and ...
- **Already patched?** Not addressed in 02-enhancement-code (only privacypolicy/termscondition/settings modified there).
- **Verifier:** I re-read the code in the 01-production-codebase snapshot and the sweeper's finding holds. One nuance is below, and one sibling endpoint has the same defect. **How these endpoints are reached.** db/services.php:26-48 registers local_users_submit_create_user_form, local_users_delete_user and local_users_suspend_user as type=write with ajax=>true. They are called through the standard lib/ajax/service.php:81, which runs external_api::call_external_function (lib/externallib.php:191-262). That function checks only three things: login (isloggedin), the caller's own sesskey (:226-235) and parameter types (:237). No plugin in local/, blocks/, theme/, auth/, mod/ or admin/tool/ defines ...

### 38. [P0] local_users - (4) IDOR — read any user's data by id

- **Capability:** none (require_login only)
- **Where:** local/users/classes/external.php profiledata (:686-731) and profilemoduledata (:801-846) take decodedata->userid and call \local_<module>\local\user::user_profile_content($userid) with no ownership/capability check. dashboard_stats (:882) similarly trusts $userid.
- **What crosses tenants:** Another user's enrolled courses, completion %, module progress across tenants.
- **Who can reach it:** Any authenticated user (services ajax=>true).
- **Fix:** Add an explicit authorisation check to both profiledata and profilemoduledata before user_profile_content is called. Do not rely on the calling page. Suggested fix, in local/users/classes/external.php inside both functions, after validate_parameters: $targetid = (int)$decodedata->userid; self::validate_context(context_system::instance()); // proper page/context setup instead of $PAGE->set_context($contextid) if ($targetid !== (int)$USER->id && !is_siteadmin()) { $pathsql = (new \local_users\lib\accesslib())::get_costcenter_path_field_concatsql('u.open_path', null, 'lowerandsamepath'); $ok = $DB->record_exists_sql("SELECT u.id FROM {user} u WHERE u.id = :id AND u.deleted = 0 $pathsql", ['id' => $targetid]); // Optionally also allow the target's direct supervisor (u.open_supervisorid = $USER->id). if (!$ok) { throw new required_capability_exception(context_system::instance(), ...
- **Already patched?** Not addressed in 02-enhancement-code.
- **Verifier:** I checked this in the 4.1.2 snapshot at D:/Claude Local/Moodle Backup/01-production-codebase/html. The core claim holds and the leak is worse than reported. The dashboard_stats sub-claim is wrong. 1. Both endpoints can be called by any logged-in user. In local/users/db/services.php:98-115, local_users_profile_moduledata and local_users_profile_data are both declared with 'ajax' => true and are also in MOODLE_OFFICIAL_MOBILE_SERVICE. Any authenticated user can call them through /lib/ajax/service.php with their own sesskey. No capability is declared. 2. There is no check on who the target is. In local/users/classes/external.php, profiledata (:686) and profilemoduledata (:801) run ...

### 39. [P0] local_users - (2) SYSTEM-context capability skips the tenant filter + raw SQL injection sink

- **Capability:** local/users:manage (checked in CALLER context only)
- **Where:** local/users/download.php:42 checks has_capability('local/users:manage', $categorycontext) then passes optional_param('extrasql', '', PARAM_RAW) into functions/users.php get_users_listing (:321-349) which concatenates it directly: "... WHERE $select ... $sort $dir LIMIT $page,$recordsperpage" with $select .= " AND $extraselect". No costcenter filter is ever added in this path.
- **What crosses tenants:** Arbitrary user rows (all tenants) plus SQL injection via extrasql/sort/dir (PARAM_ALPHANUM/ALPHA on sort/dir but extrasql is PARAM_RAW).
- **Who can reach it:** Any holder of local/users:manage. If held at system context (manager archetype, role 9), the export spans all tenants; the raw extrasql makes it an injection sink for anyone who passes the cap check.
- **Fix:** Take local/users/download.php offline now (delete it or make it return an error), then rebuild it. 1) Remove the extrasql and params request parameters entirely. Never accept SQL fragments from the client. Build any filters on the server from typed params such as PARAM_INT, PARAM_TEXT and PARAM_ALPHA. 2) In get_users_listing (local/users/classes/functions/users.php:321-349), always add the tenant filter. Use (new \local_users\lib\accesslib())::get_costcenter_path_field_concatsql('u.open_path'), or the bounded tenant::path_descendant_filter equivalent (it matches the path prefix with its trailing '/'). Only a genuine site admin may skip it; do not let local/users:manage at system context skip it. If the caller's open_path does not resolve, return no rows (fail closed). 3) Check sort against a fixed list of allowed columns and force dir to 'ASC' or 'DESC'. 4) Pass paging through ...
- **Already patched?** Not addressed in 02-enhancement-code.
- **Verifier:** I re-read the code in the 01-production-codebase snapshot and could not refute any part of the finding. One path is wrong: the sink is in local/users/classes/functions/users.php, not functions/users.php. 1) The capability is default-granted and checked only in the caller's own context. local/users/db/access.php gives 'local/users:manage' (CONTEXT_COURSECAT) to the manager archetype with CAP_ALLOW. download.php:40-44 checks it against (new \local_users\lib\accesslib())::get_module_context(). With no userid, that call goes to local/costcenter/classes/lib/accesslib.php:74-96. A non-admin gets the context of their current role (get_user_roleswitch_context, their org's category context, or ...

### 40. [P0] notifications - 3 (fail-open) plus a missing authentication gate

- **Capability:** none: no require_login() and no capability check
- **Where:** local/notifications/email_status_filters.php:25-120 (filter at :54-56, query at :101)
- **What crosses tenants:** Email-log listing for all tenants: organisation, sender and recipient full names, notification type, created and sent dates, send status, and the local_emaillogs ids that email_status_details.php accepts. The only scoping is get_costcenter_path_field_concatsql('ni.open_path'), which adds no filter for a session with no local_userdata row (costcenter accesslib.php:273/323-362), and that includes the not-logged-in session (userid 0).
- **Who can reach it:** Anyone, including an unauthenticated request. Any logged-in user whose local_userdata row is missing or empty also gets every tenant.
- **Fix:** In `local/notifications/email_status_filters.php`: 1. Right after `require_once(config.php)`, add `require_login(null, false);` and `require_sesskey();`. For the sesskey, pass `M.cfg.sesskey` in the DataTables `ajax.data` in `classes/output/renderer.php:244`. 2. Resolve the context with `$ctx = \local_notifications\lib\accesslib::get_module_context();`. Then check permission: `if (!is_siteadmin()) { require_capability('local/notifications:manage', $ctx); }`. This matches the check on the parent page, `email_status.php`. 3. Initialise `$cond_query = '';` and `$params = [];` before they are first used. 4. Make the scoping fail closed. For a non-admin, take `$scope = ...get_costcenter_path_field_concatsql('ni.open_path');`. If `trim($scope) === ''`, return an empty JSON result, or append `' AND 1=0'`. The better long-term fix is in `local_costcenter\lib\accesslib::userpath_match_sql` and ...
- **Already patched?** No. 02-enhancement-code does not touch local/notifications. The live server may differ, so verify there.
- **Verifier:** I re-read the code in the production snapshot and followed each step. The finding holds. Two of the sweeper's line numbers are off. 1. **No authentication gate.** `local/notifications/email_status_filters.php` sets `AJAX_SCRIPT` and requires `config.php` at lines 26-27. After that it never calls `require_login()`, `require_capability()`, `has_capability()` or `require_sesskey()`. - Loading `config.php` does not enforce login in Moodle. Even `forcelogin` is only applied inside `require_login()`. - No local, block or theme plugin in the snapshot has an `after_config` hook, and there is no `.htaccess` at the site root, in `local/` or in `local/notifications/`. Nothing else gates this script ...

### 41. [P0] notifications - 4 (write notification templates of any tenant) plus SQL injection

- **Capability:** none (validate_context only)
- **Where:** local/notifications/externallib.php:65-160 (update :107/:112, insert :130); form validation at classes/forms/notification_form.php:277 interpolates costcenterid/notificationid
- **What crosses tenants:** Overwrites the subject and body of any local_notification_info id, i.e. the emails cron sends to another tenant's users, or creates notifications in any org. The validation query is also injectable.
- **Who can reach it:** Any authenticated user.
- **Fix:** 1) externallib.php submit_create_notification_form, after validate_context: - If id > 0: load the record with $DB->get_record('local_notification_info', ['id' => $id], 'id, open_path', MUST_EXIST). Require local/notifications:update in the context for that record's open_path. Unless the caller is site admin, also require that the stored open_path is inside the caller's scope, using a bounded check: path === scope, or strpos(path . '/', scope . '/') === 0. - If id <= 0: require local/notifications:create. - In both cases, reject a submitted open_costcenterid outside the caller's scope with an exception. Do not rely on the silent clamp in local_costcenter_get_costcenter_path. - Work out open_path on the server only, and never copy it from the submitted data. - Add 'capabilities' => 'local/notifications:create,local/notifications:update' to db/services.php. 2) classes/lib/accesslib.php:41: ...
- **Already patched?** No.
- **Verifier:** The main finding holds: any logged-in user can overwrite another tenant's notification templates. One sub-claim is wrong: the SQL injection in the form's validation query can never run. CONFIRMED: cross-tenant write with no capability check (class 4) - db/services.php:28-35 registers local_notifications_submit_create_notification_form as an ajax 'write' function. There is no 'capabilities' key. - externallib.php:65-155 checks nothing except validate_context() on a contextid the caller supplies (:73-76). With the system context, that only calls require_login. There is no has_capability or require_capability anywhere in the file. - The UI page index.php:43 does require ...

### 42. [P0] onlineexams - 4 (destructive write, no target check) plus missing capability

- **Capability:** none (no validate_context, no has_capability)
- **Where:** local/onlineexams/classes/external.php:496-530 (delete_course at :503); service local_onlineexams_deleteonlineexams, ajax=true, db/services.php:39-46
- **What crosses tenants:** Deletes any course on the site (any tenant, any course type, not only online exams), with all enrolments, grades and completions.
- **Who can reach it:** Any authenticated user with a session (learners of Airpay, Public or ZEEA), through the AJAX web-service endpoint.
- **Fix:** In `local/onlineexams/classes/external.php` `delete_onlineexams()`, make the endpoint check everything before it deletes, and fail closed: 1. Validate the parameters. `$params = self::validate_parameters(self::delete_onlineexams_parameters(), ['action'=>$action,'id'=>$id,'confirm'=>$confirm,'name'=>$name]);` 2. Load the course and check it is an online exam. `$course = $DB->get_record('course', ['id'=>$params['id']], '*', MUST_EXIST);` Refuse if `$course->id == SITEID`, if `$course->open_module !== 'online_exams'`, or if `(int)$course->open_coursetype !== 1`. 3. Check context and capabilities. `$coursectx = context_course::instance($course->id); self::validate_context($coursectx);` Then `require_capability('local/onlineexams:delete', context_coursecat::instance($course->category));`, `require_capability('local/onlineexams:manage', context_coursecat::instance($course->category));` and ...
- **Already patched?** No, not in 02-enhancement-code.
- **Verifier:** I read the code myself, tried to refute the finding, and could not. 1. Registration: `D:/Claude Local/Moodle Backup/01-production-codebase/html/local/onlineexams/db/services.php:39-46` registers `local_onlineexams_deleteonlineexams`. It points to `local_onlineexams_external::delete_onlineexams`, has type `write` and `ajax => true`, and sets no `loginrequired => false` and no capabilities. 2. Function body: `local/onlineexams/classes/external.php:496-525` has no `validate_parameters`, no `validate_context`, no `require_capability`/`has_capability`/`is_siteadmin` check, no check that the course is an online exam, and no `open_path`/costcenter check. When `$confirm` is true it reads the ...

### 43. [P0] onlineexams - 4 (write, no capability or target check)

- **Capability:** none (validate_context of the course's category only)
- **Where:** local/onlineexams/classes/external.php:585-611 (update_record at :598); service declared type 'read' at db/services.php:63-70
- **What crosses tenants:** Hides or unhides any course site-wide, which takes courses out of, or pushes them into, every tenant's catalogue.
- **Who can reach it:** Any authenticated user.
- **Fix:** In local/onlineexams/classes/external.php course_update_status (and the same function in local/forum and local/courses): 1. After validate_parameters, load the course with MUST_EXIST. Reject SITEID. Also reject any course that is not an online exam, using the plugin's own course-type marker (for example open_coursetype or open_module). 2. Build $coursecontext = context_course::instance($course->id) and the costcenter category context from get_module_context($id). Call validate_context on the course context. Then call require_capability('local/onlineexams:manage', $catcontext), require_capability('local/onlineexams:update', $catcontext) and require_capability('moodle/course:visibility', $coursecontext). Use the equivalent local/forum or local/courses capabilities in the sibling functions. 3. Enforce the target's tenant. Unless is_siteadmin() or the caller holds ...
- **Already patched?** No.
- **Verifier:** I read the code myself and the finding holds. 1. Registration: D:/Claude Local/Moodle Backup/01-production-codebase/html/local/onlineexams/db/services.php:63-70 registers local_onlineexams_course_update_status with 'ajax' => true and 'type' => 'read'. It declares no capabilities, though Moodle would not enforce that key anyway. Because it is AJAX-enabled, any logged-in session with a sesskey can call it through lib/ajax/service.php. 2. Function body: local/onlineexams/classes/external.php:585-612. - It runs validate_parameters. - It resolves $context = local_courses\lib\accesslib::get_module_context($id), where $id is any course id the caller sends. The contextid parameter is ignored. - It ...

### 44. [P0] onlineexams - 4 (create/update of any course) plus missing capability

- **Capability:** none. $categorycontext is computed at :135 but never checked
- **Where:** local/onlineexams/classes/external.php:68-310 (create_course :165, update_course :223 and :248, update_moduleinfo :240/:254)
- **What crosses tenants:** Updates any course record and its quiz settings (grade to pass, attempts, open/close times) by the form's id field. It also creates exams whose open_path comes from submitted hierarchy fields (local_costcenter_get_costcenter_path), so exams can be planted in or moved into another tenant.
- **Who can reach it:** Any authenticated user.
- **Fix:** In local/onlineexams/classes/external.php::submit_create_onlineexams_form: (a) Validate 'id' through validate_parameters. Then take the course id to edit only from that parameter, and reject the call if the form's id differs from it. (b) When id>0: load the course with MUST_EXIST and require open_module == 'online_exams' and format == 'singleactivity'. Check the caller's scope against the course's open_path with a boundary-safe test: exact match or prefix plus '/', or tenant::path_descendant_filter / is_path_under. Then call require_capability('local/onlineexams:update', <module context for that course's open_path>) and require_capability('moodle/course:update', context_course::instance($id)). (c) When id==0: first resolve the target costcenter path from the submitted hierarchy. Reject the call if that path is not inside the caller's resolved path; never fall back to the submitted ...
- **Already patched?** No.
- **Verifier:** I tried to refute this and could not. I checked every point against the production snapshot (D:/Claude Local/Moodle Backup/01-production-codebase/html). 1. Reachability. The function local_onlineexams_submit_create_onlineexams_form is declared in local/onlineexams/db/services.php:30-37 as type 'write' with 'ajax' => true. It declares no 'capabilities', and that field would only be informational anyway. The core AJAX dispatcher, external_api::call_external_function (lib/externallib.php:221-234), only checks allowed_from_ajax, isloggedin() and require_sesskey(). Inside submit_create_onlineexams_form (local/onlineexams/classes/external.php:68) the only guards are validate_parameters and ...

### 45. [P0] onlineexams - 3 (fail-open listing) plus SQL injection in the shared sink

- **Capability:** none. The view=ajax branch runs before the require_capability calls
- **Where:** local/onlineexams/onlineexamsenrol.php:57-66, calling local/courses/lib.php:1071-1160 course_enrolled_users(), where options email, uname and idnumber are interpolated into IN (...) at :1094, :1097 and :1156
- **What crosses tenants:** User names and idnumbers, filtered only by the viewer's own path, and unfiltered when that path does not resolve. The raw interpolation of request-supplied options lets any logged-in user read arbitrary database content across all tenants.
- **Who can reach it:** Any authenticated user.
- **Fix:** 1. In local/onlineexams/onlineexamsenrol.php, put every check above the `view == 'ajax'` branch: `require_capability()` for onlineexams:enrol and :manage, and the `access_courses_permission($course_id)` check. Evaluate them in the context of the course that owns the enrol instance. - Also require `$course_id == $instance->courseid`. - Add `require_sesskey()` to the ajax branch. 2. Stop reading `$_GET` directly. Use `optional_param('options', '', PARAM_RAW)` and decode it with `json_decode($raw, true)`. Accept only a whitelist of keys and drop everything else. 3. In `course_enrolled_users()` in local/courses/lib.php, and in its copy in local/evaluation/lib.php, never put request values into SQL text. - For email, uname, idnumber and groups: explode the value, run `clean_param_array(..., PARAM_INT)`, drop zeros, and return an empty result if nothing is left. Then build the clause with ...
- **Already patched?** No. The sink lives in local/courses, which other partitions may also report.
- **Verifier:** I checked every link in the sweeper's chain in the code. None of it is refuted. This was a read-only review: I made no edits and contacted no server. 1. Reachable by any logged-in user (local/onlineexams/onlineexamsenrol.php). The only gate before the `view == 'ajax'` branch (lines 57-66) is `require_login()` at line 55. The three `require_capability()` calls (onlineexams:enrol, :unenrol, :manage) come after the branch, at lines 74-76. So does the `has_user_permission::access_courses_permission($course_id)` check at about line 83. The branch calls `exit` before any of them run. The only other precondition is `required_param('enrolid')`, which must name an existing manual enrol instance ...

### 46. [P0] onlineexams - 3 (fail-open) plus missing capability

- **Capability:** none (require_login only)
- **Where:** local/onlineexams/custom_ajax.php:38-60 (page 1) and :108-160 (page 2)
- **What crosses tenants:** Names, emails, idnumbers, grades, pass/fail status and enrol/completion dates of users in any exam course id. Users are filtered only by the viewer's own path (PATH_MODULE_CONTENT), so learners see peers' grades in their own subtree. When the viewer's path does not resolve, every tenant is returned.
- **Who can reach it:** Any authenticated user.
- **Fix:** In local/onlineexams/custom_ajax.php: 1. **Resolve and check the course.** Load it with `$course = get_course($id)` and assert it is an online exam (`open_module = 'online_exams'`, `open_coursetype = 1`). Then call `require_login($course)` (or `require_login()` plus `$PAGE->set_context(context_course::instance($id))`). 2. **Add the capability check.** Call `require_capability('local/onlineexams:grade_view', $ctx)` and `require_capability('local/onlineexams:manage', $ctx)`. Use the course context: these are COURSECAT caps, so the check inherits correctly. Fail with `required_capability_exception` otherwise. 3. **Scope the course to the caller.** Unless the caller is a site admin, assert that the course's open_path is inside one of the caller's costcenter paths, using a bounded match (path = P OR path LIKE P || '/%'). Throw if it is not. 4. **Fail closed.** If the caller is not a site ...
- **Already patched?** No.
- **Verifier:** I re-read the code myself. The finding is correct, with a few details to fix. 1. **No capability check (reachable by any logged-in user).** local/onlineexams/custom_ajax.php:37-38 sets the system context and calls require_login() only. There is no has_capability or require_capability anywhere in the file. The course `id` comes from the request (line 32) and is never checked against the caller. The listing UI does gate its grade/report links on 'local/onlineexams:grade_view' plus ':manage' (lib.php:436-439 and :576). Those capabilities are default-granted only to the manager archetype (db/access.php:94-101). But this endpoint, which renderusers.js calls (amd/src/renderusers.js:6 and :37), ...

### 47. [P0] onlineexams - 4-read (course/quiz metadata)

- **Capability:** none (require_capability commented out at :59)
- **Where:** local/onlineexams/lib.php:46-60 local_onlineexams_output_fragment_custom_onlineexams_form; also onlinexamdetails.php:18-35
- **What crosses tenants:** Pre-filled edit-form data (course settings, quiz timing and grading) and the section index for any course id, in any tenant.
- **Who can reach it:** Any authenticated user, through core_get_fragment or the details page.
- **Fix:** 1. Fragment, lib.php:54-59. Reject a missing or zero courseid. Otherwise, before get_course() reads anything: - Put the check back as require_capability('local/onlineexams:update', context_course::instance($courseid)). Also accept it in (new \local_onlineexams\lib\accesslib())::get_module_context($courseid) if that is how BizLMS grants it. - Add a tenant check. Load the course's open_path and require that it falls under the caller's costcenter path; use the local_costcenter accesslib path helper, a bounded prefix match on '/<costcenterid>/' or exact equality, and never an unbounded LIKE. Site admins are exempt. Throw required_capability_exception or moodle_exception if the check fails. - For the create path (courseid = 0), require 'local/onlineexams:create' in the module context. Either way, the file_prepare_* draft copies happen only after the check passes. 2. onlinexamdetails.php:22. ...
- **Already patched?** No.
- **Verifier:** I checked this against the production snapshot and could not refute it. Severity is low: the data is course and quiz metadata, with no personal data. Fragment (local/onlineexams/lib.php:46-162): - Reachability: core_get_fragment is declared with 'ajax' => true in lib/db/services.php:875-882. The only check in core_external::get_fragment (lib/external/externallib.php:411-412) is validate_context on the contextid the client supplies. For the system context (the plugin's own callers pass contextid:1, e.g. lib.php:713 and renderer.php:40), that check only confirms the user is logged in. - No permission check: the callback takes courseid straight from the args (:52) and loads it with ...

### 48. [P0] skillrepository - 4 (delete/update any tenant's records) plus SQL injection

- **Capability:** none (require_login only, no sesskey)
- **Where:** local/skillrepository/ajax.php:37-125: deletecategory :86-89, deleteskill :116-120 (also resets course.open_skill site-wide), deletelevel :109-114 (resets course.open_level site-wide), update :68-71, edit :63-66, insert :48-60; search :72-84 interpolates table and q; getlevelstable :91-107 calls classes/local/querylib.php:43 and :58, which interpolate sSearch
- **What crosses tenants:** Deletes or edits skills, skill categories and course levels of every tenant, and clears skill/level tags on all courses. The injectable queries allow arbitrary database reads.
- **Who can reach it:** Any authenticated user.
- **Fix:** Fixes for local/skillrepository/ajax.php and classes/local/querylib.php: 1. **Sesskey.** Call require_sesskey() right after require_login(). Update amd/src/leveltable.js, newcategory.js and newrepository.js to send M.cfg.sesskey, and send the requests as POST. 2. **Capability per action.** Require the matching capability for each action, in the context of the record's own open_path, using get_module_context(<the record's open_path>): - insert: create_skill - edit and search: view_skill - update: update_skill - deletecategory and deleteskill: delete_skill - deletelevel: delete_level - getlevelstable: view_level 3. **Tenant ownership.** Before any read, update or delete: - Fetch the target record with MUST_EXIST. - Unless the caller is a siteadmin, check that its open_path falls inside the caller's scope. Do this with a bounded path check built from get_costcenter_path_field_concatsql (or ...
- **Already patched?** No.
- **Verifier:** I re-read the code in D:/Claude Local/Moodle Backup/01-production-codebase/html/local/skillrepository and could not refute the finding. Four small corrections follow the evidence. **Evidence** - **No capability check and no sesskey.** local/skillrepository/ajax.php defines AJAX_SCRIPT and calls only require_login() (line 37). No action calls require_sesskey(), require_capability() or has_capability(). The file does not use the capabilities in db/access.php at all. So any logged-in user can reach every action: a student, an employee, or a guest if guest login is enabled. Because the requests are GET and have no sesskey, a third-party page can also make an admin's browser send them (CSRF). - ...

### 49. [P0] skillrepository - 4 (delete)

- **Capability:** none (no validate_context, no capability)
- **Where:** local/skillrepository/classes/external.php:259-263 delete_skill (ajax=true, db/services.php:53-60)
- **What crosses tenants:** Deletes any skill in any tenant.
- **Who can reach it:** Any authenticated user.
- **Fix:** Fix both delete routes. 1. In classes/external.php delete_skill: - First call `$params = self::validate_parameters(self::delete_skill_parameters(), ['id'=>$id,'contextid'=>$contextid]);`. - Load the record with `$skill = $DB->get_record('local_skill', ['id'=>$params['id']], '*', MUST_EXIST);`. - Get the context with `$context = (new \local_skillrepository\lib\accesslib())::get_module_context();`, then call `self::validate_context($context);` and `require_capability('local/skillrepository:delete_skill', $context);`. - Check the tenant: unless the caller has is_siteadmin() or a capability meant to cover every tenant (not granted to any archetype by default), require that the skill's open_path is equal to, or a descendant of, the caller's own tenant/costcenter open_path. Use the costcenter library's path-matching helper, which bounds the match at '/', not a plain prefix LIKE. Throw ...
- **Already patched?** No.
- **Verifier:** I checked this against the code in D:/Claude Local/Moodle Backup/01-production-codebase/html/local/skillrepository. 1. The function has no checks at all. classes/external.php:259-263 is only: `public static function delete_skill($id,$contextid){ global $DB; $return = $DB->delete_records('local_skill', array('id' => $id)); return $return; }` - It never calls validate_parameters, validate_context, require_capability or has_capability. - It does not look at the skill's open_path, and the local_skill table has an open_path column (db/install.xml), so skills are stored per tenant. - The contextid parameter is accepted but never used. Every sibling write function in the same file ...

### 50. [P0] skillrepository - SQL injection plus an unbounded org pattern

- **Capability:** none
- **Where:** local/skillrepository/classes/external.php:160-181 repository_selector (organisation at :176, query at :178)
- **What crosses tenants:** Skill-category names of any org. The '/<org>%' pattern also matches /177 when org is 1. query is interpolated, allowing arbitrary database reads.
- **Who can reach it:** Any authenticated user.
- **Fix:** In `local/skillrepository/classes/external.php` `repository_selector()`: (a) Add a capability gate after `validate_context()`: `require_capability('local/skillrepository:manage', $context)` (or whichever capability the skill-category form already requires). Also add a `capabilities` entry for the function in `db/services.php`. (b) Stop trusting `organisation`. Work out the caller's allowed costcenters from `$USER->open_path` through the costcenter library. Throw `required_capability_exception` / `moodle_exception` when the requested org is not one of them. Only a holder of `local/costcenter:manage_multiorganizations` (or site admin) may choose any org. If the caller's path cannot be resolved, return no results (fail closed) instead of every tenant. (c) Use a bounded, parameterised path match: `$where = '(open_path = :orgpath OR ' . $DB->sql_like('open_path', ':orgpathchild', false) . ...
- **Already patched?** No.
- **Verifier:** I checked this against the code and it holds. 1. The web service is reachable by any logged-in user. `local/skillrepository/db/services.php:37-44` registers `local_skillrepository_form_repository_selector` with `ajax => true` and `type => 'read'`. It declares no `capabilities` entry and does not override `loginrequired`, so the default applies: any logged-in session can call it through the AJAX endpoint. The shipped caller is `amd/src/form-repository-selector.js:50`. 2. No capability check. In `classes/external.php:160-182`, `repository_selector()` only validates its parameters and calls `validate_context()` on a context the caller supplies. There is no `require_capability` and no check of ...

### 51. [P0] skillrepository - 4 (overwrite another user's record)

- **Capability:** none
- **Where:** local/skillrepository/classes/external.php:591-625 submit_skills_interested_form (update :618)
- **What crosses tenants:** Overwrites any user's local_interested_skills row by id, in any tenant.
- **Who can reach it:** Any authenticated user.
- **Fix:** In local_skillrepository_external::submit_skills_interested_form (classes/external.php:591): - Do not take the row id from the client. Look up the caller's own row with $DB->get_record('local_interested_skills', ['usercreated' => $USER->id]) and update that row's id if it exists, otherwise insert. Ignore the submitted id, or throw if it is non-zero and does not match the caller's row. - If admins really need to edit another user's row, load the row by id, then require a manage capability such as local/skillrepository:update_skill at the tenant's category context, and check that the row's open_costcenterid (or the target user's open_path) sits inside the caller's scope using a boundary-anchored path check. Never overwrite open_costcenterid with the caller's tenant. - Add a 'capabilities' entry in db/services.php. - Restore the commented-out capability check at ...
- **Already patched?** No.
- **Verifier:** I checked the code in D:/Claude Local/Moodle Backup/01-production-codebase/html and the finding holds. Its real impact is low: it corrupts other users' data but exposes none. 1. The web service is open to any logged-in user. db/services.php:97-104 registers local_skillrepository_submit_skills_interested_form as type 'write', ajax => true, with no 'capabilities' key. In classes/external.php:591-626 the function runs validate_parameters, context::instance_by_id(caller-supplied contextid) and validate_context, then goes straight on. It never calls has_capability or require_capability, and never checks the user's tenant. Because contextid is chosen by the caller, the system context works for ...

### 52. [P1] block_learnerscript - 3 - fail-open: tenant placeholder silently removed

- **Capability:** n/a (placeholder scoping)
- **Where:** blocks/learnerscript/reports/sql/report.class.php:166, :185, :204 (FILTER_ORGANIZATION/DEPARTMENTS are replaced only when $USER->open_costcenterid > 0) and :292 (preg_replace strips every placeholder that was not replaced)
- **What crosses tenants:** On production users have no open_costcenterid (tenancy is in open_path), so for every non-multiorg user the tenant restriction is deleted and custom SQL reports return rows from all tenants.
- **Who can reach it:** Any user who can run a custom SQL report. That is any authenticated user, given the ignored permission check above.
- **Fix:** In blocks/learnerscript/reports/sql/report.class.php, prepare_sql(): (a) Replace every preg_match(...) followed by foreach($output[1]...) block (lines 146-222) with preg_match_all, or with a single preg_replace_callback per placeholder, so that every occurrence is actually substituted. (b) Work out the viewer's scope from $USER->open_path, never from open_costcenterid. For a non-admin without local/costcenter:manage_multiorganizations, replace each %%FILTER_ORGANIZATION:col%%, %%FILTER_DEPARTMENTS:col%% and %%FILTER_SUBDEPARTMENTS:col%% with a bounded condition from local_costcenter accesslib::get_costcenter_path_field_concatsql() (a path prefix with a trailing '/' boundary, or equality on the integer id parsed from open_path). If no scope resolves, substitute ' AND 1=0 '. (c) Make the organization and department filter plugins (components/filters/organization and departments, ...
- **Already patched?** Not in 02-enhancement-code
- **Verifier:** I confirmed this from the code. The outcome is as reported, but the cause is not quite what the sweeper said, and the real cause makes the defect worse. 1) The tenant placeholders are never substituted, in any branch. All the substitution blocks call preg_match(), not preg_match_all(), and then run foreach ($output[1] as $o). With preg_match, $output[1] is a string. That applies to the multi-org or site-admin branch at report.class.php:146-163, the own-organization branch at :165-183 (the :166 the sweeper cites), the own-departments branch at :184-202 (:185) and the fallback branch at :203-222 (:204). In PHP 8, a foreach over a string only emits the warning "foreach() argument must be of ...

### 53. [P1] block_learnerscript / block_reportdashboard - 3 - fail-open: report permission check ignored

- **Capability:** report permissions (check_permissions): the result is thrown away
- **Where:** blocks/learnerscript/classes/local/reportbase.php:532 (create_report calls $this->check_permissions() and discards the result). Entry points: blocks/learnerscript/externallib.php:378 (generate_plotgraph) and :624 (learnerscript_reportsapi); blocks/reportdashboard/ajax.php:107-111 ('dashboardtiles'). None of these call validate_context or a capability check themselves.
- **What crosses tenants:** Any report id can be run with caller-chosen filters: user lists (names/emails), course completions, grade activities, learning paths, trainer hours, custom SQL reports. It is limited only by each report's own where() scoping. It becomes cross-tenant through the multiorg and SQL-report fail-open findings below.
- **Who can reach it:** Any authenticated user, student included
- **Fix:** 1. In reportbase::create_report(), replace line 532 with an enforced gate: ```php if (!($this->cronset == 1 && $this->cronuserid > 0) && !is_siteadmin() && !$this->check_permissions($this->userid, $context)) { throw new \required_capability_exception($context, 'block/learnerscript:viewreports', 'nopermissions', ''); } ``` Also use $USER->id rather than a caller-influenceable $this->userid for interactive calls. 2. Add the same gate at the top of report_sql::create_report() in reports/sql/report.class.php:445, or have it call a shared parent helper. It overrides the parent method and never reaches the base check. 3. In externallib.php, in both generate_plotgraph and learnerscript_reportsapi, add the following, then build the report only if the check passes: - `self::validate_parameters(...)` - `$context = context_system::instance(); self::validate_context($context);` - after ...
- **Already patched?** 02-enhancement-code/new-blocks/learnerscript differs only in reportsview.php, lang and templates (UI). reportbase.php and externallib.php are identical there, so not patched.
- **Verifier:** I read every file in the chain myself. All four are byte-identical in 02-enhancement-code/new-blocks, so the sweeper was right that nothing is patched there. 1. The check result is thrown away. In blocks/learnerscript/classes/local/reportbase.php:532, create_report() runs `$this->check_permissions($this->userid, $context);` as a bare statement and discards what it returns. check_permissions() (lines 170-236) only returns a boolean. It never throws and never calls require_capability, so the report always builds. 2. The custom-SQL report type is worse than reported. reports/sql/report.class.php:445 overrides create_report() and never calls check_permissions at all. Fixing reportbase alone ...

### 54. [P1] costcenter (shared helper inherited by all four plugins) - 3 (fail-open in the parent scoping helper)

- **Capability:** n/a
- **Where:** local/costcenter/classes/lib/accesslib.php:245-285 and 315-363 (no local_userdata row gives an empty filter; an empty or NULL costcenterpath gives LIKE '/%', which matches every tenant); :74-97 and :98-180 (get_module_context falls back to context_system for users with no category role); :364-399 (unmapped path falls back to system context)
- **What crosses tenants:** Every list in these four plugins that relies on get_costcenter_path_field_concatsql() returns all tenants for a non-admin with a missing or empty local_userdata row. local_userdata is written only by local_users' own create/update UI (local/users/classes/functions/users.php:184-201), so users created by core admin, CSV upload, auth plugins or core web services have none. Users whose roles are only at system context get system context, so has_capability() passes for every tenant (class 2).
- **Who can reach it:** Any non-admin without a local_userdata row. Managers assigned at system context.
- **Fix:** 1. In userpath_match_sql() (accesslib.php:315-363), take the scope from the authoritative user.open_path column instead of, or as a fallback to, local_userdata. - Skip any path that is empty, NULL, or does not match ^/\d+(/\d+)*$ before building patterns, so '/%' can never be emitted. - Build the LIKE clauses with $DB->sql_like() and named params, not by interpolating the path into the SQL. 2. In get_user_roleswitch_costcenterpath_concatsql() (:278-284) and get_costcenter_path_field_concatsql(), fail closed: if the user is not a site admin and no path clause was built, return " AND 1=0" instead of "". 3. In costcenterpath_contextdata() (:364-399), return null for an unmapped path and make callers fail closed. Do this with require_capability on the resolved category, or by throwing nopermission. Do not fall back to context_system for non-admins. Do not deploy the 02-enhancement-code ...
- **Already patched?** No. The 02-enhancement-code bug-fixes/local_costcenter_renderer.php.patch adds another context_system fallback in costcenter/renderer.php, which widens rather than fixes this.
- **Verifier:** I re-read D:/Claude Local/Moodle Backup/01-production-codebase/html/local/costcenter/classes/lib/accesslib.php. The main fail-open is real and reachable. Two of the sweeper's sub-claims are wrong or overstated (see Corrections). 1. Empty filter when a user has no local_userdata row (class 3). For a non-admin with no path passed in, get_costcenter_path_field_concatsql() (:46-53) calls get_user_roleswitch_costcenterpath_concatsql() (:245-285). That function falls back to userpath_match_sql() in two cases: when $USER->useraccess['currentroleinfo']['contextinfo'] is empty (:273), and when a context entry has an empty or 0 costcenterpath (:265-267). userpath_match_sql() (:323) reads only ...

### 55. [P1] evaluation - 3 (inverted check, so it fails open)

- **Capability:** local/costcenter:manage_multiorganizations (default manager archetype, local/costcenter/db/access.php:151-158)
- **Where:** local/evaluation/lib.php:289-296 dep_sql(). Reached from renderer.php:352-356 (tagged_evaluations, for edititems holders) and lib.php:862-868 (evaluation_get_template_list('all'))
- **What crosses tenants:** The costcenter filter is appended ONLY when the caller holds manage_multiorganizations. Everyone else gets an undefined (empty) $sql, so no filter: evaluation names and details and template names from all tenants. The template list cases ''/'own'/'public' at :853-861 are also unscoped.
- **Who can reach it:** Org admins and trainers holding local/evaluation:edititems without manage_multiorganizations.
- **Fix:** 1. In lib.php, rewrite dep_sql() so it always returns a string. Return the tenant filter for everyone except site admins. Do not tie it to manage_multiorganizations: function dep_sql($context) { if (is_siteadmin()) { return ''; } $sql = (new \local_evaluation\lib\accesslib())::get_costcenter_path_field_concatsql('open_path'); return $sql !== '' ? $sql : ' AND 1 = 0 '; // fail closed if no path resolves } If multi-org scope must stay wider, make that explicit. Build the filter from the org paths the holder is actually assigned to, never from "no filter". 2. In renderer.php:352-356, qualify the column as c.open_path. The alias matters because local_rating is joined in the same query. Also bind $USER->id as a parameter; right now it is interpolated into the SQL string. 3. In evaluation_get_template_list(), add the same open_path filter to all four cases. Either delete the unused ...
- **Already patched?** No.
- **Verifier:** I re-read the code in D:/Claude Local/Moodle Backup/01-production-codebase/html/local/evaluation and the finding holds, with a few corrections to its scope. 1. The check in dep_sql() is inverted (local/evaluation/lib.php:289-296). The function builds $costcenterpathconcatsql, the open_path "AND (...)" filter from local_costcenter accesslib get_costcenter_path_field_concatsql() / get_user_roleswitch_costcenterpath_concatsql(). It assigns that filter to $sql only inside `if (has_capability('local/costcenter:manage_multiorganizations', $context))`. There is no else branch, so a caller without that capability gets an undefined $sql. On PHP 8 that is a warning, not a fatal error, and null is ...

### 56. [P1] evaluation - 4 (delete or modify another tenant's evaluation)

- **Capability:** local/evaluation:edititems in the caller's OWN context (get_module_context() with no id)
- **Where:** local/evaluation/index.php:61, 148-160 (soft-delete :151, no sesskey); use_templ.php:40-55; import.php:49-154 (delete_all_items :154); ajax.php:43-53 (reorder); export.php:44-48; delete_template.php:38-56
- **What crosses tenants:** Soft-deletes any evaluation id (CSRF-able, because there is no sesskey on the delete branch), replaces or wipes its questions from a template or import, reorders items, exports questions, and deletes non-public templates. None of these check that the target evaluation or template is in the caller's tenant.
- **Who can reach it:** Org admins or trainers holding edititems or deletetemplate in their own tenant.
- **Fix:** 1. In index.php, use_templ.php, import.php, ajax.php, export.php and delete_template.php, load the evaluation first with $DB->get_record('local_evaluations', ['id'=>$id, 'deleted'=>0], '*', MUST_EXIST). Then set $context = (new \local_evaluation\lib\accesslib())::get_module_context($evaluation->id) and call require_capability() against that context. edit.php and eval_view.php already do this. 2. In index.php, move the delete branch into its own path that: - uses the per-evaluation context from step 1; - requires addinstance, or better the existing local/evaluation:delete capability, in that context; - calls require_sesskey(); - shows a confirmation step before deleting. Also move the delete before $OUTPUT->header(). 3. Add a shared helper, e.g. local_evaluation_require_in_scope($openpath), that rejects any record whose open_path is not the caller's path or below it. Build it with ...
- **Already patched?** No.
- **Verifier:** I checked the code and the finding holds. Two corrections. First, nothing in db/access.php grants these capabilities by default; the grants come from the site's role definitions. Second, trainers cannot reach the index.php delete branch. How the context is resolved: local_evaluation\lib\accesslib::get_module_context($evaluationid=null) calls evaluation_costcenterpath(). With no id, that returns null. local_costcenter\lib\accesslib::get_module_context(null) (costcenter/classes/lib/accesslib.php:74-97) then returns the CALLER's own role-switch context: the tenant category where their first role is assigned, or system context for a site admin. So every check below tests the capability in the ...

### 57. [P1] evaluation - 4 (cross-tenant enrolment and notification)

- **Capability:** local/evaluation:enroll_users in the caller's OWN context (:46-51)
- **Where:** local/evaluation/users_assign.php:31-51, 124-175 (insert :151); the ajax branch :67-76 calls lib.php:2313-2437 evaluation_enrolled_users (raw IN(...) at :2332-2338)
- **What crosses tenants:** Enrols arbitrary user ids from any tenant into any evaluation id and triggers enrolment emails to them. The user picker is scoped only by the viewer's own path, and request options are interpolated into SQL.
- **Who can reach it:** Org admins holding enroll_users.
- **Fix:** **Changes in users_assign.php:** - After loading the evaluation with MUST_EXIST, build the context from the evaluation: `$context = (new \local_evaluation\lib\accesslib())::get_module_context($evaluationid);` then call `require_capability('local/evaluation:enroll_users', $context);`. Do this before the ajax branch. - Call require_sesskey() on the ajax branch. - Optionally reject evaluations with instance != 0, matching the renderer. - Replace PARAM_RAW on add[] and remove[] with `optional_param_array('add', [], PARAM_INT)` and the same for remove. - Before any insert or delete, resolve the evaluation's tenant path: `'/'.explode('/', $evaluation->open_path)[1]`. - Re-fetch the submitted ids with `$DB->get_in_or_equal($ids, SQL_PARAMS_NAMED)`, filtered by `deleted = 0 AND (u.open_path = :tp OR u.open_path LIKE :tpchild)` where tpchild is the tenant path plus '/%'. Also require the path to ...
- **Already patched?** No.
- **Verifier:** I re-read the code in the snapshot (D:/Claude Local/Moodle Backup/01-production-codebase/html/local/evaluation) and could not refute the finding. A few of the sweeper's line numbers are off; the correct ones are below. 1. **No default grant, but the check uses the wrong context.** - db/access.php:97-100 declares local/evaluation:enroll_users at CONTEXT_COURSECAT with no archetypes. It is not default-granted, so this is class 4, not class 1. It is reachable by whoever holds the capability through a BizLMS org-admin or department-admin role at their tenant category. - users_assign.php:47 loads the evaluation by id with no check on its tenant. - users_assign.php:48 builds the context with ...

### 58. [P1] evaluation - 2/3 (mitigated by a secondary path filter; fails open)

- **Capability:** local/evaluation:viewreports in the caller's OWN context
- **Where:** local/evaluation/classes/external.php:1448-1500 get_non_respondents, :1556-1580 get_responses_analysis; mitigating filters at lib.php:560 and classes/responses_table.php:346
- **What crosses tenants:** Non-respondent names and respondent names plus answers for any evaluation id. Secondary filters on the viewer's path limit this to the viewer's tenant, but they add nothing when the viewer's path does not resolve.
- **Who can reach it:** Report viewers with viewreports and no resolved path, or with a system-context role.
- **Fix:** 1. In local/evaluation/classes/external.php, get_non_respondents and get_responses_analysis: call validate_parameters first, then load the evaluation. Then set `$context = \local_evaluation\lib\accesslib::get_module_context($evaluation->id)`, call `self::validate_context($context)`, and call require_capability('local/evaluation:viewreports', $context). For responses from other users, also require edititems at that same context. Add an explicit tenant check that the evaluation's open_path is at or under one of the caller's resolved paths, and throw otherwise. Make validate_evaluation reject deleted evaluations and ones outside the caller's tenant. Apply the same change to the other get_module_context() calls with no argument in external.php (lines 282, 351, 403, 469, 650, 923, 991, 1081, 1193, 1705) wherever an evaluation id is available. 2. In local/costcenter/classes/lib/accesslib.php, ...
- **Already patched?** No.
- **Verifier:** I checked the code in D:/Claude Local/Moodle Backup/01-production-codebase/html. The finding holds, but its "who can reach it" part needs three corrections, listed further down. 1. The capability is checked in the caller's own context, not the evaluation's. In local/evaluation/classes/external.php, get_non_respondents (line 1448) gets its context at line 1452 and get_responses_analysis (line 1556) at line 1557. Both calls use `accesslib::get_module_context()` with no evaluation id. For a non-siteadmin that goes to local_costcenter\lib\accesslib::get_user_roleswitch_context(). In a token web-service call $USER->useraccess and access['rsw'] are empty, so it returns the context of the caller's ...

### 59. [P1] local_biz_cart - 2/3 - cross-tenant user directory with no capability check

- **Capability:** none: search_users has no capability check
- **Where:** local/biz_cart/classes/external/search_users.php:61-73 (only validate_context(system)); it calls biz_cart::load_users() at classes/biz_cart.php:1651-1700, which searches all non-deleted users with no open_path filter. WS local_biz_cart_search_users (services.php:118-124, ajax).
- **What crosses tenants:** Firstname, lastname and email of users in every tenant, up to 100 per query.
- **Who can reach it:** Any authenticated user
- **Fix:** 1. In classes/external/search_users.php, right after self::validate_context($context), add require_capability('local/biz_cart:cashier', $context). Also set 'capabilities' => 'local/biz_cart:cashier' in the db/services.php entry, but only as documentation, since it is not enforced. 2. Scope the query in biz_cart::load_users(). Unless the caller has site-admin or a deliberately restricted cross-tenant capability, add the caller's tenant path condition to the inner query on u.open_path, using \local_costcenter\lib\accesslib::get_costcenter_path_field_concatsql('u.open_path') (local/costcenter/classes/lib/accesslib.php:35). Pass its SQL and params through with named parameters. Make it fail closed: if the caller's open_path does not resolve to a known costcenter, return an empty list instead of skipping the filter. The helper's own empty-path behaviour should be checked so that it does not ...
- **Already patched?** No biz_cart copy in 02-enhancement-code
- **Verifier:** I checked this against the snapshot at D:/Claude Local/Moodle Backup/01-production-codebase/html/local/biz_cart and could not refute it. 1. No capability check. In classes/external/search_users.php:61-75, execute() only calls validate_parameters() and validate_context(context_system::instance()), then returns biz_cart::load_users($params['query']). There is no has_capability or require_capability call anywhere on this path. 2. The service entry does not check permissions either. db/services.php:118-124 registers local_biz_cart_search_users with 'capabilities' => '' and 'ajax' => 1. The 'capabilities' key in services.php is informational only, so even a filled-in value would not be enforced. ...

### 60. [P1] local_biz_cart - 3 - WS without capability check, plus fail-open scoping

- **Capability:** none in the WS (the page view_transactions_for_admin.php:34-37 checks local/courses:view, the WS does not)
- **Where:** local/biz_cart/classes/external.php:148-193 (transactions_view_for_admin), :244-292 (view_course_transaction_log), :342-387 (view_course_standard_log): only require_login. Scoping is in lib.php:249-315, :354-406 and :408-479.
- **What crosses tenants:** Payment records: buyer names, Airpay transaction codes, order IDs, amounts and gateway error messages for courses in the caller's tenant path, visible to learners. For a caller whose path does not resolve, the scoping SQL is empty and every tenant's transactions are returned.
- **Who can reach it:** Any authenticated user
- **Fix:** 1. **Gate each WS.** In each of the three methods in local/biz_cart/classes/external.php (and transactions_view if it is ever widened), after validate_parameters(): - resolve `$context = (new \local_courses\lib\accesslib())::get_module_context();` - call `self::validate_context($context);` - call `require_capability('local/courses:manage', $context);`, or better, a new `local/biz_cart:viewreports` capability (CONTEXT_COURSECAT, manager archetype only, RISK_PERSONAL). - Add the same capability to each function's 'capabilities' key in db/services.php. - Replace `$PAGE->set_context($contextid)` with the validated context object. 2. **Fail closed in the scoping helpers.** In local/biz_cart/lib.php get_user_transactions_for_admin, get_course_transaction_log and get_course_standard_log: if `!is_siteadmin()` and the returned scoping SQL is empty, return `['count' => 0, 'data' => []]` instead ...
- **Already patched?** No biz_cart copy in 02-enhancement-code
- **Verifier:** I read the code myself, and every part of the sweeper's claim holds. 1. Any logged-in user can call these web services, with no capability check. In local/biz_cart/db/services.php:147-171, three functions have `ajax => true` and no 'capabilities' key: local_biz_cart_transactions_view_for_admin, local_biz_cart_view_course_transaction_log and local_biz_cart_view_course_standard_log. In classes/external.php the matching methods are transactions_view_for_admin (148-193), view_course_transaction_log (244-292) and view_course_standard_log (342-387). Each one calls only require_login() and validate_parameters(). None calls validate_context(), require_capability() or has_capability(). ...

### 61. [P1] local_biz_cart - 4 - reads and writes of another user's purchase records

- **Capability:** local/biz_cart:canbuy / :history (default for user and student)
- **Where:** local/biz_cart/classes/external/get_history_items.php:84-88 (capability logic inverted: non-cashiers may pass any userid, cashiers get only their own); local/biz_cart/receipt.php:31-79 (any ledger identifier, no ownership check); local/biz_cart/classes/external/delete_item_from_cart.php:112-114 plus :123-125 (a positive userid is accepted and toggle_mark_for_rebooking runs on another user's history item)
- **What crosses tenants:** Any user's purchase history (item names, prices, payment status) and a receipt PDF with the buyer's username, name, email, institution, department and address, across tenants. Rebooking flags on other users' purchases can also be changed.
- **Who can reach it:** Any authenticated user
- **Fix:** 1) get_history_items.php:84-88. Swap the branches and add scope checks: - Set $userid = (int)$USER->id by default. - If $params['userid'] is nonzero and differs from $USER->id, require has_capability('local/biz_cart:cashier', $context). - Then confirm the target is inside the cashier's tenant. Load the target's open_path and compare it with the caller's using a bounded path-prefix match (tenant root equality, not a raw LIKE). Skip that check only for is_siteadmin(). - Also add require_capability('local/biz_cart:history', $context). 2) receipt.php: - After loading the ledger rows (line 73), throw if they are empty. - Take $owner = the rows' userid and ignore the userid request parameter. - Unless $owner == $USER->id, require_capability('local/biz_cart:cashier', context_system::instance()) plus the same tenant-root check between the cashier and $owner. - Use MUST_EXIST on the user fetch. ...
- **Already patched?** No biz_cart copy in 02-enhancement-code
- **Verifier:** I tried to refute each of the three sub-claims in D:/Claude Local/Moodle Backup/01-production-codebase/html/local/biz_cart. None of them could be refuted. Is the plugin live? Yes. The prod-derived dump pre-moodle5-backup-20260417.sql has local_biz_cart at version 2024041000.06 with accountid=1. That setting matters: get_history_list_for_user() exits early when no payment account is set, and here one is set, so that exit does not fire. mdl_local_biz_cart_history holds real purchase rows (user ids, course names, INR prices). There is no biz_cart copy anywhere under 02-enhancement-code, so there is no later patch. Capabilities (db/access.php:51-72): :history and :canbuy are default-granted to ...

### 62. [P1] local_biz_cart - 1 + 4 - archetype-granted cashier capability acts on users of every tenant

- **Capability:** local/biz_cart:cashier and local/biz_cart:cashtransfer (CONTEXT_SYSTEM, default CAP_ALLOW for manager, db/access.php:27-42)
- **Where:** local/biz_cart/cashier.php:36-45 (buy_for_user(any userid)); classes/biz_cart.php:348-366 (set_user), :851-900 (confirm_payment for other users), :1126-1200 (cancel_purchase with a custom credit), :1274 (credit_paid_back); classes/form/modal_creditsmanager.php:61/:117, modal_cashtransfer.php:92, dynamic_select_users.php:67
- **What crosses tenants:** The cashier can search every user site-wide, mark carts paid in cash, add or remove credits, cancel purchases with arbitrary refunds, and record cash transfers for users of Airpay, Public and ZEEA.
- **Who can reach it:** Any holder of a manager-archetype role at system context (e.g. a tenant admin given a system-level manager role)
- **Fix:** 1. db/access.php:33-35 and :41-43: remove 'manager' => CAP_ALLOW from local/biz_cart:cashier and local/biz_cart:cashtransfer, leaving archetypes empty. Add a db/upgrade.php step that unassigns these caps from the manager archetype role on existing sites. Grant them explicitly only to a dedicated cashier role. 2. Add one tenant-scope helper, for example biz_cart::require_target_in_scope(int $targetuserid). It should: - return immediately for is_siteadmin() only; - otherwise load the target's open_path and compare it with the acting user's costcenter path using the bounded path-descendant match (path = :p OR path LIKE :p || '/%'); - fail closed with an exception when either path is empty or unresolved. Call it in: - cashier.php before buy_for_user($userid); - set_user() before returning a cached buy-for id; - confirm_payment() when $userid != $USER->id; - cancel_purchase() when $USER->id ...
- **Already patched?** No biz_cart copy in 02-enhancement-code
- **Verifier:** I checked every claim against the snapshot at D:/Claude Local/Moodle Backup/01-production-codebase/html/local/biz_cart. The code defect is real. Two things need correcting or qualifying: the cash-transfer claim, and who can actually reach this. What the code confirms: - Default grant (class 1). db/access.php:29-36 defines local/biz_cart:cashier at CONTEXT_SYSTEM with 'manager' => CAP_ALLOW. db/access.php:37-44 does the same for local/biz_cart:cashtransfer. Both carry RISK_PERSONAL. - No tenant scoping anywhere (class 1 + 4). A grep of the whole plugin finds no open_path, local_costcenter or costcenter-path filter in any cashier path. The only 'costcenter' logic is a per-item ...

### 63. [P1] local_biz_cart (with local_courses cart provider and paygw_airpay) - 4 - enrolment after purchase without checking the course's tenant

- **Capability:** local/biz_cart:canbuy (user and student archetypes)
- **Where:** local/biz_cart/classes/biz_cart.php:84-203 (allow_add_item_to_cart / add_item_to_cart) calls local/courses/classes/biz_cart/service_provider.php:54-80 and :311-335, which load any course id without an open_path check. After payment, payment/gateway/airpay/process.php:119 and :168 enrol the buyer.
- **What crosses tenants:** A user of one tenant can buy a priced course belonging to another tenant and be enrolled in it, which brings access to that course's content and participant list.
- **Who can reach it:** Any authenticated user
- **Fix:** Main fix (class 4): - In `local/courses/classes/biz_cart/service_provider.php`, both `allow_add_item_to_cart` and `load_cartitem` should: - load the course with MUST_EXIST; - reject it unless it is visible, has `price_status=1` and has an enabled 'fee' instance; - reject it unless its open_path is within the buyer's tenant. Get the buyer's first open_path segment through the costcenter accesslib helper, fail closed if it doesn't resolve, and use a bounded path check (the tenant::path_descendant_filter style), not a raw prefix match. - allow an exception only for courses explicitly flagged as cross-tenant marketplace items. - On rejection, return `['allow' => false, 'info' => 'notallowed']`, so that biz_cart.php no longer overrides it. Remove the unconditional `$cartitem['allow'] = true` at :124. - Return the course's real costcenter id instead of the undefined `$item['costcenter']` / ...
- **Already patched?** Unknown for paygw_airpay on live; not in 02-enhancement-code
- **Verifier:** I checked every step of the path in the 01-production-codebase snapshot. None of the steps checks the tenant (costcenter or open_path) of the course or the buyer. 1. **The capability is default-granted.** local/biz_cart/db/access.php:64-75 grants `local/biz_cart:canbuy` to the manager, editingteacher, teacher, user and student archetypes. Every logged-in user holds the 'user' (Authenticated user) role at system context, so anyone has it unless an admin removed it on live. 2. **The web service can be called by anyone logged in.** db/services.php:48-54 declares `local_biz_cart_add_item` with ajax=1 and no required capability. external/add_item_to_cart.php:89-98 only checks require_login and ...

### 64. [P1] local_classroom - 3-fail-open

- **Capability:** local/classroom:manageclassroom (inverted gate)
- **Where:** local/classroom/externallib.php:530 classroomviewusers (no capability) -> local/classroom/classes/classroom.php:2161-2200 classroomusers. The tenant filter at :2188 is applied only when the caller HAS manageclassroom. Search is concatenated at :2169-2178.
- **What crosses tenants:** Full roster of any classroom (u.* including email, attendance and completion status) across tenants.
- **Who can reach it:** Any logged-in user without the manage capability, for example a learner.
- **Fix:** Changes needed in local/classroom/externallib.php (`classroomviewusers`): - Load the classroom with `$DB->get_record('local_classroom', ['id' => (int)$classroomid], '*', MUST_EXIST)`. - Set `$context = (new \local_classroom\lib\accesslib())::get_module_context($classroom->id)`, then call `self::validate_context($context)`. - Call `require_capability('local/classroom:viewusers', $context)`, or `manageclassroom`. Also allow the classroom's assigned trainer, checked against the trainer mapping for this classroom rather than just the capability. - Only accept `tabname === 'users'`. Changes needed in local/classroom/classes/classroom.php (`classroomusers`): - Remove the inverted gate. For every caller who is not a site admin, always add `get_costcenter_path_field_concatsql('u.open_path')`, and first check that the classroom's own `open_path` lies inside the caller's scope. - Keep the ...
- **Already patched?** No
- **Verifier:** I read the code myself and the main claim holds. One part of the report is wrong: the search concatenation cannot be reached from this entry point. 1. Entry point (local/classroom/db/services.php:147-154). `local_classroom_classroomviewusers` is `ajax => true` and `type => read`, with no `loginrequired` override, so it defaults to true. lib/ajax/service.php:226-233 therefore requires only `isloggedin()` and a sesskey. 2. No authorisation in the web service (local/classroom/externallib.php:530-580). Line 538 validates the hard-coded system context, `context::instance_by_id(1)`, which only amounts to `require_login`. The caller-supplied `contextid` is ignored. `classroomid` is taken from the ...

### 65. [P1] local_classroom - 2-own-context-check-then-any-target

- **Capability:** local/classroom:manageclassroom, manageusers (no archetype; assigned to org-admin roles)
- **Where:** local/classroom/enrollusers.php:49,54-55 (capability at the caller's own context), :35 (any cid), :139-143 and :162-165 (raw add[]/remove[] ids) -> classes/classroom.php:1242 classroom_add_assignusers (no target-user tenant check)
- **What crosses tenants:** Adds or removes users to or from another tenant's classroom and course enrolments, and sends emails.
- **Who can reach it:** Org admins of any tenant.
- **Fix:** 1. Check capabilities in the classroom's own context. In enrollusers.php, compute `$ctx = \local_classroom\lib\accesslib::get_module_context($classroomid)` and run require_capability for manageclassroom and manageusers against $ctx. Then confirm that the classroom's open_path starts with one of the caller's own paths, bounded as `path === p || str_starts_with(path, p . '/')`. 2. Stop trusting the orgid request parameter in classrooms() (classroom.php:867 and 895-897). Accept orgid only for site admins or holders of local/costcenter:manage_multiorganizations. For everyone else, ignore it or require it to equal the caller's own tenant. 3. Clean the posted ids with `optional_param_array('add', [], PARAM_INT)` and `optional_param_array('remove', [], PARAM_INT)`. - Keep only add ids that also appear in select_to_and_from_users('add', $classroomid, ...). That query is already tenant-filtered. ...
- **Already patched?** No
- **Verifier:** I re-read the code in D:/Claude Local/Moodle Backup/01-production-codebase/html/local/classroom. The finding holds, with one correction to how the other tenant's classroom is reached. 1. The capability is checked in the caller's own context. local/classroom/enrollusers.php:49 calls get_module_context() with no classroom id. In local/costcenter/classes/lib/accesslib.php:74-97 and 98-180, that call returns the caller's own category context (from role switch or their category role assignments). Lines 54-55 then run require_capability for manageclassroom and manageusers in that context. db/access.php:41-45 and 82-86 give neither capability an archetype, so there is no default grant. Holders are ...

### 66. [P1] local_costcenter accesslib (helper used by every plugin in this partition) - 3-fail-open

- **Capability:** n/a (scoping helper)
- **Where:** local/costcenter/classes/lib/accesslib.php:245-285 get_user_roleswitch_costcenterpath_concatsql and :315-363 userpath_match_sql. local_userdata is only written by local/users/classes/functions/users.php:184.
- **What crosses tenants:** All list queries in courses, classroom, program, request and learningplan return every tenant. Two triggers: the non-admin has no local_userdata row (users created by core tools, auth plugins or imports), in which case the filter is ''. Or the row's costcenterpath is NULL or empty, which produces the pattern '/%' and matches every tenant. A system-context role switch (theme core_renderer.php:1770, costcenterpath 0) also falls through to this path.
- **Who can reach it:** Any non-admin without a usable local_userdata path, including system-context managers.
- **Fix:** In `local/costcenter/classes/lib/accesslib.php`, make the helper fail closed: - `userpath_match_sql`: if the user is not a site admin and no valid path resolves, return `' 1=0 '`. Skip any row whose `costcenterpath` is empty or NULL, or does not match `^/\d+(/\d+)*$`. - `get_user_roleswitch_costcenterpath_concatsql`: if `array_filter($sqlarray)` is empty for a non-admin, return `'AND 1=0'`, not `''`. - When there is no `local_userdata` row, derive the path from `$USER->open_path` (validated the same way). Use bounded matching (`col = :p OR col LIKE :p/%`) with bound parameters instead of interpolating strings. - Treat `costcenterpath` 0 from `get_costcenterpath_context` (system or unmapped category) as the user's own `open_path`, not as "no filter". Only site admins and holders of `manage_multiorganizations` should ever get an empty filter, checked explicitly. Also backfill ...
- **Already patched?** No: not in 02-enhancement-code.
- **Verifier:** I re-read the code. The fail-open works as the sweeper described. The sweeper's list of who is affected goes too far, though. **What the code does.** Everything below is in `local/costcenter/classes/lib/accesslib.php`. - The main helper `get_costcenter_path_field_concatsql` (lines 35-72) is called with no path for a non-admin. It then calls `get_user_roleswitch_costcenterpath_concatsql` (lines 245-283). - That function calls `userpath_match_sql` (lines 315-363) in two cases: - `$USER->useraccess['currentroleinfo']['contextinfo']` is empty (line 272). - A contextinfo entry has an empty `costcenterpath`, including the integer 0 (lines 261-265). - `userpath_match_sql` reads only ...

### 67. [P1] local_costcenter accesslib (root cause behind path scoping in biz_cart, forum, learnerscript and trainerdashboard) - 3 - fail-open when the caller's path does not resolve

- **Capability:** n/a (scoping helper)
- **Where:** local/costcenter/classes/lib/accesslib.php:238-280 (get_user_roleswitch_costcenterpath_concatsql: an empty costcenterpath falls back to userpath_match_sql, and an empty result leaves $concatsql = '') and :324-370 (userpath_match_sql returns '' when the user has no local_userdata rows). get_module_context() at :69-89/:94-180 falls back to context_system.
- **What crosses tenants:** Every consumer that appends get_costcenter_path_field_concatsql() gets no tenant filter for a non-admin whose role-switch path is empty (e.g. after switching to a system-level role, where get_costcenterpath_context() returns 0) or who has no local_userdata row. Affected here: biz_cart lib.php:251/346/356/410, forum lib.php get_listof_forum, forumenrol ajax, learnerscript reports/filters, trainerdashboard manager.
- **Who can reach it:** Any non-admin user without a resolvable local_userdata path, e.g. a newly created user or an imported account missing that row
- **Fix:** 1. Make the helper fail closed (local/costcenter/classes/lib/accesslib.php). - In get_user_roleswitch_costcenterpath_concatsql (:276-279): if the user is not a site admin and array_filter($sqlarray) is empty, return " AND 1=0 " instead of ''. - In userpath_match_sql (:316-362): skip empty or '/' costcenterpath values. For a non-admin with no usable path, fall back to the user's own user.open_path. If that is also empty, return '1=0'. - In both userpath_match_sql and costcenterpath_match_sql, build the LIKE clauses with $DB->sql_like() and named bound parameters instead of interpolating $path. Match subtrees with the boundary pattern path . '/%' plus exact equality. 2. Keep local_userdata populated. Either write it in a \core\event\user_created / user_updated observer, or stop using it and read user.open_path directly. - Backfill the existing users that have no row. - Remove the ...
- **Already patched?** Outside 02-enhancement-code (only renderer/settings patches exist for local_costcenter), so unverified on live
- **Verifier:** I re-read the code in the 01-production-codebase snapshot. The fail-open is real and can be reached. One detail in the sweeper's trigger needs correcting. 1. The helper fails open (local/costcenter/classes/lib/accesslib.php). - In get_costcenter_path_field_concatsql (:34-72), a non-admin with a null path goes to get_user_roleswitch_costcenterpath_concatsql (:245-282). - If $USER->useraccess['currentroleinfo']['contextinfo'] is empty, or an entry has an empty costcenterpath, that function calls userpath_match_sql (:316-362). - userpath_match_sql loads the user's paths only from local_userdata (:323). With no rows, $paths stays empty and it returns ''. - The caller then sees ...

### 68. [P1] local_courses - 2-own-context-check-then-any-target

- **Capability:** local/courses:view or local/courses:manage (manager archetype)
- **Where:** local/courses/courses.php:53-54 (capability checked at the caller's own role-switch context), :73-75 delete_course($deleteid)
- **What crosses tenants:** Deletes any tenant's course.
- **Who can reach it:** An org admin with a manager-archetype role in any tenant.
- **Fix:** In local/courses/courses.php:73, and the same way in local/forum/index.php:73 and local/onlineexams/index.php:75, replace the delete branch with: ```php if ($deleteid && $confirm && confirm_sesskey()) { $course = $DB->get_record('course', ['id' => $deleteid], '*', MUST_EXIST); if ($course->id == SITEID) { throw new moodle_exception('invalidcourse'); } $coursecontext = context_course::instance($course->id); require_capability('moodle/course:delete', $coursecontext); if (!is_siteadmin()) { require_capability('local/courses:delete', (new \local_courses\lib\accesslib())::get_module_context($course->id)); // Tenant boundary: the course's open_path must sit inside the caller's own path. $callerpath = $USER->open_path; if (empty($callerpath) || strpos($course->open_path . '/', rtrim($callerpath, '/') . '/') !== 0) { throw new required_capability_exception($coursecontext, ...
- **Already patched?** No
- **Verifier:** I read the code myself and the finding holds. It is actually easier to exploit than the sweeper said. 1. Default grant. local/courses/db/access.php:54-61 (local/courses:view) and :94-101 (local/courses:manage) are both default-granted to the manager archetype at CONTEXT_COURSECAT. 2. The check is against the caller's own context. At local/courses/courses.php:53, get_module_context() is called with no courseid. It goes to local_courses\lib\accesslib::get_module_context(null), then to local_costcenter\lib\accesslib::get_module_context(null) (costcenter/classes/lib/accesslib.php:74-97), then to get_user_roleswitch_context(). That returns the caller's current role-switch category context. If ...

### 69. [P1] local_courses - 2/4-own-context-check + target mismatch

- **Capability:** local/courses:enrol, unenrol, manage (manager archetype)
- **Where:** local/courses/courseenrol.php:55,77-79 (capabilities at the caller's own context), :86 uses local/includes.php access_courses_permission (always true, see separate hit), :43-47 vs :86 (access checked on the 'id' param while enrolment goes into the course of the 'enrolid' param), :183/:207 (added user ids never validated), :59-69 (ajax listing runs before any capability check), :192/:249 (assign_capability writes a role override at course context)
- **What crosses tenants:** Cross-tenant enrolment and unenrolment of arbitrary users, with notification emails sent. Any logged-in user can read user lists (name and idnumber). course_enrolled_users (local/courses/lib.php:1094-1199) concatenates request-supplied option values into SQL.
- **Who can reach it:** Org admins with a manager archetype for writes. Any logged-in user for the ajax branch.
- **Fix:** 1. In courseenrol.php, take the course only from $instance->courseid and ignore or reject `id`. Replace get_module_context() with (new \local_courses\lib\accesslib())::get_module_context($instance->courseid), or with context_course::instance($instance->courseid). Then require local/courses:enrol, :unenrol, :manage and enrol/manual:enrol or :unenrol at that context. Also require that the course's open_path falls under the caller's own costcenter path, using a bounded match ('/X' or '/X/%'). 2. Rewrite access_courses_permission in local/includes.php:235-264 to compare the course open_path with the caller's roleswitch path and return false by default (fail closed). Do not return true unconditionally. 3. Clean add[] and remove[] with clean_param_array(..., PARAM_INT). Check every target user id: not deleted, and open_path inside both the caller's scope and the course's tenant. Reject the ...
- **Already patched?** No
- **Verifier:** I re-read the code in the snapshot (D:/Claude Local/Moodle Backup/01-production-codebase/html) and every part of the claim holds. 1. The capabilities are granted by default. local/courses/db/access.php:30-45 grants local/courses:enrol and :unenrol, and :94-101 grants :manage, all as CAP_ALLOW to the manager archetype. The course list (local/courses/lib.php:1768-1769) shows the enrol link to anyone holding :enrol and :manage in their own context, so org admins are the intended users of this page. 2. The capabilities are checked in the caller's own context, not the target course's. courseenrol.php:55 calls get_module_context() with no course id. That goes to ...

### 70. [P1] local_forum - 4 + 5 - cross-tenant enrol/unenrol, arbitrary role on enrol, runtime role override

- **Capability:** local/forum:enrol + local/forum:unenrol + local/forum:manage (manager archetype by default, db/access.php)
- **Where:** local/forum/forumenrol.php:41-45, 75-87, 175-231, 232-293. The tenant gate has_user_permission::access_courses_permission() (local/includes.php:235-264) always returns true, and it is applied to the 'id' param rather than $instance->courseid. roleid comes from the request (:43); the assignable-roles check is commented out (:91-96). assign_capability/unassign_capability at :190/:220/:246/:283. Raw SQL with $removeuser at :261-263.
- **What crosses tenants:** An org admin can pass the enrolid of another tenant's course and enrol or unenrol any user ids (the add[]/remove[] values are not scoped), including users of other tenants. They can pick any roleid (e.g. manager or editingteacher), which grants elevated rights in that course. The page also temporarily edits the org admin's role definition at course context; if the loop throws, the override is left in place.
- **Who can reach it:** Tenant/org admins holding a manager-archetype role at their category context
- **Fix:** In local/forum/forumenrol.php: 1. After :45, set $course_id = $instance->courseid, or reject the request if the 'id' param differs from it. Use that one value everywhere: the gate, the lists, the SQL at :261 and the notifications. 2. Replace the gate at :82-87 with a real scope check. Use require_capability('local/forum:enrol', local_forum\lib\accesslib::get_module_context($instance->courseid)), which resolves the course's own category context. Also assert that the course's open_path equals, or is a '/'-bounded descendant of, the caller's role-switch costcenter path (use a bounded check, not a bare LIKE prefix). At the same time, fix local/includes.php:235-264 so it actually compares course.open_path with the caller's path, since other pages use it too. 3. Clean add[] and remove[] with optional_param_array(..., PARAM_INT). Intersect them with the keys of ...
- **Already patched?** Not in 02-enhancement-code
- **Verifier:** All paths are under D:/Claude Local/Moodle Backup/01-production-codebase/html. 02-enhancement-code has no copy of local/forum or local/includes.php, and all-modifications.patch has 0 hits for local/forum/, so no patch covers this. Verified: 1. Default grant (class 1/2): local/forum/db/access.php:30-37, 38-45 and 86-93 grant local/forum:enrol, :unenrol and :manage to the manager archetype at CONTEXT_COURSECAT. Which role production org admins actually hold is stored in the DB, not in code. So the reach depends on the org-admin role having the manager archetype, as the brief states. 2. The capability check is on the caller's own context, not the target's. forumenrol.php:53 calls local_forum ...

### 71. [P1] local_forum - 4 - delete with no target tenant or type check

- **Capability:** local/forum:view OR local/forum:manage (manager archetype)
- **Where:** local/forum/index.php:54-57 (gate) and :73-88 (delete_course on any ?delete=<id> with sesskey)
- **What crosses tenants:** Deletes any course (not only forums) in any tenant. local_courses delete_coursedetails also runs.
- **Who can reach it:** Org admins, or any role holding local/forum:view at their role-switch context
- **Fix:** 1. **index.php:72-88.** Remove the legacy GET delete branch, or harden it like this: - Load the course with MUST_EXIST and reject SITEID. - Require course.open_module === 'forum' and open_coursetype == 1. - Build $ctx = local_forum\lib\accesslib::get_module_context($course->id) (the course's own costcenter path). Then require local/forum:delete AND local/forum:manage in $ctx, and moodle/course:delete in context_course::instance($course->id). - Unless is_siteadmin(), also require that $course->open_path is the caller's role-switch costcenter path or a descendant of it. Use a bounded prefix match (path === p OR path LIKE p.'/%'), never a bare prefix. - Only after all these checks call delete_course(), then delete_coursedetails() and the log entry. 2. **classes/external.php delete_forum().** Add the same guard. In order: - self::validate_parameters() - $course = $DB->get_record('course', ...
- **Already patched?** Not in 02-enhancement-code
- **Verifier:** I checked this against the snapshot at D:/Claude Local/Moodle Backup/01-production-codebase/html. The finding holds. 1. **Capability defaults.** In local/forum/db/access.php, lines 46-53 give local/forum:view (CONTEXT_COURSECAT) to the manager archetype by default. Lines 86-93 do the same for local/forum:manage. 2. **The page gate.** local/forum/index.php:53 resolves the context with local_forum\lib\accesslib::get_module_context() and passes no course. That lands in local_costcenter\lib\accesslib::get_module_context() (local/costcenter/classes/lib/accesslib.php:74-97): - A site admin gets the system context. - Anyone else gets get_user_roleswitch_context() (lines 98-180). That returns the ...

### 72. [P1] local_groups - (2)+(4) system-context cap skips tenant scoping; submitted user ids never re-validated

- **Capability:** moodle/cohort:assign (checked in CALLER context only)
- **Where:** local/groups/assign.php:43 require_capability('moodle/cohort:assign', caller context) then acts on $id (any cohort) and add[]/remove[] (optional_param PARAM_RAW) at :141-190 via local_groups_add_member/remove_member with no per-user tenant check. mass_enroll.php:44 same pattern. local_group_users (lib.php:753) scopes the CANDIDATE list but the submitted ids bypass it.
- **What crosses tenants:** Add/remove any user (any tenant) to/from any cohort/group; drives enrolments through cohort sync.
- **Who can reach it:** Any holder of moodle/cohort:assign; role 9 (org admin) has moodle/cohort:assign at system context (mdl_role_capabilities row ~2192-equiv) -> all tenants.
- **Fix:** assign.php: - Load local_groups for $id and pass its id so accesslib::get_module_context() resolves the cohort's own open_path context. Call require_capability('moodle/cohort:assign', <that context>). - Also require that the cohort's open_path is at or below the caller's path, using a bounded match: exact path, or path + '/%'. Never allow a bare prefix. - Cast add[]/remove[] with optional_param_array(..., PARAM_INT). - Before calling local_groups_add_member() or local_groups_remove_member(), intersect the submitted ids with array_keys(local_group_users($type, $id, $options, false, -1, -1)). Rebuild $options on the server from filters_form data; never json_decode it from $_GET or $_REQUEST. - Keep confirm_sesskey(). local_group_users(): - Replace every `IN ({$params[...]})` interpolation, and the $lastitem/$groupid concatenation, with $DB->get_in_or_equal() and named parameters. ...
- **Already patched?** Not addressed in 02-enhancement-code.
- **Verifier:** I re-read the code and the main finding holds. One part of the sweeper's "who" claim is wrong, and mass_enroll.php behaves differently from assign.php. 1) The capability is checked in the caller's context, not the cohort's. In local/groups/assign.php:39 the cohort is loaded with `get_record('cohort', ['id'=>$id])` for any id. Line :41 then calls `\local_groups\lib\accesslib::get_module_context()` with no group id, so groups_costcenterpath() returns null. For a non-siteadmin, local_costcenter\lib\accesslib::get_module_context() (local/costcenter/classes/lib/accesslib.php:74-97) falls back to get_user_roleswitch_context(), which is the caller's own tenant category context. Line :43 requires ...

### 73. [P1] local_location - (4) GET-based delete with no sesskey and no tenant check

- **Capability:** local/location:viewinstitute OR :manageinstitute (checked in caller context)
- **Where:** local/location/index.php:84-87 if($delete){ $institute->delete_institutes($id); } and room.php:60-62 delete_rooms($id). delete_institutes/delete_rooms (classes/event/location.php:118-133) do a bare $DB->delete_records by id. The page only requires the view capability; no confirm_sesskey; $id is not checked against the caller's costcenter.
- **What crosses tenants:** Delete any institute/room row in any tenant (CSRF-able via GET ?delete=1&id=X).
- **Who can reach it:** Any holder of local/location:viewinstitute/manageinstitute (org admins). Cross-tenant because id is unvalidated against costcenter.
- **Fix:** index.php and room.php: - Move the delete branch out of the view gate. - Call require_sesskey() first. - Require local/location:deleteinstitute (or :manageinstitute) for institutes and :deleteroom (or :manageroom) for rooms, checked in the target row's own context. Get it with (new \local_location\lib\accesslib())::get_module_context($id) for an institute, or the context of the room's parent institute for a room. - Before deleting, load the target row with MUST_EXIST (for a room, join to local_location_institutes). Unless is_siteadmin(), reject it when its costcenter differs from (int)explode('/', $USER->open_path)[1]. Fail closed when open_path does not resolve. - Switch the delete link to POST, or keep the delconfirm flow but have the server verify the sesskey. - In delete_institutes, refuse (or cascade) when rooms or classroom sessions still reference the institute. Edit path: apply ...
- **Already patched?** Not addressed in 02-enhancement-code.
- **Verifier:** I re-checked the code and the finding holds. One point needs correcting: it is the production role configuration that makes this reachable, not a default grant in access.php. 1. Unguarded delete, verified in code. - local/location/index.php:61 lets the page run if the user holds manageinstitute OR viewinstitute in $categorycontext. Lines 84-87 then run `if($delete){ $institute->delete_institutes($id); }`. $id comes from optional_param at line 45, and there is no require_sesskey()/confirm_sesskey(). - room.php:46-47 checks (manageinstitute||viewinstitute) AND (manageroom||viewroom). Lines 60-62 then call delete_rooms($id) the same way. - classes/event/location.php:118-133 run bare ...

### 74. [P1] local_program - 3-fail-open/read

- **Capability:** none
- **Where:** local/program/ajax.php:88-99 (action=viewprogramusers, only require_login) -> classes/output/renderer.php:859-930 -> classes/program.php:984-1017 programusers (no tenant filter; search concatenated at :990-999)
- **What crosses tenants:** Roster of any program (name, employee id, email, supervisor, level completion) across tenants.
- **Who can reach it:** Any logged-in user.
- **Fix:** 1. In ajax.php, for case 'viewprogramusers' (and the sibling cases, which are also ungated): take $context = (new \local_program\lib\accesslib())::get_module_context($programid), then call require_capability('local/program:viewusers', $context) or require manageusers. Also add require_sesskey(). 2. Check that the program belongs to the caller's tenant. The program's open_path must fall inside the caller's open_path subtree, using a bounded path-prefix match. Site admins are the only exception. If the caller's path does not resolve, deny rather than allow. 3. In program::programusers(), parameterise everything. Use :programid for the program. Build the search with $DB->sql_like('u.firstname', :s1, false) and so on, with the value '%' . $DB->sql_like_escape($stable->search) . '%'. Do the same for lastname, email and idnumber, with no string concatenation of user input. Also add an ...
- **Already patched?** No
- **Verifier:** I re-read the code and the finding holds. The search part is worse than the sweeper said: it is SQL injection, not just a missing tenant filter. 1. The entry point has no permission check. local/program/ajax.php:54 calls require_login() and nothing else: no require_capability, no sesskey, no tenant check. It reads programid (PARAM_INT, line 32) and search (optional_param_array, PARAM_RAW, line 38). The case 'viewprogramusers' branch at :88-99 copies $search['value'] into $stable->search and calls $renderer->viewprogramusers($stable). 2. The renderer does not block anyone. classes/output/renderer.php:859-966 calls get_module_context($programid) at :862. That resolves to ...

### 75. [P1] local_recompletion - 4-write-no-target-check (low)

- **Capability:** local/recompletion:manage (editingteacher, manager)
- **Where:** local/recompletion/editcompletion.php:30-37 and the form write path. users[] are not checked for enrolment or tenant. resetcompletion.php:47-50 is similar for the 'user' param.
- **What crosses tenants:** Creates or resets completion records in the teacher's course for users of other tenants who are not enrolled. The plugin is otherwise correctly context-scoped (course context) and has no cross-tenant reads.
- **Who can reach it:** Course editing teachers and managers.
- **Fix:** 1. editcompletion.php: right after building $users (after line 67), before the form is built or anything is printed, drop every target id that fails `is_enrolled($context, $uid, '', true)`, or throw on it. Also require that the user exists and is not deleted. On a BizLMS deployment, also require that the target's open_path sits under the course's costcenter path. Use the platform's bounded path-descendant helper, not a raw LIKE prefix. The resulting list is then used both by the mark_complete loop (lines 82-93) and by user_get_users_by_id (line 99). Also drop $userid when it is not enrolled. 2. resetcompletion.php: at lines 49-51, when $USER->id != $userid, call `$user = $DB->get_record('user', ['id'=>$userid, 'deleted'=>0], '*', MUST_EXIST);`. Then require either `is_enrolled($context, $user)` or an existing course_completions row for (userid, course), plus the same ...
- **Already patched?** No
- **Verifier:** I checked every part of the claim in the snapshot at D:/Claude Local/Moodle Backup/01-production-codebase/html/local/recompletion/. It holds, and the impact is somewhat larger than the sweeper reported. 1. The capability is default-granted as stated. db/access.php:29-37 defines local/recompletion:manage with CONTEXT_COURSE and archetypes editingteacher=CAP_ALLOW and manager=CAP_ALLOW. (resetmycompletion also goes to student and teacher, but it only covers the user's own record.) 2. editcompletion.php never checks the target users. - Lines 29-37 check only require_login($course) and require_capability('local/recompletion:manage', course context). - $userid (line 30, the 'user' parameter) and ...

### 76. [P1] local_request - 4/5-approval-bypass

- **Capability:** local/request:approverecord / denyrecord / deleterecord (never checked)
- **Where:** local/request/ajax.php:21-39 and local/request/tableresponse.php:42-57 (only require_login) -> classes/api/requestapi.php:161 approve, :245 deny, :139 delete. requestapi::create (:64) does not check the component's tenant. enroll_to_component (:314-355) enrols 'elearning' through the self-enrol instance directly.
- **What crosses tenants:** A user can create a request for any tenant's course, classroom, program or learning plan and approve it, which enrols them and bypasses enrolment and approval rules. A user can also approve, deny or delete anyone's request.
- **Who can reach it:** Any logged-in user.
- **Fix:** 1) In ajax.php and tableresponse.php, add require_sesskey() and require POST. Send JS the sesskey (M.cfg.sesskey). 2) In requestapi::approve and requestapi::deny: - Load the record with MUST_EXIST. - Resolve the target component's context: context_course for elearning, or the classroom/program/learningplan/certification accesslib::get_module_context($componentid). - Call require_capability('local/request:approverecord' or ':denyrecord', $thatcontext). - Reject the call when $record->createdbyid == $USER->id (no self-approval), unless is_siteadmin(). - Check that the component's open_path costcenter matches the responder's costcenter. Use a bounded check: local_costcenter path descendant filter, or explode('/', open_path)[1] equality. Do not use a LIKE prefix. - Only act on status PENDING for approve; deny should apply to PENDING or APPROVED records. 3) In requestapi::delete: - Allow ...
- **Already patched?** No
- **Verifier:** I re-read the code in the 01-production-codebase snapshot and the finding holds. Entry points - local/request/ajax.php:8 and tableresponse.php:76 (the file's second `<?php` block) only call require_login(). - Neither calls require_sesskey() or has_capability() anywhere. - Both pass a caller-supplied `id` straight to requestapi::approve (ajax.php:28/31), ::deny (:35) and ::delete (:38). Tableresponse.php does the same at :95/:97/:100. - The front-end (amd/src/requestconfirm.js:126-130) POSTs exactly {component, componentid, action, id} to ajax.php. The capabilities are never checked - db/access.php defines local/request:approverecord, :denyrecord and :deleterecord. - In requestapi.php their ...

### 77. [P1] local_search - (4) self-enrol to any course id with no tenant/visibility check

- **Capability:** n/a (self-enrol) — noted as lower severity
- **Where:** local/search/courseinfo.php:14-34 (?enrol=1&id=X self-enrols $USER to any course with a self enrol instance) and classes/external.php enrol_user_to_module (:152-175) dispatches to \<type>\output\search::enrol_user_to_component with client moduleid/type, validate_context on client contextid only. coursedetails.php:24 gates VIEW by $USER->open_costcenterid != $course->open_costcenterid but the enrol endpoints do not.
- **What crosses tenants:** Cross-tenant self-enrolment into courses the user should not see (course rows carry open_costcenterid/open_path).
- **Who can reach it:** Any authenticated learner.
- **Fix:** courseinfo.php (lines 17-38): - Require POST plus require_sesskey(). - Drop the hand-rolled enrol_user call and delegate to (new \local_courses\output\search())->enrol_user_to_component('self', $id). That function already enforces the in-tenant open_path match, c.visible=1, c.selfenrol=1, instance status=0 and approvalreqd!=1. - Alternatively, fetch only an enabled self instance, call $plugin->can_self_enrol($instance) === true, then $plugin->enrol_self($instance). Keep enrol_user out of any learner-reachable path. - In either case, first check that the course's open_path sits under the caller's tenant with a boundary-anchored match (the tenant path_descendant_filter pattern), and fail closed when the caller's open_path does not resolve. - In the non-enrol branch, apply the same tenant/visibility check before renderer::get_course_info. local_learningplan is_plan_accessible ...
- **Already patched?** coursedetails.php view-check was hardened in 02-enhancement-code/modified-files/local/search/coursedetails.php:47-48 (adds isloggedin/guest guards) but the enrol endpoints (courseinfo.php, enrol_user_to_module) were not changed.
- **Verifier:** I re-read the code in the 01-production-codebase snapshot. The main claim holds: courseinfo.php self-enrols the caller with no tenant check. Part of the claim about the web-service path is wrong, but that path has a different cross-tenant hole of its own. 1) local/search/courseinfo.php — CONFIRMED, and worse than the sweeper said. - Line 12: the only gate is require_login(). There is no capability check and no sesskey. The JS (amd/src/courseinfo.js:89/139) calls it with a plain GET. - Lines 14-16: $id comes from the client as a PARAM_INT. The course is loaded with no check of open_path or open_costcenterid, and no check of visible, selfenrol or approvalreqd. - Line 22: `select * from ...

### 78. [P1] notifications - 4-read (email content across tenants)

- **Capability:** local/notifications:view in the caller's own context (renderer.php:269-276); the page itself is require_login only
- **Where:** local/notifications/email_status_details.php:30-40, classes/output/renderer.php:264-287, classes/local/email_status_func.php:89-95
- **What crosses tenants:** From and To names, subject and full email body of any local_emaillogs id in any tenant. BizLMS emails carry names, course details and possibly account or credential notices.
- **Who can reach it:** Org admins (notifications:view is default-granted to manager, db/access.php:37-44). Ids are enumerable from the unauthenticated listing above.
- **Fix:** 1. `email_status_details.php`: before rendering anything, call `require_login()`, then `$id = required_param('id', PARAM_INT)`. Load the log together with its notification in one query that fails if nothing matches: `$log = $DB->get_record_sql('SELECT le.*, ni.open_path FROM {local_emaillogs} le JOIN {local_notification_info} ni ON ni.id = le.notification_infoid WHERE le.id = :id', ['id' => $id], MUST_EXIST);` 2. For non-siteadmins, build the scope from the target, not the caller. Either: - (a) resolve `$ctx = \local_costcenter\lib\accesslib::costcenterpath_contextdata($log->open_path)` and call `require_capability('local/notifications:view', $ctx)`; or - (b) add `\local_costcenter\lib\accesslib::get_costcenter_path_field_concatsql('ni.open_path')` to the query above. If (b) returns an empty string for a non-admin, deny access (fail closed). In both cases, also check that the ...
- **Already patched?** No.
- **Verifier:** I re-read the code and could not refute the finding. The defect is real: the page checks the capability in the caller's own context, not in the context of the email log being viewed, and it never filters by tenant. 1. The page only requires login. `local/notifications/email_status_details.php:30-31` reads `id` with `optional_param(PARAM_INT)` and calls `require_login()`, with no capability check. Line 50 passes the raw id to `renderer::view_email_status_details($id)`. (Line 33 calls `set_context($sitecontext)` with an undefined variable, so Moodle falls back to the system context. That is harmless.) 2. The lookup is not scoped. At `renderer.php:272-274`, `getNotificationInfoById($id)` runs ...

### 79. [P1] notifications - 4 (delete)

- **Capability:** local/notifications:manage in the caller's own context (:43)
- **Where:** local/notifications/index.php:31-50 (delete_records :47)
- **What crosses tenants:** Deletes any tenant's notification definition by id.
- **Who can reach it:** Org admins (manager archetype).
- **Fix:** In local/notifications/index.php, replace lines 46-51 with a check that loads the row and verifies its tenant before deleting: if ($deleteid && $confirm && confirm_sesskey()) { $rec = $DB->get_record('local_notification_info', ['id' => $deleteid], 'id, open_path', MUST_EXIST); // Capability must hold in the TARGET record's tenant context, not the caller's own. $targetctx = \local_notifications\lib\accesslib::get_module_context($rec->id); require_capability('local/notifications:delete', $targetctx); // or :manage; :delete exists and is currently unused if (!is_siteadmin()) { // Defence in depth: the row must fall inside the caller's own costcenter scope. $scopesql = \local_notifications\lib\accesslib::get_costcenter_path_field_concatsql('ni.open_path'); if (!$DB->record_exists_sql("SELECT ni.id FROM {local_notification_info} ni WHERE ni.id = :id $scopesql", ['id' => $rec->id])) { throw ...
- **Already patched?** No.
- **Verifier:** I checked the code in the production snapshot (D:/Claude Local/Moodle Backup/01-production-codebase/html/local/notifications) and the finding holds. 1. The capability is granted by default. db/access.php:29-36 defines local/notifications:manage at CONTEXT_COURSECAT with 'archetypes' => ['manager' => CAP_ALLOW]. So any manager-archetype role holds it, and so does any role the tenant/org admin uses to reach this page. The page is built for org admins: renderer.php has a non-siteadmin branch that filters the list by tenant. 2. The capability is only checked in the caller's own context. index.php:33 calls accesslib::get_module_context() with no record id. For a non-siteadmin, ...

### 80. [P1] notifications - 3 (unbounded path pattern) / 4-read

- **Capability:** none (require_login only)
- **Where:** local/notifications/custom_ajax.php:33-120 (pattern built at :40); the same pattern is at classes/forms/notification_form.php:84 and :190
- **What crosses tenants:** Names and ids of courses, classrooms, online tests, evaluations, programs, learning plans and online exams for any caller-chosen costcenterid. The pattern is '%'.$costcenterid.'%', which is unbounded, so id 1 also matches /177, /10 and so on.
- **Who can reach it:** Any authenticated user.
- **Fix:** 1) In custom_ajax.php, directly after require_login(), add require_sesskey() and require_capability('local/notifications:manage', $context), where $context comes from (new \local_notifications\lib\accesslib())::get_module_context(). Apply the same capability check at the top of local_notifications_output_fragment_new_notification_form in lib.php, and in externallib.php submit_create_notification_form after validate_context. 2) Restrict costcenterid to the caller's own tenant. Unless is_siteadmin() or has_capability('local/costcenter:manage_multiorganizations', context_system::instance()), derive the tenant from the caller's open_path: $own = (int) explode('/', $USER->open_path)[1]. If a costcenterid is supplied and differs from $own, throw required_capability_exception. If the caller's open_path does not resolve, throw as well (fail closed). Never fall back to 0 or to an empty filter. ...
- **Already patched?** No.
- **Verifier:** I read the code and it matches the finding. The data that leaks is catalogue metadata: ids and names. No user PII is exposed. 1. Gate is login only. local/notifications/custom_ajax.php:36-37 sets the context and calls require_login(). Nothing in the file calls require_capability or has_capability, so the manage cap is never checked. local/notifications/db/access.php:29-76 does grant :manage, :view, :create, :delete, :update and :visible only to the manager archetype, but custom_ajax.php never checks any of them. Any authenticated user can call it directly. 2. The caller picks the tenant. :34 reads costcenterid with optional_param(..., 0, PARAM_INT). Nothing checks it against the caller's ...

### 81. [P1] onlineexams - 4 (enrol/unenrol with no target-tenant check) plus 5 (arbitrary role assignment)

- **Capability:** local/onlineexams:enrol + :unenrol + :manage, checked in the caller's OWN role context (get_module_context() with no id, :53, :74-76)
- **Where:** local/onlineexams/onlineexamsenrol.php:41-45, 83-86, 168-221, 224-284. roleid at :43 is passed to enrol_user at :198. Runtime assign_capability/unassign_capability at :178-186, 211-215, 233-241, 273-277.
- **What crosses tenants:** Enrols or unenrols arbitrary user ids (any tenant) in the course behind any manual enrol instance (enrolid), which is independent of the 'id' used for the permission check. access_courses_permission() always returns true (local/includes.php:235-266, tenant comparison commented out). The caller-supplied roleid is assigned at course context, so any role (manager, editingteacher, custom) can be given in any tenant's course. The page also writes role_capabilities overrides for the caller's role; they persist if the request aborts before unassign.
- **Who can reach it:** Org admins holding the onlineexams enrol caps (default-granted to the manager archetype, db/access.php:30-93).
- **Fix:** All changes are in local/onlineexams/onlineexamsenrol.php unless another file is named. 1. Move require_login() and all require_capability() calls above the view=ajax branch at :57. 2. After loading $instance (:44), set $ctx = context_course::instance($instance->courseid). Require local/onlineexams:enrol, :unenrol and :manage at $ctx, not at get_module_context(). 3. Require $course_id == $instance->courseid, and throw moodle_exception otherwise. 4. Require that the course's open_path equals, or is a bounded descendant of, the caller's roleswitch costcenter path, using exact or '/'-bounded matching. Fail closed when the caller's path does not resolve. Fix has_user_permission::access_courses_permission() in local/includes.php:235-264 the same way: compare the course's open_path tenant with the caller's tenant and return false by default. 5. Read add/remove with optional_param_array(..., ...
- **Already patched?** No.
- **Verifier:** I re-read the code in D:/Claude Local/Moodle Backup/01-production-codebase/html. The core finding holds. One sub-claim does not. 1. The capability really is granted to org admins. db/access.php:30-45 and :86-93 give local/onlineexams:enrol, :unenrol and :manage to the manager archetype. The prod-derived dump (pre-moodle5-backup.sql) has role_capabilities rows at system context for role 1 (manager) and role 9. Role 9 is 'administrator' with archetype 'manager', which is the org-admin role. So every org admin holds all three caps wherever that role is assigned. 2. The permission check runs in the caller's own context, not the target's. onlineexamsenrol.php:53 calls get_module_context() with ...

### 82. [P1] onlineexams - 4 (delete any course)

- **Capability:** local/courses:view OR local/courses:manage in the caller's own context (:54)
- **Where:** local/onlineexams/index.php:28, 54, 74-88 (delete_course at :76)
- **What crosses tenants:** Deletes any course id in any tenant. There is no moodle/course:delete check, no online-exam type check and no tenant check. Only a sesskey is required.
- **Who can reach it:** Org admins (local/courses:view is default-granted to the manager archetype, local/courses/db/access.php:54-61).
- **Fix:** In local/onlineexams/index.php:74-90, replace the branch with a guarded version, then apply the same change to local/courses/courses.php:73 and local/forum/index.php:72. 1. Load the course with MUST_EXIST: `$course = $DB->get_record('course', ['id' => $deleteid], '*', MUST_EXIST);`. Refuse SITEID explicitly. 2. Check the course type. Require the course to be an online-exam course (its open_coursetype / module-type marker). Throw moodle_exception otherwise. 3. Check the target's tenant. Build the context from the target: `$ctx = \local_onlineexams\lib\accesslib::get_module_context($course->id);`. That resolves through the course's own open_path. Then assert that open_path is inside the caller's scope, using `\local_costcenter\lib\accesslib::get_costcenter_path_field_concatsql('c.open_path')` on a record_exists_sql, or an equivalent path-descendant check. Fail closed: if the caller's path ...
- **Already patched?** No.
- **Verifier:** I tried to refute the finding and could not. I checked each claim against the snapshot at D:/Claude Local/Moodle Backup/01-production-codebase/html. 1. Page gate (local/onlineexams/index.php:51-56). The page calls require_login(). It then takes $categorycontext from \local_onlineexams\lib\accesslib::get_module_context() with a null course id. That goes through local/costcenter/classes/lib/accesslib.php:74-97. A site admin gets the system context. Anyone else gets their role-switch context, which is their own org or category context, or system context if none resolves. The only check is has_capability('local/courses:view' OR 'local/courses:manage') in that caller context. 2. Default grant. ...

### 83. [P1] skillrepository - 1/4 (write caps default-granted to trainers, target not scoped)

- **Capability:** local/skillrepository:create_skill (default-granted to editingteacher + manager, db/access.php:38-63) in the caller's own context
- **Where:** local/skillrepository/index.php:55-83 (update by submitted id :77-78); skill_category.php:46-80
- **What crosses tenants:** Trainers and org admins can edit any tenant's skill or category through the submitted hidden id.
- **Who can reach it:** editingteacher-archetype (trainer) and manager-archetype roles.
- **Fix:** 1. Add one record guard and call it before every update or delete: for example local_skillrepository_require_record($table, $id, $cap). - Whitelist $table to local_skill, local_skill_categories and local_course_levels. - Load the row with MUST_EXIST. - Unless the caller is a site admin, resolve the context from the row's own open_path (accesslib::costcenterpath_contextdata) and call require_capability($cap, that context). - Also require the row's open_path to equal one of the caller's role costcenter paths, or start with that path followed by '/'. That keeps the path boundary. 2. External functions in classes/external.php: - submit_skill_repository_form_form: require_capability('local/skillrepository:create_skill') for new rows. For id > 0, call the guard with update_skill. - submit_skill_category and submit_level_form: same pattern, with the level caps for levels. - delete_skill: add ...
- **Already patched?** No.
- **Verifier:** I re-read the code in D:/Claude Local/Moodle Backup/01-production-codebase/html/local/skillrepository. The finding holds, and it understates the problem: the modal path the UI actually uses checks no capability at all. 1. The default grant is real. db/access.php:38-46 (create_skill), :47-55 (delete_skill), :56-64 (update_skill) and the *_level caps at :76-102 all give CAP_ALLOW to the manager and editingteacher archetypes at CONTEXT_COURSECAT. 2. index.php:55 checks create_skill only in the caller's own context. get_module_context() is called with no record id, so classes/lib/accesslib.php:47-50 falls through to local/costcenter/classes/lib/accesslib.php:98-180 and returns the caller's ...

### 84. [P1] skillrepository - 3 (latent wrong-table scoping, plus a wrong alias)

- **Capability:** n/a (scoping helper)
- **Where:** local/skillrepository/classes/lib/accesslib.php:33-44 (reads local_evaluations.open_path by skill id); classes/local/querylib.php:36 (filter uses alias sk. on a query aliased lcl)
- **What crosses tenants:** Any future get_module_context($skillid) or concatsql call would resolve to an unrelated evaluation's tenant. The levels table query errors when a filter is produced and returns every tenant's levels, with creator names, when the filter is empty.
- **Who can reach it:** Anyone viewing the levels table (level.php and ajax getlevelstable are require_login only).
- **Fix:** (1) accesslib.php:41: read the path from the right table, `$DB->get_field('local_skill', 'open_path', ['id' => $skillid])` (or local_course_levels when the helper is used for levels). Add `MUST_EXIST` or return a deny marker when the row is missing. (2) querylib.php:36: use `get_costcenter_path_field_concatsql('lcl.open_path')`. Apply the same filter to get_total_levels_count() (alias the table `lcl`). Replace both interpolated `sSearch` clauses with `$DB->sql_like('lcl.name', ':s', false)` and a `$DB->sql_like_escape()`'d named parameter. Read the request fields with `optional_param(..., PARAM_INT/PARAM_TEXT)` instead of `$_REQUEST`. (3) Fail closed, platform-wide. In local/costcenter/classes/lib/accesslib.php, `get_user_roleswitch_costcenterpath_concatsql()` (around line 279) should return `" AND 1=0 "` for a non-site-admin when `array_filter($sqlarray)` is empty, instead of `""`. ...
- **Already patched?** No.
- **Verifier:** I checked every part of the finding against the snapshot. All of it holds, with two caveats about how far it reaches. 1. accesslib.php:39-41 has the wrong-table read. skillrepositery_costcenterpath() does `$DB->get_field('local_evaluations','open_path', array('id'=> $skillid))`, so it looks up a skill id in the evaluations table. It is LATENT, not live. I found about 30 callers of get_module_context() and get_costcenter_path_field_concatsql() (lib.php, renderer.php, querylib.php, external.php, forms, events) and none of them passes a $skillid. The helper therefore always returns null and falls back to the user's own path. The sweeper's "any future call" wording is accurate. 2. ...

### 85. [P2] evaluation - 4-read / 2

- **Capability:** local/evaluation:viewreports / :viewanalysepage in the caller's OWN context (structure.php:173-184)
- **Where:** local/evaluation/classes/external.php:1191-1240 get_analysis (mobile service); data from lib.php:1892-1950 evaluation_get_group_values, which has no tenant filter; validate_evaluation() at :679-686 does no permission check
- **What crosses tenants:** Aggregated answers and free-text responses for any tenant's evaluation id. analysis.php:56-60 has an explicit top-level-org comparison, but this web service does not.
- **Who can reach it:** Org admins or report viewers with viewreports in their own tenant, using a mobile web-service token.
- **Fix:** 1. **Scope the context to the evaluation.** In local_evaluation_external::validate_evaluation() (classes/external.php:679): - Resolve `$ctx = \local_evaluation\lib\accesslib::get_module_context($evaluationid)`. Passing the id maps the evaluation's open_path to its org category context. - Call `self::validate_context($ctx)`. - For non-site-admins, require that the evaluation's open_path falls inside the caller's own path. Use a delimiter-bounded prefix check (path equals the caller's path, or starts with it followed by '/'), not a raw LIKE. Otherwise throw required_capability_exception or moodle_exception('nopermission'). - Return `[$evaluation, $ctx]`. 2. **Make the permission methods use that context.** Change local_evaluation_structure::can_view_analysis() (structure.php:173), and every other method that calls get_module_context() without an id, to use ...
- **Already patched?** No.
- **Verifier:** I checked the finding against the code and could not refute it. It holds as a class 2 / class 4-read defect: the permission check runs in the caller's own context and never looks at which tenant owns the evaluation. It is not class 1. 1. **Default grants (class 1 does not apply).** local/evaluation/db/access.php defines every capability, including viewreports and viewanalysepage, with no 'archetypes' key. Nothing is default-granted. Whoever can reach this gets it through site-configured roles. In practice that is the BizLMS org-admin or report-viewer role, and that role must actually hold local/evaluation:viewreports. 2. **The function is exposed.** db/services.php registers ...

### 86. [P2] evaluation - 2 (scoping code built but never applied)

- **Capability:** local/evaluation:viewanalysepage (evaluation context)
- **Where:** local/evaluation/show_entries.php:80-86 (superuser SQL built and never executed); classes/completion.php:80 (anonymity filter commented out)
- **What crosses tenants:** The supervisor ('SP') restriction is never enforced, so any holder sees all identified responses. A userid parameter also de-anonymises anonymous replies.
- **Who can reach it:** Holders of viewanalysepage in the evaluation's tenant, or at system context for all tenants.
- **Fix:** 1) classes/completion.php:80 – restore the filter: `$params['anonymous_response'] = EVALUATION_ANONYMOUS_NO;` inside the `if ($userid)` branch. Also stop reading `->courseid` from a false record at line 84 (throw or return early if the lookup finds nothing). 2) show_entries.php, after line 56 – tie per-response access to the viewer. If the viewer is not a site admin and lacks local/evaluation:edititems in $context, allow the showcompleted or userid view only when (a) userid == $USER->id and the completion record's userid == $USER->id, or (b) the evaluation is in 'SP' mode and the respondent's open_supervisorid == $USER->id. For (b), actually execute the supervisor query from lines 82-86 with $DB->record_exists_sql, and add `AND leu.userid = :targetuserid` so it checks the named respondent rather than any team member. Otherwise print_error. When the completion is anonymous ...
- **Already patched?** No.
- **Verifier:** I'm confirming a narrower finding than the sweeper filed. Two of its three claims don't hold up. The de-anonymisation claim does, but it's a privacy defect within one tenant, not a cross-tenant one. 1. Class 1 (default grant): refuted. local/evaluation/db/access.php:28-125 declares every capability with no 'archetypes' key. Nothing is granted by default to manager, coursecreator, editingteacher, teacher, user or student. That includes viewanalysepage (line 44) and the plugin's own manage_multiorganizations (line 110). 2. Class 2 ("scoping code built but never applied"): refuted. - The tenant gate is applied. show_entries.php:47 takes the context from the evaluation's own open_path, via ...

### 87. [P2] local_costcenter - (3) fail-open when a non-admin's path does not resolve

- **Capability:** none — resolver fail-open
- **Where:** local/costcenter/classes/lib/accesslib.php:245-285 get_user_roleswitch_costcenterpath_concatsql() and :315-363 userpath_match_sql(). If $USER->useraccess['currentroleinfo']['contextinfo'] is empty AND local_userdata has no row for the user, $paths is empty, no 'AND (...)' clause is appended, and the caller returns rows for ALL tenants.
- **What crosses tenants:** Same PII/grades/completions as above, for any user whose costcenter path fails to resolve.
- **Who can reach it:** Any authenticated non-admin whose role-switch context is unset and who has no mdl_local_userdata row. In the prod dump mdl_local_userdata holds only 1560 rows vs ~2871 users, so >1000 users have no row; for them userpath_match_sql yields an empty filter (fail-open) on any page that relies on it before role-switch state is built.
- **Fix:** In local/costcenter/classes/lib/accesslib.php: 1. `userpath_match_sql()`: for a non-admin, when `local_userdata` gives no non-empty path, fall back to the authoritative `$USER->open_path` (re-read with `$DB->get_field('user', 'open_path', ['id' => $USER->id])`). Skip empty or `'/'` paths so `''` can never become `LIKE '/%'`. If nothing resolves, return `' 1=0 '` (deny all), never `''`. 2. `get_user_roleswitch_costcenterpath_concatsql()`: after building `$sqlarray`, if `!is_siteadmin()` and `array_filter($sqlarray)` is empty, return `'AND (1=0)'` instead of `''`. Also treat `get_costcenterpath_context()` returning 0 as "unresolved", not "unscoped". 3. `get_costcenter_path_field_concatsql()`: add a final guard so a non-admin can never get an empty string. If `$concatsql === ''` and `!is_siteadmin()`, return `'AND (1=0)'`. 4. Bind the LIKE patterns as named parameters (`$DB->sql_like` plus ...
- **Already patched?** No fix found in 02-enhancement-code; the empty-clause pattern is present in the snapshot.
- **Verifier:** I read the code myself and the fail-open holds. One ordinary logged-in user can reach it, and the patch set does not fix it. **The resolver**, in D:/Claude Local/Moodle Backup/01-production-codebase/html/local/costcenter/classes/lib/accesslib.php: - `get_costcenter_path_field_concatsql()` (lines 35-72): the admin shortcut at line 46 applies only to site admins. A non-admin with no explicit path goes to `get_user_roleswitch_costcenterpath_concatsql()` (line 53). - That function (lines 245-285): when `$USER->useraccess['currentroleinfo']['contextinfo']` is empty, it sets `$sqlarray[] = userpath_match_sql(...)` (line 273). It also falls back to `userpath_match_sql` when a contextinfo entry's ...

### 88. [P2] local_program - 2-own-context-check-then-any-target

- **Capability:** local/program:manageprogram, manageusers
- **Where:** local/program/enrollusers.php:50,55-56 (capability at the caller's own context), :35 bcid, :134/:155 program_add/remove_assignusers
- **What crosses tenants:** Cross-tenant add and remove of program users.
- **Who can reach it:** Org admins.
- **Fix:** In enrollusers.php: - Replace :50 with `$categorycontext = (new \local_program\lib\accesslib())::get_module_context($programid);` so :55-56 are checked against the program's own tenant category. - Load the program the way mass_enroll.php:35-45 does: `get_costcenter_path_field_concatsql('lp.open_path')` scoped `get_record_sql`, then `print_error` / `moodle_exception` if it is missing and the caller is not site admin. Fix the fail-open in `programview_check` (classes/output/renderer.php:1061): throw when `$program` is empty or false, not only for an empty array. In `program_add_assignusers` and `program_remove_assignusers` (classes/program.php:594 and :674): - Re-check the capability at `get_module_context($programid)`. - Cut the posted user ids down to those whose `u.open_path` is within the program's `open_path` (for example `path_descendant` / `lowerandsamepath` matching), or pass them ...
- **Already patched?** No
- **Verifier:** I read the code myself, and the finding holds. File paths below are relative to D:/Claude Local/Moodle Backup/01-production-codebase/html/local/program/. 1. Capability check runs in the caller's own context, not the program's. enrollusers.php:50 calls `accesslib::get_module_context()` with no `$programid`. Through local/costcenter/classes/lib/accesslib.php `get_module_context()` and then `get_user_roleswitch_context()`, that returns the caller's own role-switch category context. The checks at :55-56 (`require_capability manageprogram` and `manageusers`) are therefore tested against the org admin's own tenant category, whatever `bcid` (:35) names. mass_enroll.php:51 does it correctly with ...

### 89. [P2] notifications - 3 (fail-open) plus missing capability

- **Capability:** none (require_login only)
- **Where:** local/notifications/externallib.php:196-230 managenotificationsview; lib.php:762-790 notification_details
- **What crosses tenants:** Notification templates (type, subject, org). They are scoped to the caller's path, so learners can read their tenant's templates, and every tenant is returned when the path does not resolve.
- **Who can reach it:** Any authenticated user.
- **Fix:** 1) externallib.php managenotificationsview: remove the caller-supplied $PAGE->set_context($contextid). Instead: $ctx = \local_notifications\lib\accesslib::get_module_context(); self::validate_context($ctx); require_capability('local/notifications:manage', $ctx); Use :manage to match index.php:43, or :view if that is the intended read permission. Also add 'capabilities' => 'local/notifications:manage' to the db/services.php entry. 2) Fail closed in notification_details (lib.php:773-780). For a non-admin, if the scoping clause returned by get_costcenter_path_field_concatsql() is empty, return ['count' => 0, 'data' => []] (or throw required_capability_exception) instead of running the query unscoped. Delete the dead `else print_error` branch. 3) Fail closed centrally in local/costcenter/classes/lib/accesslib.php, because every BizLMS plugin shares this helper: - userpath_match_sql(): if a ...
- **Already patched?** No.
- **Verifier:** I read the code and it matches the report. Both halves of the claim hold. 1) The web service is missing a capability check. The function is defined at local/notifications/externallib.php:196-230. The service is registered in db/services.php:36-44 as ajax=true, and the registration declares no 'capabilities'. The function body only calls require_login() (line 206) and $PAGE->set_context($contextid) (line 208), where $contextid is an int the caller supplies. There is no validate_context, has_capability or require_capability. The HTML page does check: index.php:43 has require_capability('local/notifications:manage', system). The web service is a path around that check. In db/access.php, every ...

### 90. [P2] skillrepository - 4 (update or re-home another tenant's records)

- **Capability:** none (validate_context only)
- **Where:** local/skillrepository/classes/external.php:42-70 (skill update :63), :99-130 (category, classes/event/insertcategory.php:45-51), :212-235 (level, classes/local/querylib.php:14-25)
- **What crosses tenants:** Updates any skill, category or level by caller-supplied id. open_path comes from submitted hierarchy fields, so records can be moved between tenants.
- **Who can reach it:** Any authenticated user.
- **Fix:** **1. Add capability checks to every write endpoint.** After `validate_context`: - submit_skill_repository_form_form: `require_capability('local/skillrepository:create_skill', $context)`, or `:update_skill` when `id > 0`. - submit_level_form: `:create_level` / `:update_level`. - submit_skill_category: `:manage`, or a new create/update_category capability. - delete_skill: add `validate_context` and `require_capability(':delete_skill')`. **2. Check the target record before any update or delete.** When id > 0: - Load the existing row with `$DB->get_record(table, ['id' => $id], 'id, open_path', MUST_EXIST)`. - Reject it unless the caller is site admin or `$existing->open_path` is inside one of the caller's `contextinfo` costcenterpaths. - Use a boundary-safe test: `$p === $scope || strpos($p . '/', rtrim($scope, '/') . '/') === 0`. - Apply the same check to the new open_path produced by ...
- **Already patched?** No.
- **Verifier:** I re-read the code and could not refute the finding. The production snapshot has the defect, and nothing in 02-enhancement-code touches local/skillrepository. The only match in all-modifications.patch is a settings.local.json allow-list line. **1. Any logged-in user can call the endpoints.** - db/services.php:29-36, :45-52 and :61-68 declare submit_skill_repository_form_form, submit_skill_category_form and submit_level_form as `ajax => true`. None of them has a capability check. - Each function only calls `validate_context()` on `accesslib::get_module_context()` (external.php:49-51, :106-108, :218-220). - For a non-admin with no category role, `get_user_roleswitch_context()` ...

## Refuted findings (kept for the record)

- **local_costcenter** (local/costcenter:manage_multiorganizations (+ manage_ownorganization, manage_owndepartments, view, create, update, ...): The finding says org admins can see every tenant. That part is wrong on four counts, though two of its supporting facts are true. True: - local/costcenter/db/access.php:151-158 gives 'local/costcenter:manage_multiorganizations' => manager CAP_ALLOW. The rest of the :71-182 block is the same, and the second 'local/costcenter:manage' key at :119 silently replaces the empty one at :29. - Several consumers do drop the tenant filter when a user holds this capability: - local/users/lib.php:134, :197 ...
- **local_users** (local/costcenter:manage_multiorganizations at SYSTEM context (manager archetype)): **The code pattern is real, but the claim that org admins can reach it is wrong.** 1. **The branches exist.** Every cited site in local/users/lib.php (lines 134, 197, 256, 306, 479, 506, 534, 557, 585, 793, 1333), plus classes/functions/users.php:277 and skillprofile.php:42, follows the same shape. When `is_siteadmin() || has_capability('local/costcenter:manage_multiorganizations', $categorycontext)` is true, the query drops the open_path filter. - They are reachable through ...
- **local_courses** (none (no require_login)): Some of the sweeper's facts are right, but the leak and the injection it describes cannot happen. The query always fails before it returns rows. What is true: - local/courses/filterajax.php:25-63 never calls require_login, so anyone can request it. - With action=courseenroll&type=empname, line 111 calls get_all_users_id_fullname() in local/courses/filterclass.php:342. The courseid parameter is passed in as $planid. - The `q` parameter is PARAM_RAW and is pasted straight into the SQL at ...
- **local_classroom** (local/costcenter:manage_ownorganization / manage_owndepartments (manager archetype), ...): The finding is wrong as stated. get_classroom_details() really has no tenant filter, but an org admin cannot use it to read counts for any classid they choose. 1. The capabilities are real, but they only choose which kind of details to show. local/costcenter:manage_ownorganization and manage_owndepartments are default-granted to the manager archetype (local/costcenter/db/access.php:167-182). local/classroom:trainer_viewclassroom has no archetypes (local/classroom/db/access.php:102-105). In ...
- **local (top-level lib.php)** (local/costcenter:manage_multiorganizations (manager archetype)): The code facts are right, but nothing can reach the code. The audit asked for reachable findings, so this does not count as one. What checks out: - `local/costcenter/db/access.php:151-158` gives `local/costcenter:manage_multiorganizations` to the `manager` archetype by default (CAP_ALLOW, contextlevel COURSECAT). `manage_multidepartments` (lines 159-166) is also a manager default. - `local/lib.php:40-45` `get_mydepartments()`: when the caller holds `manage_multiorganizations` in ...
- **evaluation** (local/evaluation:viewreports in the evaluation's context; sesskey checked only for POST): The code-level flaws are real, but no message is ever delivered, so no data or action crosses tenants in this snapshot. What the sweeper got right, in local/evaluation/show_nonrespondents.php: - Line 36 reads messageuser[] from GET or POST. - Line 65 runs confirm_sesskey() only when data_submitted() is true. data_submitted() checks POST only, so a GET request skips the sesskey check. - Line 69 is the only gate: local/evaluation:viewreports in the evaluation's context. The sendmessage branch ...
- **skillrepository** (none (require_login only, no sesskey)): The claim is that any user can create records credited to Airpay. That part does not hold up. The endpoint is badly broken, but in a different and more serious way. Confirmed in the code (D:/Claude Local/Moodle Backup/01-production-codebase/html/local/skillrepository/addcategory.php): - Line 25 has only require_login(). There is no capability check and no sesskey check. Line 28 only sets the page context and checks nothing. - Line 32 reads $table as PARAM_RAW. Line 38 assigns $open_path, which ...
- **block_learnerscript (depends on local_costcenter)** (local/costcenter:manage_multiorganizations (default CAP_ALLOW for the manager archetype, ...): The code pattern is real, but the claimed exposure does not happen with production's role configuration, so this is not a reachable finding today. What the code does (confirmed): - local/costcenter/db/access.php:151-158 gives 'local/costcenter:manage_multiorganizations' to the manager archetype by default (CAP_ALLOW, contextlevel COURSECAT). - blocks/learnerscript/reports/coursesoverview/report.class.php:75, coursegradeactivities/report.class.php:98 and learningpathoverview/report.class.php:103 ...

## Coverage

- Partition 1: 16 findings; checked: local/costcenter/db/access.php (capability archetype grants); local/costcenter/classes/lib/accesslib.php (get_costcenter_path_field_concatsql, get_module_context, userpath_match_sql, get_user_roleswitch_costcenterpath_concatsql — fail-open + cap-skip resolver, shared base class); local/costcenter/classes/external.php (submit_costcenterform_form, costcenter_status_confirm, costcenter_delete_costcenter, department_create, form_option_selector incl. user_supervisor_selector); local/costcenter/costcenterview.php, index.php, costcentersettings.php, test.php, renderer.php, lib.php (multipleorg inert); local/users/db/access.php, db/services.php; local/users/classes/external.php ...
- Partition 2: 26 findings; checked: local/courses: db/access.php, db/services.php, classes/external.php (all 19 functions), classes/lib/accesslib.php, lib.php (mass_enroll, course_enrolled_users, get_listof_courses, get_enrolledusers), includes.php, filterclass.php, renderer.php get_course_enrolledusers, courseenrol.php, mass_enroll.php, courses.php, index.php, edit.php, editenrol.php, enrolledusers.php, exportcsv.php, ajax.php, filterajax.php, classes/form/mass_enroll_form.php, classes/form/custom_course_form.php; local/classroom: db/access.php, db/services.php, externallib.php (all functions scanned; delete/unenrol/viewusers read), classes/lib/accesslib.php, classes/classroom.php (classroom_add_assignusers, classroomusers, ...
- Partition 3.1: 35 findings; checked: Codebase root: D:/Claude Local/Moodle Backup/01-production-codebase/html (read-only; nothing edited, no server contacted, no config.php, .env or credentials read); local/onlineexams: db/access.php (all 9 caps default-granted to the manager archetype), db/services.php, classes/external.php (all 7 functions), classes/lib/accesslib.php, lib.php (fragment, get_listof_onlineexams, filters, costcenterwise count), index.php, onlineexamsenrol.php, custom_ajax.php, onlinexamdetails.php, classes/form/custom_onlineexams_form.php, classes/local/general_lib.php and user.php (self-scoped to $USER, OK); local/evaluation: db/access.php (no archetypes; grants exist only in production DB role definitions, so ...
- Partition 3.2: 21 findings; checked: local/costcenter/db/access.php: manage_multiorganizations and all costcenter capabilities default to the manager archetype (root of the class-1 finding); local/costcenter/classes/lib/accesslib.php: path SQL builder is correctly anchored ('/X/%' plus exact match), so no path-boundary overmatch; fail-open reported instead; theme/epsilon/classes/output/core_renderer.php:1755-1830 (role switch): read to understand how currentroleinfo/costcenterpath are set; local/biz_cart: db/access.php, db/services.php, cashier.php, checkout.php, history.php, receipt.php, view_transactions*.php, log_report.php, standard_log.php, lib.php, classes/external.php, classes/external/*, classes/biz_cart.php (set_user, ...

Raw data: session scratchpad `prod_audit_hits.json` / `prod_audit_p3_hits.json` (not committed).
