<div>
    <div class="w-100 pull-left p-0">
        <h3 class="watnext_head pb-15">{{# str}}what_next, local_courses{{/ str}}</h3>
    </div>
    <div class="course_buttons col-12 pull-left pt-15 pb-15">
        <div class="col-md-6 pull-left">{{# str}}doyouwantto_addthecontent, local_courses{{/ str}}</div>
        <div class="col-md-6 pull-left pr-0">
            <a href={{configpath}}/course/view.php?id={{courseid}}><button class="btn btn-primary">{{# str}}goto, local_courses{{/ str}}</button></a>
        </div>
    </div>
    <div class="course_buttons col-12 pull-left pt-15 pb-15 mt-2">
         <div class="col-md-6 pull-left">{{# str}}doyouwantto_enrolusers, local_courses{{/ str}}</div>
         <div class="col-md-6 pull-left pr-0">
            <a href={{configpath}}/local/courses/courseenrol.php?id={{courseid}}&enrolid={{enrolid}}><button class="btn btn-primary">{{# str}}goto, local_courses{{/ str}}</button></a>
        </div>
    </div>
</div>